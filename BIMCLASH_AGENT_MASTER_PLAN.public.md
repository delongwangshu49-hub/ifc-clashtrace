# IFC ClashTrace 项目总纲与进展日志

> 文档状态：`APPROVED`  
> 文档版本：`0.4.1`
> 创建时间：2026-08-25（Asia/Hong_Kong）  
> 目标完成时间：不晚于 2026-08-31 晚间（Asia/Hong_Kong）  
> 项目根目录：`<PROJECT_ROOT>`  
> 当前阶段：VG 有限逻辑修复已本地通过，精确 25 路径公开检查点待集体授权；G7A 未启动；Sites 保持所有者私有
> 当前 Gate：`VG — LOCAL_REPAIR_PASS / PUBLIC_CHECKPOINT_PENDING`（创意包已获用户确认；正式 VG 闭合仍待 GitHub 检查点；G7A/G7B/G7 未启动）

---

## 0. 文档定位与权威性

本文件同时承担以下职责：

1. 项目唯一总纲（Single Source of Truth，SSOT）；
2. 产品范围、技术路线、数据策略和验收标准的权威记录；
3. 项目阶段、决策门、停止门和回退路线的控制文件；
4. 每次工作进展、验证证据、失败、阻塞和下一步的连续日志；
5. GitHub、演示视频和公开材料形成前的隐私与发布审计依据。

若本文件与其他临时说明、聊天记录或代码注释冲突，按以下顺序处理：

1. 用户最新明确指令；
2. 本文件中已标记为 `APPROVED` 的约束与决定；
3. 本文件中的最新决策记录；
4. 其他项目文档；
5. 实现细节和临时工作笔记。

任何会改变核心规则、输入边界、数据来源、技术栈、UI 风格、部署方式或公开声明的事项，都必须先登记为待决策项；未经用户确认，不得静默改变。

---

## 1. 用户治理约束（最高优先级）

### 1.1 工作区约束

- 本项目唯一主文件夹为 `<PROJECT_ROOT>`。
- 所有项目文件、代码、依赖环境、下载样本、测试产物、截图、日志、构建产物和临时文件均须位于该目录内部。
- 项目命令的工作目录必须显式设置为 `<PROJECT_ROOT>` 或其子目录。
- 不得在 `<LEGACY_REFERENCE_ROOT>`、用户主目录或其他位置创建本项目产物。
- 如必须引用旧项目经验，只能将其作为设计参考；未经用户批准，不直接复制旧项目代码或资产。

### 1.2 本地 Git 版本管理约束

- 项目正式启动后，必须立即在 `<PROJECT_ROOT>` 根目录初始化本地 Git 仓库。
- 本地 Git 是项目开发过程、代码状态和阶段证据的权威版本历史；任何实质变更不得长期停留在无版本管理状态。
- 本地仓库不得自动配置 GitHub `origin`，不得通过命令行连接 GitHub 远程仓库。
- 所有本地 Git 命令必须在项目根目录内使用 PowerShell 7 执行。
- 每个明确步骤开始前必须记录 `git status --short --branch`；步骤完成后再次核对工作树。
- 每个步骤只有在对应测试、文档和进展日志完成后，才允许形成步骤提交。
- 一个提交只承载一个可解释的步骤或修复，不把不相关修改混入同一提交。
- 提交信息使用可追踪格式：

  ```text
  step(<STEP_ID>): <简洁的中文或英文完成说明>
  fix(<STEP_ID>): <阻塞性修复说明>
  sync(<STEP_ID>): <阶段性 GitHub 上传审计说明>
  ```

- 每个步骤提交必须可由步骤 ID、测试证据和本文件进展日志相互对应。
- 禁止为了整理历史而使用破坏性的 `git reset --hard`、强制覆盖或未经用户批准的历史改写。
- 提交前必须检查大文件、密钥、个人信息、本地绝对路径、缓存和临时产物。
- 项目日志中的阶段记录应写明对应的本地提交主题；完整或短 SHA 在提交形成后登记于同步台账或下一条日志中。

### 1.3 GitHub 阶段性上传约束

- 任何 GitHub 远程操作必须在用户已稳定登录的 Google Chrome GitHub 网页中完成。
- 禁止使用失效或不稳定的本机 GitHub 产品、`gh` CLI、GitHub API 或其他非浏览器方式执行远程维护。
- 禁止通过命令行执行 GitHub 远程仓库的创建、上传、修改、Issue、PR、Release、Tag 或仓库设置变更。
- 项目采用阶段性 GitHub 上传：每个在本总纲或当前执行计划中明确列出的步骤完成并通过验收后，上传更新一次。
- 本项目将 `G0A、G1、G2、G3A、G3B、G3C、G3、DG、G4、G4AI、G5、G6、PG、VG、G7A、G7B、G7` 视为默认阶段步骤；每个步骤从 `IN_PROGRESS` 转为 `PASS` 时形成一个 GitHub 上传检查点。
- 若某 Gate 被进一步拆分为已登记的独立执行步骤，则每个子步骤完成后也须上传一次；未登记的临时实验不单独上传。
- 阶段性上传是正式交付要求，不得把全部历史压缩成截止日的一次性上传。
- 上传内容必须来自已经通过本地验证的干净步骤快照；不得上传未通过测试、包含密钥或尚未完成脱敏的工作树。
- GitHub 网页提交信息必须包含步骤 ID 和对应本地 Git 提交短 SHA，例如：

  ```text
  G2 dataset and ground truth | local a1b2c3d
  ```

- 本地 Git 历史与 GitHub 网页提交历史是两个独立账本；通过步骤 ID、本地 SHA、GitHub 提交 URL/远程 SHA 和上传时间建立映射，不假设两者提交哈希相同。
- 每次上传后必须在 GitHub 网页核对关键文件、提交信息、页面渲染和访问权限，并将结果写入同步台账或下一条进展日志。
- 常规的“已完成步骤上传到既定仓库”视为用户的持续要求；仓库首次创建、公开可见性、删除、Release、Tag 和其他重要外部状态变更仍须单独确认。
- 同一失败的 GitHub 操作不得反复尝试；首次失败即记录错误、当前页面状态和下一种可行路线，等待用户决定。

### 1.4 PowerShell 约束

- 本项目只允许使用 PowerShell 7（`pwsh`），禁止使用 Windows PowerShell 5.x。
- 每个新执行环境首次运行 PowerShell 命令前，必须核对：

  ```powershell
  $PSVersionTable.PSVersion
  ```

- 主版本号必须不低于 `7`；否则立即停止，不得继续执行项目命令。
- 所有 PowerShell 脚本必须兼容 PowerShell 7，并避免依赖 PowerShell 5 特有行为。
- 当前已核验的 PowerShell 版本：`7.6.4`。

### 1.5 隐私与脱敏约束

- 任何拟公开内容不得包含用户真实姓名、私人邮箱、电话号码、住址、身份证明、账号信息或不必要的学校/单位细节。
- 不公开本机用户名、绝对本地路径、浏览器账户状态、API Key、访问令牌、Cookie、环境变量值或截图中的个人信息。
- 示例数据、截图和视频只使用公开授权或程序生成的模型。
- 公开报告中的本地路径必须替换为中性占位符或仓库相对路径。
- 所有截图和视频录制前关闭无关窗口、通知、浏览器个人资料信息和本地路径显示。
- 文件发布前检查 EXIF、文档元数据、Git 提交作者信息、压缩包路径和生成器元数据。
- 任何可能包含个人信息的内容，默认视为不可公开，直到完成专项审计并获得用户确认。

### 1.6 UI、前端与风格审批约束

- 在用户与助手就产品风格、视觉方向、信息架构和核心交互达成一致前，不开展正式 UI、前端样式或品牌设计工作。
- 不擅自生成“第一版视觉方案”、品牌图、Logo、配色、动效、首页文案或复杂页面布局。
- 技术验证阶段允许无样式的自动测试、命令行输出或最小技术页面，但不得把它发展为产品 UI。
- 正式前端工作必须通过 `DG — Design Gate`，审批材料至少包含：
  - 目标用户与使用场景；
  - 首屏任务与信息优先级；
  - 页面结构或低保真线框；
  - 视觉关键词与明确排除项；
  - 配色、字体、密度和 3D 视图策略；
  - 用户确认记录。
- Design Gate 未通过时，前端相关任务保持 `BLOCKED_BY_DESIGN_APPROVAL`，不得以赶进度为由绕过。

### 1.7 本轮停止边界

本轮仅创建并审校本总纲。总纲未经用户批准前，不执行以下操作：

- 不初始化产品仓库；
- 不创建项目代码目录；
- 不安装依赖；
- 不下载 IFC 数据；
- 不实现碰撞算法；
- 不创建 UI 或前端；
- 不创建或部署 Site；
- 不初始化本地 Git；
- 不访问、创建或修改 GitHub 仓库；
- 不录制或发布任何内容。

---

## 2. 项目背景与任务解释

### 2.1 外部任务

在有限时间内构建一个 Web 微原型或智能 Agent，用于对建筑模型/设计执行 1–2 条基础合规或合理性检查。提交物包括：

1. 一个 GitHub 仓库链接，包含代码与提示词；
2. 一个不超过 3 分钟的演示/介绍视频链接；
3. 按指定邮件主题提交并附简历。

### 2.2 本项目选择

本项目选择 **Web 微原型** 路线，核心能力为：

> 在浏览器中读取受控范围内的 IFC 模型，对 MEP 管线与结构构件执行确定性硬碰撞检查，并将结果映射为可追溯的 Clash Records 与 3D 审阅证据。

### 2.3 与 BIMChange-Agent 的区别

| 项目 | 核心输入 | 核心问题 | 核心输出 |
|---|---|---|---|
| BIMChange-Agent | 两个 IFC 版本 | 模型发生了什么变化 | Change Records |
| IFC ClashTrace | MEP 与结构 IFC | 哪些几何关系构成受控硬碰撞 | Clash Records |

本项目可继承旧项目的“确定性事实优先、结构化证据、失败关闭、边界声明和可复现测试”方法，但不以版本差分为功能基础。

---

## 3. 项目目标、研究问题与成功定义

### 3.1 核心目标

在 2026-08-31 晚间前完成一个范围受控、真实计算、可通过浏览器操作、可审计且适合 3 分钟演示的 IFC 硬碰撞检查 Web 微原型。

### 3.2 核心研究问题

**RQ1：** 浏览器端 IFC/WASM 几何与 BVH 相交检查，能否在受控案例上与 IfcOpenShell 参考结果保持一致？

**RQ2：** 将碰撞结果规范化为包含双方 GUID、类型、位置、容差和几何证据的 Clash Records，能否让用户快速定位和理解问题？

**RQ3：** 在不上传 IFC 到服务器的前提下，浏览器内处理能否满足小型联合模型的可接受性能与隐私边界？

### 3.3 项目成功定义

只有同时满足以下条件，项目才视为达到本次测试的主要完成标准：

1. 核心碰撞不是写死结果，而是由代码真实计算；
2. 至少 8 个受控案例具有机器可读标准答案；
3. 浏览器实现与参考实现对受控案例的碰撞构件对完全一致；
4. 能加载至少一组 buildingSMART 官方 MEP/结构样本并完成兼容性运行；
5. 每条碰撞可追溯到双方 GUID 和确定性几何证据；
6. 页面能完成“选择模型—运行—查看结果—定位构件”的闭环；
7. 无法判断时输出 `NOT_EVALUATED`，不把失败冒充通过；
8. GitHub 仓库可由 README 复现；
9. 公开材料通过隐私、许可证、密钥和本地路径审计；
10. 演示视频不超过 180 秒；
11. 对管线与结构构件实现小于 `50 mm` 的净距预警，并通过阈值、旋转几何和失败关闭测试；
12. 实现一个运行时可选、可关闭且不参与几何判定的 AI 结果解读入口；
13. 视频不包含用户本人出镜或原声，经过脚本、分镜、风格、配音、音乐、字幕、隐私与事实核验流程。
14. 首页、工作台、研发历程页、README 与演示材料中的阶段、指标、日期和链接均与权威 Gate 证据一致；未来步骤只能标为“计划中/即将开展”或保持隐藏；
15. 提供至少一组近真实工程复杂度、许可证与来源清晰的人工试用 IFC 文件包，并完成用户端到端试用记录；
16. GitHub README 使用经渲染回查的品牌动效 Logo，并具备清晰静态终态、替代文本与体积预算；
17. 成片通过质量验收后上传为 YouTube `Unlisted` 视频，配有人工校对字幕、封面与可追溯哈希，并从香港地区未登录视角验证直接观看链接可达；视频不回嵌项目首页。

---

## 4. 产品范围与明确边界

### 4.1 核心硬碰撞规则与强制净距规则

硬碰撞规则 ID：`MEP_STRUCTURE_HARD_CLASH_V1`

规则定义：

```text
Group A（MEP）:
- IfcPipeSegment

Group B（结构）:
- IfcBeam
- IfcWall

判定方式:
- 检测真实几何相交；
- 参考容差为 0.002 m；
- 仅报告超过容差的 protrusion / pierce；
- 单纯 touching 不视为硬碰撞；
- 缺少几何、坐标不一致或解析失败时不自动判定 CLEAR。
```

净距预警规则 ID：`MEP_STRUCTURE_CLEARANCE_WARNING_V1`

```text
Group A（MEP）:
- IfcPipeSegment

Group B（结构）:
- IfcBeam
- IfcWall

判定方式:
- 硬碰撞规则优先；已判定为 CLASH 的构件对不重复输出净距预警；
- 对其余可求值构件对计算可审计的最小表面净距；
- 0 mm ≤ 净距 < 50 mm：WARNING；
- 净距 ≥ 50 mm：CLEAR；
- 恰好 50 mm 不预警；
- 距离算法不可靠、几何缺失、坐标不一致或解析失败：NOT_EVALUATED；
- 页面同时显示实测净距、50 mm 阈值、双方 GUID、类型和算法/精度边界。
```

该净距规则是开发阶段与最终演示的必要功能，不再属于可选增强项。它不得复用世界轴 AABB 间距冒充一般表面净距，且不得改变 G2 已冻结的 8 个硬碰撞案例和真值。

`IfcDuctSegment` 仍仅作为候选增强项。只有硬碰撞与净距预警两条规则全部通过且不影响截止时间时，才可通过新决策记录加入。

### 4.2 初始输入边界

以下为待 Gate 验证的候选边界，不是提前声称的通用能力：

- 模型格式：未压缩 IFC STEP 文件；
- 初始 Schema 主张：精确 IFC4；
- 输入方式：一份 MEP IFC + 一份结构 IFC；
- 坐标条件：两份模型已经处于共同项目坐标系；
- 单文件候选上限：25 MiB；
- 每份模型候选 `IfcElement` 上限：5,000；
- 浏览器：桌面版当前 Chrome；
- 不保存用户上传模型；
- 不处理保密工程文件。

上述限制必须通过实际测试后锁定。若性能不足，只能收窄边界，不能静默降低检测质量。

### 4.3 Clash Record 最小契约

每条结构化结果至少包含：

```json
{
  "clash_id": "稳定且可重复的标识",
  "rule_id": "MEP_STRUCTURE_HARD_CLASH_V1",
  "status": "CLASH | CLEAR | NOT_EVALUATED",
  "element_a": {
    "model_role": "mep",
    "entity_type": "IfcPipeSegment",
    "global_id": "...",
    "name": "..."
  },
  "element_b": {
    "model_role": "structure",
    "entity_type": "IfcBeam | IfcWall",
    "global_id": "...",
    "name": "..."
  },
  "location": {
    "building_storey": "...",
    "point_a_m": [0.0, 0.0, 0.0],
    "point_b_m": [0.0, 0.0, 0.0]
  },
  "clash_type": "protrusion | pierce",
  "penetration_distance_m": 0.0,
  "tolerance_m": 0.002,
  "evidence": {
    "detector": "...",
    "model_a_sha256": "...",
    "model_b_sha256": "...",
    "selector": "..."
  }
}
```

浏览器实现若不能可靠计算穿透距离，可先将该字段设为 `null` 并说明边界；不得伪造近似值冒充精确结果。

### 4.4 最小用户闭环

```text
选择示例或本地 IFC
→ 识别模型与支持边界
→ 执行硬碰撞检查
→ 显示 CLASH / CLEAR / NOT_EVALUATED 汇总
→ 查看 Clash Records
→ 点击记录定位双方构件
→ 查看小于 50 mm 的 Clearance Warnings
→ 查看 GUID、类型、位置、容差、净距与限制
→ 可选请求 AI 对确定性结果作自然语言解读
```

### 4.5 明确不做

- 不做通用 BIM 协调平台；
- 不支持任意 CAD 格式；
- 不支持任意 IFC Schema、导出器和大型项目；
- 不自动配准坐标不一致的专业模型；
- 不开发账号、权限、团队协作或任务历史；
- 不实现完整 BCF 工作流；
- 不声称完成正式规范、消防或结构安全审查；
- 不把包围盒重叠直接称为精确碰撞；
- 不让 LLM 决定几何碰撞或净距预警是否成立；
- 不以复杂动画、品牌或页面数量代替核心算法证据；
- 不在截止日前扩展第三条无关规则。

---

## 5. 技术路线

### 5.1 推荐产品实现

```text
本地 IFC 文件
→ 浏览器 File API
→ web-ifc / WASM 解析
→ GUID、类型与网格映射
→ 候选构件筛选
→ BVH / 精确三角网格相交
→ 可审计最小表面净距
→ Clash Records + Clearance Warning Records
→ Three.js 3D 审阅
→ JSON/页面证据
```

目标是浏览器本地处理，默认不将 IFC 上传至服务器。

### 5.2 参考验证实现

```text
同一受控 IFC 对
→ Python 3.13
→ IfcOpenShell 0.8.5 geometry iterator
→ geometry tree
→ clash_intersection_many
→ Reference Clash Records
```

参考实现用于建立交叉验证，不等同于绝对真实世界真值。受控生成器的操作台账仍是标准答案的首要来源。

### 5.3 双路径验证关系

```text
受控生成器台账 ───────────┐
                         ├─ 三方一致性审计
IfcOpenShell 参考实现 ─────┤
                         │
浏览器产品实现 ────────────┘
```

若三方不一致，必须记录差异并定位原因，不得通过修改标准答案迎合产品输出。

### 5.4 部署路线

首选：OpenAI Sites 托管浏览器端应用。

部署前置条件：

1. 浏览器端 WASM、静态资源和 Worker 构建兼容性通过；
2. 不依赖 Python 原生扩展作为 Sites 运行时；
3. 私有预览通过；
4. UI/风格已通过 Design Gate；
5. 隐私、许可证和第三方通知通过；
6. 用户明确批准公开访问级别。

回退路线：若浏览器端核心在决策门前失败，构建本地 Web 微原型（Python/IfcOpenShell 后端 + 浏览器前端），并将在线站点限制为受控交互演示或项目说明。回退不等于失败，但必须诚实说明哪些计算只在本地版本中运行。

### 5.5 可选 AI 结果解读路线

AI 解读是必须实现、但由使用者主动触发的增强层；确定性检测与净距规则仍可在无 API、无网络或配额耗尽时独立完成。

```text
Clash Records + Clearance Warning Records + 已公开的规则边界
→ 最小化与结构化（不发送 IFC、网格、文件路径或浏览器元数据）
→ 用户主动点击“AI 解读”并确认将发送的字段
→ 服务端/受控代理调用经核验具有免费额度的 API
→ 自然语言摘要、风险排序建议与下一步复核提示
→ 与确定性状态分栏展示并标注“AI 生成，不改变检测结论”
```

提供商不在本次计划修订中静态指定。G4AI 开始时必须依据当日官方文档复核免费额度、速率限制、地区可用性、数据保留、商用/展示条款和密钥使用方式，再通过适配器接入至少一个候选；不得把免费额度视为永久承诺。

---

## 6. 数据与案例库计划

### 6.1 数据原则

- 只使用程序生成、公开且许可证明确、或用户明确授权的数据；
- 不使用雇主、客户、实习单位或私人项目 IFC；
- 每个外部文件必须记录来源 URL、许可证、获取日期和 SHA-256；
- 许可证不明确的文件不随公开仓库重新分发；
- 官方模型不自动等于碰撞真值；
- 合成结果不自动等于真实项目有效性。

### 6.2 第一层：受控标准答案案例

最低数量：8 个。

| Case ID | 情景 | 预期状态 | 核心目的 |
|---|---|---|---|
| C01 | 管道穿墙，明显超过容差 | CLASH | 基础墙体穿透 |
| C02 | 管道穿梁，明显超过容差 | CLASH | 基础梁体穿透 |
| C03 | 管道与墙面刚好接触 | CLEAR | 排除 touching 误报 |
| C04 | 侵入 1 mm，低于 2 mm 容差 | CLEAR | 容差边界 |
| C05 | 管道与结构保持明确间距 | CLEAR | 真阴性 |
| C06 | 管道通过已建模真实开洞 | CLEAR | 布尔开洞与误报 |
| C07 | 旋转管道斜穿墙体 | CLASH | 非轴对齐几何 |
| C08 | 构件缺少或无法解析几何 | NOT_EVALUATED | 失败关闭 |

最低类别分布：

- `CLASH`：3；
- `CLEAR`：4；
- `NOT_EVALUATED`：1。

每个案例必须具有：

- 可再运行的生成脚本；
- 固定或可重建 GUID；
- 操作台账；
- 预期构件对；
- 模型哈希；
- 许可证说明；
- 至少一次参考实现验证结果。

G2 的上述 8 个案例保持冻结。G3C 另建不改写 G2 真值的净距补充夹具，最低覆盖：`0 mm` touching、`49 mm`、恰好 `50 mm`、`51 mm`、已硬碰撞对、旋转/斜交构件、真实开洞邻近构件和不可求值几何。每个补充夹具同样需要生成依据、预期状态、路径—哈希映射、单位/坐标系声明和至少一次独立参考核验。

### 6.3 第二层：buildingSMART 官方兼容性案例

首选来源：buildingSMART `Certification-datasets`，根许可证为 CC BY 4.0。

候选文件：

- `IFC 4.0.2.1 (IFC 4)/PCERT-Sample-Scene/Building-Hvac.ifc`
- `IFC 4.0.2.1 (IFC 4)/PCERT-Sample-Scene/Building-Structural.ifc`

用途：

- 多专业模型共同加载；
- Schema 与几何兼容；
- 共同坐标；
- GUID 映射；
- 性能与错误恢复；
- 候选碰撞人工复核。

限制：该官方数据没有公开的碰撞构件对标准答案，因此只能作为兼容性与外部样本，不能用于未经人工审计的准确率结论。

### 6.4 第三层：异常与压力案例

候选来源：IfcOpenShell Public Test Files 或其他许可证明确的几何测试集。

用途：

- 无几何；
- 错误几何；
- 缺失布尔减法；
- 过大包围盒；
- 超时或解析异常；
- 非法/退化三角形。

纳入条件：

1. 来源和许可证可确认；
2. 不包含隐私或保密数据；
3. 对核心失败模式有直接价值；
4. 不因数量增加而挤压核心测试时间。

### 6.5 数据清单契约

计划建立 `data/dataset-manifest.json`，每个数据项记录：

- `case_id`；
- `origin`；
- `source_url`；
- `license_spdx_or_name`；
- `attribution`；
- `schema`；
- `file_sha256`；
- `expected_status`；
- `ground_truth_basis`；
- `redistribution_permitted`；
- `limitations`。

---

## 7. 评价设计与量化验收

### 7.1 正确性指标

在 8 个受控案例上计算：

- 碰撞构件对 Precision；
- 碰撞构件对 Recall；
- 碰撞构件对 F1；
- 状态准确率；
- 浏览器与参考实现一致率；
- GUID 映射完整率；
- 未支持输入的失败关闭率。

核心验收门：

| 指标 | 最低要求 |
|---|---:|
| 受控碰撞构件对 Precision | 100% |
| 受控碰撞构件对 Recall | 100% |
| C03 touching 误报 | 0 |
| C04 低于容差误报 | 0 |
| C06 开洞误报 | 0 |
| C08 错误判为 CLEAR | 0 |
| 浏览器/参考结果构件对一致率 | 100% |
| 受控案例 GUID 映射完整率 | 100% |

若无法达到以上要求，必须收窄规则或输入边界；不得以平均分掩盖关键误报。

### 7.2 兼容性指标

对 buildingSMART 官方 HVAC/Structural 样本记录：

- 两个文件能否打开；
- 支持 Schema；
- 相关构件数量；
- 可生成几何数量；
- 失败构件数量及原因；
- GUID 映射成功率；
- 联合场景是否保持共同坐标；
- 运行是否崩溃；
- 碰撞候选数量；
- 人工复核状态。

官方样本通过标准：

- 两文件均成功打开；
- 页面不崩溃；
- 所有被报告碰撞均包含双方 GUID；
- 解析失败有明确诊断；
- 不宣称未人工验证的候选碰撞为真实工程错误。

### 7.3 性能指标

候选目标，须在实际测试后锁定：

| 场景 | 测量方式 | 候选目标 |
|---|---|---:|
| 受控小型案例 | 3 次干净运行中位数 | ≤ 3 秒 |
| 官方 HVAC/Structural 联合案例 | 3 次运行中位数 | ≤ 30 秒 |
| UI 点击结果到高亮 | 5 次操作中位数 | ≤ 300 ms |
| 页面阻塞或崩溃 | 全测试 | 0 |

若官方样本超过 30 秒但仍稳定，允许作为已知限制记录，不得为达成数字而降低几何精度。

### 7.4 最小可用性测试

在最终候选上执行三个任务：

1. 选择示例并运行检查；
2. 找到一条指定碰撞；
3. 说出双方 GUID、构件类型和判定依据。

最低验收：

- 任务完成率：3/3；
- 无需阅读源码；
- 不超过一次外部提示；
- 总任务时间目标：≤ 3 分钟；
- 记录至少一项用户困惑或确认“无明显阻塞”。

### 7.5 自动测试最低构成

- 8 个受控案例端到端测试；
- Schema/Clash Record 合约测试；
- 容差边界测试；
- touching 排除测试；
- 缺失几何失败关闭测试；
- 文件类型/大小/Schema 输入守卫测试；
- GUID 映射测试；
- 已提交基线不可被测试静默覆盖：生成应在隔离目录执行或在运行前后校验路径—哈希映射与工作树洁净性；
- 契约突变负向测试：修改 `rule_id`、Schema、单位、坐标系、容差、案例路径或哈希时必须明确失败；
- 薄构件、旋转几何和阈值上下边界测试，证明一般容差判定不依赖 AABB 重叠冒充穿透深度；
- 隐私测试：输出不得包含绝对本地路径；
- 构建与页面启动烟雾测试。
- 净距规则阈值测试：`49 mm` 为 `WARNING`、`50 mm` 与 `51 mm` 为 `CLEAR`，touching 按净距规则预警，硬碰撞对不得重复预警；
- 净距规则旋转/斜交、开洞与缺失几何失败关闭测试；
- AI 解读适配器的模拟响应、超时、限流、格式错误、提示注入和无网络降级测试；默认自动测试不得消耗真实 API 配额。

不以测试数量代替覆盖质量；每项测试必须对应明确风险或契约。

---

## 8. 阶段、决策门与停止门

### 步骤完成与版本检查点通用协议

除尚在等待批准的 G0 外，任何 Gate 或已登记子步骤只有同时完成以下事项，才可从 `IN_PROGRESS` 转为 `PASS`：

1. 完成该步骤规定的产出，并通过对应测试或人工验收；
2. 更新本总纲的进展日志、决策记录和必要文档；
3. 完成密钥、个人信息、本地路径、大文件、许可证与临时产物检查；
4. 使用 PowerShell 7 核对本地 `git status --short --branch`，确认提交范围只属于本步骤；
5. 形成一个可解释的本地 Git 步骤提交，并取得本地提交短 SHA；
6. 使用用户已登录的 Google Chrome GitHub 网页，将该步骤的已验证快照上传一次；
7. GitHub 网页提交信息包含步骤 ID 与本地短 SHA；
8. 在网页回查关键文件、渲染、访问权限和提交记录；
9. 在 `PROGRESS_SYNC.md` 或本总纲日志中登记步骤 ID、本地 SHA、GitHub 提交 URL/远程 SHA、上传时间、核验结果和异常；
10. 仅当本地提交与 GitHub 阶段同步均为 `PASS` 后，才进入下一步骤。

若技术验收通过但网页上传失败，则该 Gate 记录为 `TECH_PASS_SYNC_BLOCKED`，不得将其描述为完整完成；默认暂停下一步骤，直到同步恢复或用户明确批准例外。临时试验、失败探索和同一步骤内的中间修改可以保留在本地提交中，但不替代步骤完成时的一次正式上传。

### G0 — 总纲人工审批

通过条件：

- 用户确认项目方向、规则、时间表和治理约束；
- 用户允许正式开始项目；
- 未决问题已登记。

当前状态：`PASS`（用户已明确批准 0.2.0-draft 修订稿并指示立即开始 G0A）。

停止门：未经用户批准，不进行项目实现。

### G0A — 本地版本库初始化与首个阶段上传

目标：在用户批准本总纲并明确开始后，为全项目建立可追溯的本地与远程版本基线。

必须完成：

- 使用 PowerShell 7 在 `<PROJECT_ROOT>` 根目录初始化本地 Git 仓库；
- 创建并审查 `.gitignore`，排除密钥、缓存、依赖、临时文件、私有输出与不适合公开的大文件；
- 将用户批准后的本总纲作为首个基线快照，更新日志并形成 G0A 本地提交；
- 检查 Git 作者信息是否适合未来公开；不得擅自公开用户真实身份；
- 经用户确认仓库名称与可见性后，在其已登录的 Chrome GitHub 网页创建或打开目标仓库；
- 形成 G0A 的公开候选清单；本地总纲中的绝对路径等治理信息不得未经脱敏直接上传，必要时生成内容一致但已脱敏的公开版计划；
- 通过 GitHub 网页上传经脱敏的基线文件，提交信息包含 `G0A` 与对应本地短 SHA；
- 回查网页内容、权限和提交记录，并登记本地—远程映射。

通过条件：本地仓库、首个本地提交、首个 GitHub 网页检查点和同步记录全部为 `PASS`。

当前状态：`PASS`（本地基线提交、GitHub 网页提交、文件/渲染/权限回查与映射登记均已通过）。

停止门：G0A 未通过，不进入 G1；不得为绕过网页上传而配置 GitHub 远程或改用命令行上传。

### G1 — 技术可行性

目标：证明两条几何路线至少有一条可行。

当前状态：`PASS`（双路线技术验收、本地步骤提交、GitHub Chrome 网页上传、远程文件树/提交链回查与本地映射登记均已通过）。

必须证明：

- IfcOpenShell 参考碰撞路线可运行；
- 浏览器/Node 环境可以解析至少一个受控 IFC；
- 能把几何结果映射回 GUID；
- 能检测至少一个已知硬碰撞；
- 记录使用的坐标、单位和容差。

截止建议：2026-08-26 晚间。

停止/回退门：若浏览器路线在截止前不能稳定返回一个已知碰撞，冻结浏览器检测实验，切换本地 Python Web 核心，不继续无界调试 WASM。

### G2 — 数据与标准答案

目标：冻结可复现的最小案例库。

当前状态：`PASS`（8 个受控案例、操作台账、机器真值、清单/哈希/许可证、参考结果、无留出限制声明、本地步骤提交与 GitHub Chrome 网页检查点均已验收）。

必须完成：

- 8 个受控案例；
- 操作台账；
- 机器可读标准答案；
- 数据清单、哈希和许可证；
- 参考实现结果；
- 开发集/最终验收集划分或明确不做留出评测的限制。

截止建议：2026-08-27 晚间。

停止门：没有标准答案，不继续制作产品结果页。

### G3A — 契约测试加固

目标：关闭 G2 全量审计发现的“测试可能通过再生成掩盖契约漂移”风险，建立可信的 G3 验收基线。

当前状态：`PASS`（隔离生成、冻结路径—SHA、精确契约断言、8 类突变负向测试、G1/G2 回归、本地步骤提交及 Chrome GitHub 网页同步闭环均已通过）。

必须完成：

- 将确定性比较从无路径的哈希集合升级为稳定的仓库相对路径—SHA-256 映射比较；
- 显式断言批准的 `rule_id`、`schema=IFC4`、`length_unit=metre`、`coordinate_system=shared_project_coordinates` 与 `tolerance_m=0.002`；
- 生成测试不得静默修复已提交产物：优先在项目内忽略的隔离目录生成并与冻结基线比较；若必须原位生成，则运行前后必须断言 Git 工作树和冻结文件哈希不变；
- 增加最少一组契约突变负向测试，证明状态、路径、哈希、单位、Schema、坐标系或容差漂移会使测试失败；
- 保持 G1/G2 正向回归、16 个 IFC 字节确定性和 8/8 真值匹配通过。

停止门：G3A 未 `PASS` 时，不接受任何“测试全绿”作为 G3 规则验收证据，不开始正式产品 UI。

### G3B — 容差语义与 AABB 守卫隔离

目标：保留 C04 受控证据，同时阻止最小世界坐标 AABB 重叠被推广为一般穿透深度或通用容差算法。

当前状态：`PASS`（G3B-R1 已关闭薄构件假阴性、严格阈值死区与审计脚本回归；本地验收、Chrome GitHub 连续技术链、网页内容回查与映射登记均已完成）。

必须完成：

- 将现有 AABB 守卫明确限制在 C04 受控夹具/参考证据路径，其他案例不得隐式继承该分类规则；
- G3 浏览器产品实现不得以 AABB 重叠直接输出精确碰撞、穿透距离或通用 CLEAR；
- 为 2 mm 容差选择并记录可解释的几何语义；优先验证三角网格交线、点到表面/体内深度或其他可审计方法，若不能可靠求值则失败关闭为 `NOT_EVALUATED`；
- 增加薄结构、旋转/斜交和 `0.002 m` 阈值下方/等于/上方案例，验证不会因世界轴 AABB 而产生假阴性或假精度；
- 继续保持 C03 touching、C04 1 mm、C06 真开洞和 C08 缺失几何的既有合同结果。

停止门：未证明一般容差语义或未明确收窄支持边界时，不得将 G3 标记为 `PASS`，不得进入 Design Gate。

### G3C — 50 mm 净距预警规则

目标：在不降低硬碰撞规则可信度的前提下，实现同一受控构件范围内的表面净距预警，并建立独立于 G2 冻结真值的补充验收集。

当前状态：`PASS`（本地技术验收、发布审计、权威步骤提交、Chrome GitHub 连续技术链与网页回查均已完成；最终映射按账本自引用闭合规则登记）。

必须完成：

- 定义并实现 `MEP_STRUCTURE_CLEARANCE_WARNING_V1`，仅处理 `IfcPipeSegment` 对 `IfcWall`/`IfcBeam`；
- 硬碰撞优先且去重：已为 `CLASH` 的构件对不再生成 Clearance Warning Record；
- 使用可解释的最小表面距离方法，禁止用世界轴 AABB 间距冒充一般净距；
- 建立 `0/49/50/51 mm`、旋转/斜交、开洞邻近、硬碰撞去重和缺失几何补充夹具；
- 输出双方 GUID、类型、测得净距、阈值、单位、证据来源、算法边界与 `WARNING/CLEAR/NOT_EVALUATED`；
- 自动测试证明 `49 mm` 预警、`50 mm` 不预警、计算失败不冒充 `CLEAR`，并保持 G1/G2/G3A/G3B 回归通过。

停止门：净距算法、阈值边界、去重或失败关闭任一未通过时，G3 不得 `PASS`，页面不得宣称支持 50 mm 净距检查。

### G3 — 核心检测引擎

目标：浏览器产品实现通过受控规则契约。

当前状态：`PASS`（浏览器兼容双规则核心、Node/当前 Chrome 受控验收、全量回归、公开候选审计、本地步骤提交、GitHub 网页检查点与最终证据闭合均已完成；后续有限审计修复单列为 G3-R1）。

必须完成：

- 两模型输入；
- 构件筛选；
- 几何相交；
- 容差；
- Clash Records；
- Clearance Warning Records；
- 失败关闭；
- 自动测试。

截止建议：2026-08-28 晚间。

停止门：G3A/G3B/G3C 未通过，或两条规则在各自受控验收集上的 Precision/Recall 未达到 100%，先修复契约证据或收窄边界，不增加 UI 或第三条规则。

### DG — Design Gate

目标：用户批准 UI、前端与视觉方向。

必须提交用户审阅：

- 页面信息架构；
- 低保真线框；
- 交互流程；
- 视觉关键词与排除项；
- 3D 视图与结果列表关系；
- Clash、净距预警与可选 AI 解读之间的信息层级、触发方式和免责声明；
- 中英文与品牌策略；
- 移动端是否在范围内。
- 主页、功能页与研发历程页的路由/新标签页关系、下滑介绍结构和页脚链接；
- 大众版与黑白灰极简版双风格、简中/英语、亮/暗外观和 AI 解读开关的偏好契约；
- 字体家族/语义字号统一、按钮文字与图标居中及说明完整性验收要求。

状态：DG R4 `PASS`；DG-R1 `PASS`（三项限定审计修复、本地负向守卫、G3 回归、精确 5 文件 Chrome 检查点、网页核验与初始本地—远程映射均已通过。正式 UI 与 G4 仍须另行授权）。

停止门：用户未批准，不实施正式前端样式和风格化内容。

### G4 — Web 纵向切片

目标：完成可操作的完整闭环。

当前状态：基础 G4 `PASS`；G4-R1 `PASS`（自选模型状态失效、共享坐标确认撤销、3D 重新载入与历史 PNG 审计误报已修复；本地回归、授权的 Chrome 公开修复与网页核验均已闭合）。

必须完成：

- 文件/示例选择；
- 边界与错误提示；
- 运行状态；
- 汇总与结果列表；
- 证据详情；
- 3D 定位；
- 受控示例一键体验。

G4 的无 AI 基础闭环必须同时展示硬碰撞与净距预警，并在断网条件下完整可用。

截止建议：2026-08-29 晚间。

### G4AI — 可选 AI 结果解读

目标：接入至少一个在实施当日经官方资料核验、具有可用免费额度的 API，将确定性结果转换为更易读的自然语言说明，同时保持隐私边界与确定性结论权威性。

当前状态：基础 G4AI、G4AI-R1 与当前 G4AI-R3 成段分析路径均为 `PASS`。2026-08-28 的提供商选择、最小字段/二次确认、服务端密钥边界、模拟故障降级、双语与四次逐次授权 live 证据保持有效。2026-08-29 三轮人工试用依次修复了分析过度克制与入口过深、重复 AI 模块与冗长动作、以及面向公众不适合的账户/API/内部错误码措辞。G4AI-R2 把重试改为重新预览并取得新同意，以单调操作身份隔离异步结果，并把单次成段分析容量收紧为六条记录；两次分别获批的当时合同 live 均失败关闭。G4AI-R3 把提供商 strict schema 收敛到 Groq 当前文档结构子集，把 completion cap 提高到 `1,600`，固定 temperature 为零并细分失败类别；随后在用户明确授权“直到功能成功实现”的五次生成 review-pack 请求中，修复了大小写不敏感状态词误报，并通过 schema 描述、提示词和本地守卫拒绝未支持的安全、可施工、设计意图、责任归属、误报/漏报、合规/认证及物理改造建议。最终英文与简体中文 provider 结果均通过，确定性 `1/1/1/1`、记录、证据和控制台保持不变。严格停止在 G4AI-R3，不进入 G5。

必须完成：

- 建立提供商对比表，记录官方文档 URL、核验日期、免费额度、速率限制、地区/账号要求、数据保留、使用条款、模型名称和退场条件；
- 使用提供商中立适配器，API 变更时不重写检测核心；密钥只存在于本地环境或部署密钥存储，不进入浏览器包、仓库、日志或视频；
- 用户主动触发前展示数据发送说明；只发送必要的结构化记录和公开规则边界，不发送 IFC、网格、文件名、绝对路径或其他模型内容；
- AI 输出只可解释、归纳和提出复核建议，不得新增/删除记录、改变 `CLASH/WARNING/CLEAR/NOT_EVALUATED` 或伪造净距；
- 页面明确区分“确定性检测结果”和“AI 生成解读”，提供复制、重试、取消和关闭功能；
- 实现超时、限流、配额耗尽、无网络、提供商不可用和畸形响应降级；失败时回到确定性模板说明，核心页面继续可用；
- 以模拟 API 完成自动测试，以脱敏合成记录完成一次受控 live smoke test；公开 CI 不要求真实密钥或消耗真实配额。

停止门：若没有候选同时满足免费额度、密钥安全、数据边界和演示稳定性，则不得仓促暴露密钥或上传 IFC；记录阻塞并保留确定性模板解释，不影响硬碰撞与净距核心结果，但整体扩展范围保持未完成。

### G5 — 评价与外部样本

目标：形成可复核的结果而非单个 Demo。

当前状态：`PASS`（本地受控指标、净距补充、三方一致性、buildingSMART 官方样本、性能、失败/限制、最小 Chrome 可用性与 AI 事实保持/降级/时延/人工可读性证据均已通过；精确 10 文件技术/证据公开链已获行动时授权并通过 Chrome 父链、文件数与路径回查，公开总纲和最终台账按自引用规则闭合；严格停止在 G5）。

必须完成：

- 受控案例指标；
- 净距补充夹具指标；
- 三方一致性审计；
- buildingSMART 官方样本兼容性运行；
- 性能记录；
- 失败案例与限制；
- 最小可用性测试；
- AI 解读的事实保持率、降级行为、首字节/完成时延和至少一轮人工可读性检查。

截止建议：2026-08-30 中午。

### G6 — 部署候选与隐私审计

目标：形成可公开但尚未公开的候选版本。

当前状态：`PASS`（所有者私有版本 4 已通过用户基本体验验收，访问仍为单一所有者、无组/外部访客/托管变量。用户明确授权 0.4.0 审计后的精确 29 文件方案；Chrome 前置回查确认仓库为 `Public/main` 且 G5-R1 尾为 `1d4380806f741563d4179395ff76e91c83485940`。因 GitHub 网页选择器不能在一次混合选择中保留 `app` 子目录，相同 27 文件技术范围由原 7 组安全拆为 9 笔连续提交，精确文件数 `1/1/1/4/1/5/6/1/7`，技术尾 `e7217d65cfe43fc965c8c0a192656113a3a11aaf`；标题、父 SHA、路径和文件数逐笔回查通过。公开总纲与最终台账按账本自引用规则依次登记，最终台账远端 SHA 只由随后一个不改文件的本地 `sync(G6)` 提交记录，不递归重发。Sites 仍不公开；严格停止在 G6）。

必须完成：

- 构建通过；
- 私有预览；
- 许可证与第三方通知；
- 密钥扫描；
- PII、本地路径、元数据与截图审计；
- AI API 数据流、密钥、日志、配额和提供商条款审计；
- README、PROMPTS、架构和评价结果；
- 用户人工验收。

截止建议：2026-08-30 晚间。

停止门：未通过隐私或许可证审计，不得公开。

### PG — 展示就绪 Gate

目标：在视频脚本冻结与正式录制前，把网站事实口径、品牌入口和人工试用证据收敛为一套可公开、可演示、可回查的展示基线。

当前状态：`IN_PROGRESS`（PG-C、PG-B 与已发布 PG-B-R1 为 `PASS`；PG-B-R2/PG-E 本地修复候选为 `LOCAL_REPAIR_PASS_EXTERNAL_SYNC_PENDING_USER_HOSTED_UAT_PENDING`；PG 整体尚未通过）。先前29文件公开链与 Sites 版本9/10保持历史有效；当前30路径修复候选尚未公开，VG 与 G7 系列保持 `NOT_STARTED`。

#### PG-C — 全站内容与进度口径审计

- 建立页面主张台账，逐项盘点首页、工作台、研发历程页、README、导航/页尾、元数据、错误/空状态和演示截图中的 Gate、状态、日期、指标、链接与能力主张；
- 以本总纲、`PROGRESS_SYNC.md`、测试证据和部署回查为权威来源，把研发历程从仅停留在 DG 的旧状态更新到实际已闭合进度，并同时覆盖简中/英语、大众体验/工程极简、亮色/暗色；
- 在 1024/1280/1433 px 与当前 Chrome 中检查三页内容、跳转、折行和可读性，私有候选与公开构建不得出现不同事实口径；
- 为当前 Gate、关键指标与证据链接建立自动守卫，发现过期状态、空目标、未来步骤冒充已完成或中英文语义不一致时测试失败；
- 允许预先编写从当前 Gate 之后直至项目结束的所有后续阶段文案和组件，但源文件必须标记为 `DRAFT/PLANNED`；若在页面提前显示，只能明确写成“计划中/即将开展/尚未开始”，不得使用 `PASS`、完成式措辞，也不得伪造完成状态、日期、指标、链接、验收、公开可用性、权限或证据。预写不构成阶段启动、Gate 通过、用户验收、外部写入、部署/公开访问授权，也不得绕过任何停止门；只有对应 Gate 的真实证据与必要授权均形成后，才可改为已完成。

#### PG-B — 品牌资产与 GitHub 静态 Logo

- 复用 D-026/D-027/D-028 已批准的墙体—管线—碰撞—提示气泡 Logo、橘红强调色与静态终态，不重新启动品牌方向探索；
- 使用用户最终选定的透明 RGBA PNG 原文件作为 GitHub README Logo，不再制作或引用 GIF、动效回退、生成图层或构建脚本；
- 冻结原文件尺寸、透明度、体积、SHA-256 与来源，README 以受控显示宽度避免大图压迫正文，并清除旧动效链的发布歧义；
- 在 GitHub README 桌面/窄屏视角核对透明度、清晰度、布局、替代文本、失效回退和页面加载体积，并确保网站现有 Logo 与 README 资产保持同一品牌系统；
- 任何 GitHub README 写入仍须遵守行动时授权与 Chrome 网页流程，PG-B 本地完成不自动授权远程修改。

#### PG-E — 近真实工程样例与人工试用

当前状态：`IN_PROGRESS / LOCAL_REPAIR_PASS_EXTERNAL_SYNC_PENDING_USER_HOSTED_UAT_PENDING`。程序生成的单层双 IFC、六个独立哨兵、隔离再生成与发布核心 6/6 技术预检继续通过；当前口径明确为7条具象管线加1个缺表示导入管段，88条候选记录中77条完成评估、11条失败关闭。结构化 UAT 与远端活体验证守卫已加入；当前30路径修复候选尚待集体授权、公开同步与最终托管试用，因此不得标记 `PASS`。

- 建立至少一组“工程情境化但不冒充真实项目”的 MEP/结构双 IFC 样例：含楼层/轴网或等价空间组织、多段管线、墙/梁、硬碰撞、49 mm 净距预警、50 mm 边界、明确安全间隔和至少一个失败关闭情形；
- 样例优先程序生成；若使用公开模型，必须记录来源、许可证、哈希、修改与再分发边界。禁止使用雇主、客户、学校或其他保密工程文件；
- 为关键哨兵构件对建立独立预期结果，但不把整份复杂模型包装成已证明的真实工程准确率；兼容性未知或超出冻结输入边界时如实输出 `NOT_EVALUATED`；
- 由用户在最终候选网站完成端到端人工试用：选择/加载两份文件、确认支持边界、运行、筛选、3D 定位、查看证据、触发或关闭 AI、重试/取消、切换语言/主题并重复运行；
- 记录浏览器版本、文件尺寸、元素/候选对/结果数量、耗时、峰值内存（可可靠取得时）、控制台错误、观察问题与修复复验。大文件或许可不适合公开时保留为本地 UAT 包，并另备可公开缩减版。

通过条件：PG-C、PG-B、PG-E 全部通过；用户已试用近真实样例；网站没有已知的过期或未来冒充完成的公开主张；Logo 资产完成本地与 GitHub 渲染候选审计；形成 PG 本地提交、GitHub 网页检查点与映射记录。

停止门：PG 未 `PASS` 时，不冻结 VG 最终脚本，不开始正式录屏、最终旁白或成片剪辑。

### VG — 视频创意、脚本与素材 Gate

目标：在正式录制和生成配音前冻结一套可在 180 秒内完成、事实准确且不披露用户身份的制作方案。

当前状态：`LOCAL_REPAIR_PASS / PUBLIC_CHECKPOINT_PENDING`；创意组合已获用户确认并冻结 H、M2、P1。有限修复把英语台本收敛至 272 词并逐镜满足 `114 wpm + 420 ms`，澄清 M2 裁切后首拍为零，冻结 9 个 video-shotcraft 卡片的精确文档/演示/hash，并把 S11 固定为一次加载四记录受控批次。G7A 仍为 `NOT_STARTED`；正式 VG 闭合只差经用户集体授权的精确 25 路径 GitHub 检查点。

必须提交用户审阅：

- 单一核心叙事、目标受众、片长预算和逐句脚本；
- 逐镜头分镜/shot list，标明真实录屏、字幕、局部放大、转场与备用镜头；
- 候选视觉风格帧；默认方向为克制、干净、工程审查感，不使用喧宾夺主的生成动画；
- Codex/AI 合成旁白的语言、音色、语速、停顿和专有名词读法样例；
- 音乐使用或不使用的决定；如使用，只选无歌词、弱存在感且许可明确的音乐，并记录来源和授权；
- 屏幕隐私清单、素材许可证清单、字幕方案和事实/指标出处表；
- 用户对脚本、风格、旁白和音乐方案的确认记录。
- 关键帧预处理表及其可复建配方；它只冻结构图、信息层级与字幕安全区，不是最终视频帧；
- 对任何付费语音、生成服务或额度外费用进行显式披露；用户未另行同意前一律采用零费用路径或停止。

停止门：在精确 25 路径公开检查点完成并复验、VG 正式转为 `PASS` 前，不开始最终旁白合成、正式录屏、实时 AI、Remotion 成片、最终字幕/音效/混音或渲染。

### G7A — 视频成片制作与质量验收

目标：严格依照已批准的 VG 脚本和 PG 展示基线完成录屏、AI 旁白、字幕、后期与成片 QA。

必须完成：

- 演示视频 `≤180 秒`，至少展示一次近真实样例或受控样例的真实连续计算；
- 不包含用户本人出镜或原声，不泄露账号、通知、本地路径、私有 URL、API Key 或私人窗口；
- 旁白、字幕、页面状态、指标与链接逐句核对，未来计划不得表述为已完成成果；
- 音乐、字体、生成资产与第三方素材具有明确许可记录；
- 导出文件完成画面、音频、字幕、事实、隐私、许可证和常见分辨率质量验收，并经用户确认最终版本。
- 主制作任务推荐 `gpt-5.6-sol` + `xhigh`；只在意图已冻结的机械性 render/test/fix 循环临时降为 `high`，并仅在工期与可用额度允许时用 `max` 做一次导出前有界失败搜索；
- Remotion、video-shotcraft、其他已安装技能/插件及其免费额度可在 G7A 内以质量优先自由使用；付费购买、超额费用、公开发布、访问变化或新增隐私暴露仍须单独授权。

停止门：G7A 未 `PASS`，不得把候选视频标为最终视频，也不得上传 YouTube 或形成提交链接。

### G7B — YouTube 上传与最终视频链接复验

目标：在成片通过 G7A 后，将最终视频上传为可由香港教授团队直接打开的 YouTube `Unlisted` 视频，并形成适合邮件提交的稳定直链；不修改或重新部署项目首页。

必须完成：

- 在上传前由用户确认 YouTube 频道、频道显示名/头像、视频标题、说明、封面、语言、字幕、评论与可见性不会泄露不希望公开的身份或账号信息；
- 只上传 G7A 已确认的最终 MP4，登记本地文件 SHA-256、时长、分辨率、编码、音频、版本与上传时间；完整 MP4 不进入普通 GitHub 仓库；
- 使用 YouTube Studio 上传，原始语言设为实际旁白语言，可见性精确设为 `Unlisted`；明确记录“持链接者可观看和转发、通常不出现在搜索和频道视频列表中，但并非严格私密”；
- 设置克制且准确的标题、项目说明与封面；说明只包含已经验证的功能与限制，并在需要时提供 GitHub 和在线工具的直接链接；
- 上传并人工复核简体中文与英文字幕文件，核对 `IFC`、`MEP`、`50 mm`、`2 mm`、状态名、数字、单位与时间轴；不得只依赖未校对的自动字幕；
- 等待 YouTube 必要处理与检查完成，确认 `≤180 秒`、目标清晰度可选、音画同步、封面正确、无阻塞性版权/政策问题，并记录最终 watch URL 与视频 ID；
- 使用香港网络的 Chrome 无痕/未登录视角打开 YouTube 直接链接，复验无需申请权限即可播放、拖动、开启中英文字幕、查看说明与链接；再以窄屏或移动视角做一次基本播放检查；
- 将最终 YouTube 直接链接登记到 README、视频制作记录和提交材料清单，按既定 Chrome GitHub 流程形成 G7B 检查点；邮件正文提交 YouTube 直接链接，不以项目首页代替；
- 保留最终 MP4、字幕、封面和哈希的本地受控备份。若 YouTube 上传或访问失败，只记录阻塞并由用户决定是否启用 Google Drive 等备用承载，不静默更换平台。

停止门：未获用户对 YouTube 上传与频道身份的行动时确认、可见性不是精确 `Unlisted`、处理/版权检查存在阻塞、字幕或事实未通过、链接要求登录/申请权限，或香港未登录播放失败时，G7B 保持 `BLOCKED`，不得发送提交邮件。

### G7 — GitHub、视频与提交材料封版

目标：在全过程阶段检查点已完整同步的基础上，完成最终外部交付。

必须完成：

- 通过用户 Google Chrome 登录会话完成 GitHub 远程操作；
- 审计 G0A 至 G7B 的步骤、本地 SHA、GitHub 提交与上传时间映射，缺项必须补齐并说明；
- 完成 G7 最终本地提交与对应 GitHub 网页检查点；
- 仓库链接有效；
- 依照第 15 节完成视频预制作、画面/音频制作、后期、质量核验、YouTube `Unlisted` 上传与直接链接复验；
- 演示视频 ≤ 180 秒且至少展示一次真实计算；
- 视频不包含用户本人出镜或原声，不泄露用户身份、通知、账号、文件路径或私人窗口；
- 视频链接有效且权限正确；
- 邮件正文和简历附件由用户最终确认；
- 公开访问前取得用户明确授权。

目标代码冻结时间：2026-08-31 17:00。  
最终完成时间：2026-08-31 晚间。

---

## 9. 2026-08-25 至 2026-08-31 倒排计划

| 日期 | 主任务 | 必须产出 | 当日停止条件 |
|---|---|---|---|
| 8月25日晚 | 总纲与方向冻结 | 本总纲、用户审批意见 | 未批准不开发 |
| 8月26日 | G1/G2 提前完成；扩展范围修订与 G3A 准备 | 已冻结 G2、0.3.0 总纲、契约加固执行清单 | 未登记范围不进入实现 |
| 8月27日 | G3A/G3B/G3C 与核心引擎 | 基线不可变测试、一般容差语义、50 mm 净距夹具与记录、Clash Records、失败关闭 | 三个前置 Gate 任一未通过不验收 G3 |
| 8月28日上午 | 核心引擎收口与 Design Gate | 两条规则受控集 100% 通过、设计简报/线框、AI 解读信息层级 | G3 或 DG 未通过不写正式样式 |
| 8月28日下午—29日上午 | 经批准的 Web 纵向切片 | 硬碰撞 + 净距预警闭环、3D 定位、断网可用 | 不增加非核心页面 |
| 8月29日下午 | G4AI 与 G5 评价 | 免费额度 API 候选核验、可选 AI 解读、降级测试、官方样本与指标 | 不暴露密钥，不让 AI 改写结果 |
| 8月30日 09:00–11:00 | G6 收尾与 0.4.0 候选重审 | 更新后的精确候选清单、GitHub 行动时授权、网页回查与 G6 映射 | G6 未 `PASS` 不启动 PG |
| 8月30日 11:00–15:00 | PG-C 全站内容与进度口径审计 | 页面主张台账、研发历程更新、双语/双风格/亮暗/链接与自动守卫 | 存在过期状态或未来冒充完成时不通过 |
| 8月30日 15:00–18:00 | PG-B 品牌与动效 Logo | GitHub 渲染候选、静态终态、README 布局与体积/可访问性记录 | 渲染不稳定或无静态终态时不上传 |
| 8月30日 18:00–21:00 | PG-E 近真实样例与人工试用 | 合成工程情境双 IFC、哨兵预期、UAT 清单与用户试用记录 | 许可、性能、控制台或关键闭环问题未关闭时不通过 |
| 8月30日 21:00–23:00 | VG 视频预制作与用户确认 | 165–175 秒脚本、分镜、风格帧、旁白样例、音乐/字幕方案、录制清单 | 未确认不做最终旁白与正式录屏 |
| 8月31日 09:00–12:00 | G7A 视频画面与音频制作 | 干净录屏、备用镜头、Codex/AI 旁白、已授权音乐/无音乐版本 | 真实计算镜头或隐私检查失败则重录 |
| 8月31日 12:00–15:00 | G7A 剪辑、字幕与质量核验 | 最终成片、字幕、混音、事实/隐私/许可证/音画同步检查 | 超过 180 秒、结论不准确或用户未确认不得发布 |
| 8月31日 15:00–16:30 | G7B YouTube 上传与直链复验 | `Unlisted` 视频、双语字幕、封面、哈希/视频 ID、香港未登录播放记录 | 身份、处理/版权、字幕、权限或地区访问失败则不提交 |
| 8月31日 16:30–17:00 | G7 最终交付封版 | 最终仓库、站点/视频链接、提交清单、无痕访问回查 | 17:00 后只修阻塞问题 |

版本检查点要求：总纲批准后的首项工作为 G0A。此后，每个当日主步骤或已登记子步骤通过验收后，必须在当日完成“日志更新 → 本地 Git 提交 → Chrome GitHub 网页上传 → 网页回查 → 映射登记”；未完成同步时，不把下一步骤标记为已开始。若一天内完成多个已登记步骤，则分别上传，不合并为一次笼统更新。

### 时间预算建议

| 工作包 | 预算 |
|---|---:|
| 技术可行性与回退判断 | 6–8 小时 |
| 受控数据与标准答案 | 8–10 小时 |
| G3A/G3B 契约与容差前置 | 5–8 小时 |
| G3C 50 mm 净距规则与补充夹具 | 6–10 小时 |
| 双规则核心引擎与测试 | 10–14 小时 |
| 经批准的 Web/3D 交互 | 10–14 小时 |
| G4AI 提供商核验、适配器、隐私与降级 | 4–7 小时 |
| 评价、官方样本与性能 | 6–8 小时 |
| Sites 兼容与部署 | 4–6 小时 |
| README、提示词、隐私与许可证 | 5–7 小时 |
| PG-C 全站事实口径审计与回归 | 3–5 小时 |
| PG-B GitHub 动效 Logo 与品牌回查 | 2–4 小时 |
| PG-E 近真实样例、哨兵证据与人工 UAT | 4–8 小时 |
| 视频预制作、旁白/音乐、录制与后期 | 8–12 小时 |
| G7B YouTube 上传、字幕/封面、处理检查与香港访问复验 | 1–2.5 小时 |

总估算：82–123.5 个专注工程小时，其中 G1–PG-B 已完成。剩余 PG-E 用户 UAT/闭合与 G7B 的预算约 4–8.5 小时，另加 VG/G7A 视频制作 8–12 小时。若实际可用时间不足，优先保证近真实样例 UAT、两条确定性规则、证据、真实录屏、人工校对字幕和 YouTube 直链可达；不得删除 50 mm 净距规则、用 AI 替代确定性结果、把技术预检写成用户验收/完成事实，或绕过隐私、许可证、YouTube 频道身份和香港未登录访问检查。

---

## 10. 建议仓库结构（审批后创建）

```text
<PROJECT_ROOT>\
├─ README.md
├─ PROMPTS.md
├─ LICENSE
├─ THIRD-PARTY-NOTICES.md
├─ BIMCLASH_AGENT_MASTER_PLAN.md
├─ PROGRESS_SYNC.md
├─ package.json
├─ .gitignore
├─ .env.example
├─ .openai/
│  └─ hosting.json
├─ app/
│  ├─ core/
│  ├─ ifc/
│  ├─ clash/
│  ├─ clearance/
│  ├─ ai/
│  ├─ viewer/
│  └─ ui/
├─ reference/
│  └─ ifcopenshell/
├─ data/
│  ├─ generated/
│  ├─ external/
│  ├─ stress/
│  ├─ uat/
│  ├─ ground-truth/
│  └─ dataset-manifest.json
├─ schemas/
│  └─ clash-record.schema.json
├─ tests/
│  ├─ controlled/
│  ├─ contracts/
│  ├─ privacy/
│  └─ smoke/
├─ scripts/
├─ docs/
│  ├─ architecture.md
│  ├─ evaluation.md
│  ├─ data-and-licenses.md
│  ├─ privacy.md
│  ├─ design-brief.md
│  ├─ content-claim-ledger.md
│  ├─ engineering-sample-uat.md
│  ├─ demo-script.md
│  ├─ video-production-plan.md
│  └─ video-publication.md
├─ assets/
│  └─ brand/
│     ├─ logo-static.svg
│     ├─ logo-static.png
│     └─ logo-animated.<approved-format>
└─ outputs/
   ├─ local-only/
   └─ publication-candidate/
```

说明：该结构是候选方案，不为追求形式提前创建空目录。实际创建须以每个阶段真实需要为准。

`PROGRESS_SYNC.md` 在 G0A 创建，用于记录步骤 ID、本地提交主题与 SHA、GitHub 网页提交 URL/远程 SHA、上传时间、核验状态、公开性状态和异常；它不保存凭据、Cookie、个人账号信息或本机绝对路径。

---

## 11. Prompt 与 AI 使用记录

GitHub 要求包含提示词，因此必须建立可审计的 `PROMPTS.md`。

至少记录：

- 提示词编号；
- 日期；
- 目标；
- 使用的工具/模型（如允许公开）；
- 脱敏后的完整提示词；
- 人工约束；
- 生成结果是否被采用；
- 人工修改与验证；
- 失败或被拒绝的建议。

不得记录：

- API Key；
- 私人邮箱和账号；
- 本机用户路径；
- 浏览器登录信息；
- 保密文件内容；
- 未经许可的完整第三方材料。

运行时 AI 解读是必须交付的可选增强层，但不是判定核心。必须满足：

- AI 只解释确定性 Clash Records、Clearance Warning Records 和已公开规则边界；
- AI 不自行决定碰撞或净距预警是否成立；
- AI 失败不改变几何结果；
- 默认可在无 API Key、无网络或配额耗尽时完成两条规则的核心演示，并显示确定性模板说明；
- 真实调用由用户主动触发，调用前明确告知发送字段；
- 不向提供商发送 IFC、网格、文件名、绝对路径、账号信息或不必要元数据；
- API Key 只进入本地环境或部署密钥存储，不得进入客户端包、仓库、日志、截图或视频；
- 提示词、提供商核验日期、输入字段、数据边界、降级策略和人工验证公开说明；
- AI 文字必须与确定性状态分开显示，任何矛盾以确定性记录为准。

---

## 12. 风险登记

评分：可能性 `L` 1–5，影响 `I` 1–5，风险值 `R=L×I`。

| ID | 风险 | L | I | R | 触发信号 | 应对 |
|---|---|---:|---:|---:|---|---|
| R-01 | 浏览器 WASM 无法稳定解析 IFC | 3 | 5 | 15 | 8月26日晚仍无已知碰撞结果 | 切换本地 Python Web 核心 |
| R-02 | BVH 仅检测表面相交，漏掉包含关系 | 3 | 4 | 12 | C 类包含案例与参考不一致 | 明确限制或增加包含检查 |
| R-03 | touching/容差造成误报 | 4 | 5 | 20 | C03/C04 失败 | 冻结容差、收窄相交类型 |
| R-04 | IFC 开洞布尔结果导致误报 | 3 | 5 | 15 | C06 失败 | 只支持成功解析的最终几何；失败关闭 |
| R-05 | 官方模型坐标或 Schema 超出范围 | 3 | 3 | 9 | 联合模型错位/无法加载 | 不作准确率结论；记录兼容性限制 |
| R-06 | Sites 不兼容 WASM/构建输出 | 3 | 4 | 12 | 私有部署构建失败 | 保留本地 Web；在线展示受控结果 |
| R-07 | UI 方向未经确认造成返工 | 4 | 4 | 16 | 开始写样式但无审批 | Design Gate 强制阻断 |
| R-08 | GitHub 操作循环失败 | 3 | 4 | 12 | 同一网页动作第一次失败 | 记录后停止；不重复盲试 |
| R-09 | 许可证不清导致无法公开数据 | 3 | 5 | 15 | 无明确许可证或归属 | 不分发文件，仅保留来源链接/哈希 |
| R-10 | 公开材料泄露隐私 | 2 | 5 | 10 | 路径、姓名、邮箱、账号出现在候选物 | 逐文件和截图隐私审计 |
| R-11 | 功能膨胀导致错过截止时间 | 4 | 5 | 20 | 8月29日前讨论第三条无关规则/协作平台 | 坚持两条已批准规则和停止门 |
| R-12 | 以参考检测器输出循环定义真值 | 3 | 5 | 15 | 标准答案由产品输出反推 | 以生成台账为主，三方独立比较 |
| R-13 | 测试再生成静默覆盖已提交产物或遗漏顶层契约漂移 | 3 | 5 | 15 | 被篡改基线在测试后恢复且仍显示 PASS；单位/Schema/容差变化未失败 | G3A 隔离生成、路径—哈希基线、Git 前后断言和契约突变负向测试 |
| R-14 | 将世界轴 AABB 最小重叠推广为一般穿透深度或容差规则 | 4 | 5 | 20 | 薄构件或旋转案例被错误过滤；输出未经验证的距离 | G3B 将守卫限制为 C04 证据，验证一般几何语义；无法可靠判定则失败关闭 |
| R-15 | 50 mm 净距被 AABB/中心线近似误判或与硬碰撞重复 | 4 | 5 | 20 | 49/50/51 mm、旋转或开洞夹具失败；同一构件对双报 | G3C 使用可审计表面距离、阈值三点测试、硬碰撞优先去重和失败关闭 |
| R-16 | 免费 AI API 配额、条款或可用性变化，或密钥/模型数据泄露 | 4 | 5 | 20 | 官方免费额度取消、429/地区限制、客户端出现密钥、请求携带文件信息 | G4AI 当日核验官方条款、提供商适配器、最小化请求、服务端密钥、超时限流和确定性降级 |
| R-17 | 视频制作挤压代码冻结，或 AI 旁白/音乐存在事实、许可与隐私问题 | 3 | 5 | 15 | 8月30日仍无冻结脚本；成片超时；账号/路径入镜；素材许可不明 | VG 提前冻结脚本与风格，使用合成数据，准备备用镜头，逐项事实/隐私/许可/音画 QA |
| R-18 | 页面进度、README、视频脚本与权威 Gate 不一致，或任一后续阶段预写内容被误读为已完成 | 4 | 5 | 20 | 研发历程仍停在 DG；页面对 PG-B 至项目结束任一阶段出现无证据的完成状态、日期、指标、链接、验收、公开可用性、权限或证据 | PG-C 建主张台账与自动守卫；全部后续内容只标 `DRAFT/PLANNED` 或隐藏；预写不启动 Gate、不授予外部写入/部署/公开访问权限，也不绕过停止门；录制前逐项冻结事实 |
| R-19 | GitHub 不支持候选动效格式、Logo 过重/无限循环或缺少可访问回退 | 3 | 3 | 9 | README 不播放、模糊、布局跳动或加载显著变慢 | PG-B 先做实际渲染试验，短时一次性动效、静态终态、替代文本和体积上限 |
| R-20 | “近真实”样例含许可/隐私问题，超出支持边界或被误当成真实工程准确率证明 | 3 | 5 | 15 | 文件来源不清、真实单位/名称泄露、复杂模型产生无法解释结果 | 优先程序生成，建立哨兵真值与许可证清单；未知部分 `NOT_EVALUATED`；限制准确率主张 |
| R-21 | YouTube 频道身份泄露、不公开列出权限误解、处理/版权阻塞、字幕错误、链接失效或香港未登录不可达 | 3 | 5 | 15 | 频道姓名/头像不适合提交；视频要求登录；版权检查阻塞；1080p/字幕不可用 | G7B 行动时身份审阅、精确 `Unlisted`、人工双语字幕、处理/版权检查、SHA/视频 ID 登记与香港无痕复验；失败时阻止提交并由用户决定备用承载 |

高风险项（R≥15）必须在每日日志中更新状态。

---

## 13. 隐私、许可证与公开发布检查表

### 13.1 代码与仓库

- [ ] 无 API Key、Token、Cookie、密码；
- [ ] 无真实私人邮箱和电话号码；
- [ ] 无不必要的真实姓名、学校或单位信息；
- [ ] 无绝对本机路径；
- [ ] `.env`、缓存、浏览器资料和本地输出已忽略；
- [ ] Git 作者信息已由用户确认适合公开；
- [ ] 每个第三方依赖具有许可证登记；
- [ ] 每个数据文件具有来源、许可证、哈希和署名；
- [ ] CC BY 衍生内容已标明修改；
- [ ] 许可证不明确的数据未被重新分发。

### 13.2 页面与部署

- [ ] 上传的 IFC 默认不离开浏览器；
- [ ] 页面明确说明支持边界；
- [ ] 页面明确说明不是正式工程或法规认证；
- [ ] 错误日志不包含本地完整文件路径；
- [ ] 部署环境无测试密钥和内部 URL；
- [ ] AI 调用只发送经声明的最小结构化字段，用户可选择不发送；
- [ ] API Key 位于受控密钥存储，客户端 bundle、source map、日志和错误页均不含密钥；
- [ ] 免费额度、数据保留、地区和使用条款已按实施日期记录；
- [ ] 访问级别由用户明确确认；
- [ ] 公开 URL 使用公开测试数据。
- [ ] 首页、工作台、研发历程页、README、元数据与错误/空状态中的 Gate、日期、指标、链接均与主张台账一致；
- [ ] 从当前 Gate 之后直至项目结束的全部后续阶段内容仅显示为 `DRAFT/PLANNED`、“计划中/即将开展/尚未开始”或保持隐藏；没有完成式措辞，也没有伪造状态、日期、指标、链接、验收、公开可用性、权限或证据；预写未被当作阶段启动、Gate 通过或任何外部授权；
- [ ] 简中/英语、大众体验/工程极简、亮色/暗色与主流桌面宽度使用同一事实口径；
- [ ] 近真实人工试用样例来源、许可证、哈希、支持边界与非真实项目声明完整；
- [ ] GitHub 动效 Logo 具有静态终态、替代文本、体积记录并通过实际 README 渲染回查；
- [ ] 网站首页不内嵌最终视频，G7B 不因视频执行 Sites 重部署；视频以邮件和 README 中的 YouTube 直接链接交付；
- [ ] 用户批准网站公开后，香港地区未登录/无痕访问已覆盖网站和 GitHub 入口；YouTube 视频按 G7B 独立复验。

### 13.3 截图与视频

- [ ] 仅显示公开或合成 IFC；
- [ ] 关闭通知和无关窗口；
- [ ] 不显示 Chrome 个人资料、书签和账号菜单；
- [ ] 不显示 GitHub 私人仓库或个人仪表板；
- [ ] 不显示本地用户名和绝对路径；
- [ ] 不包含用户本人出镜或原声；
- [ ] AI 旁白逐句与批准脚本、页面事实和数字核对；
- [ ] 音乐/音效/生成素材来源、许可和修改记录完整；
- [ ] 字幕与旁白一致，专有名词和数值读法正确；
- [ ] 至少一次真实计算连续镜头，不以生成画面伪装产品运行；
- [ ] 视频长度 ≤ 180 秒；
- [ ] YouTube 频道显示名/头像、标题、说明、封面、评论和可见性已经用户确认，不泄露不希望公开的身份；
- [ ] YouTube 可见性精确为 `Unlisted`，已明确其“持链接可转发、并非严格私密”的边界；
- [ ] 简体中文和英文字幕均已人工校对，数字、单位、状态名和时间轴正确；
- [ ] YouTube 处理与版权/政策检查无阻塞，最终 MP4 SHA-256、视频 ID、watch URL 与上传时间已登记；
- [ ] 视频直接链接在香港 Chrome 无痕、未登录及窄屏视角通过播放、拖动、字幕和说明链接验证。

任何一项未通过，公开状态必须保持 `BLOCKED`。

---

## 14. GitHub 网页交付流程

该流程从 G0A 开始，在每个步骤完成后执行一次，而不是延迟到 G6 或截止日集中执行：

1. 完成步骤验收、日志更新、隐私/许可证检查和本地 Git 提交；
2. 记录本地提交主题、完整或短 SHA 以及干净工作树状态；
3. 用户确认 Google Chrome 中 GitHub 已稳定登录；
4. G0A 首次执行时，经用户确认仓库名称、描述与可见性后，在 GitHub 网页创建或打开目标仓库；后续步骤只打开同一既定仓库；
5. 通过 GitHub 网页上传该步骤相对上个检查点新增或修改的已验证、已脱敏文件；本地总纲若仍含绝对路径或其他仅限本地的治理信息，上传其脱敏公开版或将其列入不上传清单；不得借此上传本地缓存、密钥、私有输出或无关文件；
6. 网页提交信息写明步骤 ID 和本地短 SHA；
7. 上传后核对文件数量、关键文件、提交记录、Markdown 渲染和访问权限；
8. 从 README 已产生的阶段开始，同时核对运行说明、链接和图片；从相关文件已产生的阶段开始，核对 `PROMPTS.md`、LICENSE、`THIRD-PARTY-NOTICES.md` 和数据署名；
9. 在 `PROGRESS_SYNC.md` 或进展日志登记本地 SHA、GitHub 提交 URL/远程 SHA、上传时间、核验结论和任何差异；
10. G6/G7 公开候选阶段，再使用未登录或无痕视角验证公开可见性；在此之前，不因“阶段上传”自动把仓库改为公开；
11. 出现失败时停止重复尝试，记录页面错误与当前状态，由用户决定恢复方式；
12. 同步为 `PASS` 后，才把该步骤标记为完整完成并进入下一步骤。

由于 GitHub 网页上传会产生独立于本地 Git 的远程提交，本项目不宣称两边 SHA 相同；可追溯性以“步骤 ID + 本地 SHA + GitHub 提交 URL/远程 SHA”的映射为准。若网页界面不支持安全、准确地维护当前文件规模，应在当次失败后暂停并向用户报告，不得自行改用命令行远程方式。

不得将浏览器登录凭据复制到项目文件、命令行或聊天中。

---

## 15. 演示视频 AI 制作流程（≤180 秒）

### 15.1 已冻结方向

- 不要求用户本人出镜，不录制或使用用户原声；
- 由 Codex 协助完成脚本、分镜、画面准备、录制编排、AI 旁白、字幕、音乐选择、剪辑清单和质量核验；
- 产品运行画面以真实、连续、干净的屏幕录制为主，生成画面只能用于短标题、示意或转场，不得伪装为真实操作；
- 已冻结为英语女声旁白，并交付人工校对的英语/简体中文字幕；不使用用户原声；
- 默认候选风格为克制、清晰、工程审查感，画面优先展示证据而非装饰。

### 15.2 建议成片结构与时长预算

| 时间 | 内容 | 必须画面/信息 |
|---|---|---|
| 0–15 秒 | 问题与价值 | 一句话说明 MEP—结构硬碰撞与净距审查的人工痛点 |
| 15–35 秒 | 范围与可信方法 | 两条确定性规则、受控真值、浏览器本地处理边界 |
| 35–100 秒 | 真实操作主镜头 | 选择合成模型、运行、查看 Clash 与 `<50 mm` Warning、3D 定位 |
| 100–125 秒 | 确定性证据 | GUID、类型、容差/净距、失败关闭、参考一致性 |
| 125–145 秒 | 可选 AI 解读 | 用户主动触发；展示自然语言摘要并强调不改变检测结论；若 live API 不稳定则使用已录制的真实成功调用并明确标识 |
| 145–165 秒 | 评价与性能 | 只展示已由 G5/G6 冻结的指标与兼容性结论 |
| 165–175 秒 | 限制与收束 | 非正式工程认证、支持边界、仓库/演示入口 |
| 175–180 秒 | 安全余量 | 片尾或静音缓冲；最终目标片长 `165–175 秒` |

### 15.3 展示基线与预制作（PG → VG）

1. PG-C 先冻结页面主张台账；从 G5/G6/PG 证据生成“可说事实 / 不可说主张 / 未来计划”清单；
2. PG-E 选定近真实工程样例或受控样例、预期输出、关键镜头与备用样例，并完成用户实际试用；
3. 编写 `272` 个英语单词的 13 句旁白稿，并为每句提供简体中文待人工复核字幕、证据来源、预估时长和对应镜头；每句必须通过 `114 wpm + 420 ms` 句末呼吸余量守卫；
4. 制作 shot list：窗口尺寸、鼠标路径、模型状态、需放大的字段、转场点、主镜头与备用镜头；
5. 准备统一章节系统的 3 个风格帧、13 张 1080p 关键帧与一张 4K 总表、女声样例和 M2 低音量混音目标；
6. 确定字体、字幕安全区、色彩、光标高亮、缩放方式、旁白音色、语速、停顿、音乐和响度目标；
7. 完成用户对脚本、风格、旁白和音乐决定的 VG 确认。

### 15.4 画面与音频制作（8月31日 09:00–12:00）

1. 冻结演示 build、浏览器缩放、窗口尺寸、合成数据和预期输出，先完整彩排两遍；
2. 关闭通知、书签栏、个人资料入口和无关窗口；清理文件名、路径、历史记录与账号信息；
3. 分段录制：开场、真实运行、Clash 定位、50 mm Warning、AI 解读、证据/指标和限制；每段至少保留一个备用 take；
4. 旁白只使用已批准脚本；合成后逐句核对数字、GUID 读法、`IFC`、`MEP`、`50 mm`、`2 mm` 等专有词；
5. 音乐优先弱存在感、无歌词、许可明确；若授权或混音质量不确定，采用无音乐版本；
6. 保存源素材、许可记录和中间产物于项目内受控目录，未经审计不进入公开仓库。

### 15.5 后期与发布 QA（8月31日 12:00–17:00）

1. 粗剪先保证叙事与真实操作完整，再做局部放大、字幕、光标强调和轻量转场；
2. 旁白始终优先于音乐，避免削波、噪声、突兀停顿和过快语速；
3. 字幕逐字复核，所有数字、单位、状态名和免责声明与页面一致；
4. 逐帧检查用户身份、账号、通知、本地路径、浏览器资料、私有 URL、API Key 与无关窗口；
5. 逐条检查事实、指标、许可证、素材来源、AI 输出标识和“至少一次真实计算”证据；
6. 导出后从头到尾观看至少两遍：一次检查内容/节奏，一次检查音画/字幕/隐私；
7. 验证时长 `≤180 秒`、常见分辨率下文字可读、音量一致、链接可从无痕窗口访问；
8. 只有全部 QA 通过后才冻结 G7B 上传候选，登记本地版本与 SHA-256；YouTube 上传、链接与权限登记只在 G7B 行动时授权后执行。

视频必须展示一次真实计算，不使用纯动画、预设结果或生成画面冒充运行。AI API 在录制时若出现偶发限流，不得通过剪辑制造不存在的稳定性声明。

### 15.6 YouTube 上传与提交直链复验（G7B）

1. G7A 成片与用户确认均通过后，冻结最终 MP4、简体中文/英文字幕、封面与说明文案，记录 SHA-256、编码、时长、分辨率和版本；
2. 在任何上传前展示 YouTube 频道显示名/头像、标题、说明、封面、评论、语言和 `Unlisted` 可见性，取得用户对该精确外部动作的确认；
3. 通过用户可见的 YouTube Studio 上传最终 MP4，原始语言匹配旁白，精确选择 `Unlisted`；不把完整 MP4 放入普通 GitHub 仓库；
4. 上传并逐行复核两种字幕，等待必要处理与平台检查完成；核对时长、目标清晰度、音画同步、封面和版权/政策状态；
5. 记录最终视频 ID、watch URL、上传时间与本地 SHA-256；从香港 Chrome 无痕/未登录及窄屏视角验证直接播放、拖动、双语字幕、说明和链接；
6. 验证通过后，才把直接 watch URL 写入 README、视频制作记录和邮件提交清单，并形成 G7B GitHub 检查点；网站首页不增加播放器，也不因视频重部署；
7. 更新研发历程时只把实际通过的阶段写成已完成；其余预写文案继续标为 `DRAFT/PLANNED` 或隐藏；
8. 任一身份、权限、处理、版权、字幕或香港访问检查失败时停止发布链，不发送邮件；备用承载只能在用户另行决定后启用。

---

## 16. 责任分工与授权边界

### 助手可在总纲批准后自主执行

- 在项目目录内创建和编辑代码、测试与文档；
- 使用 PowerShell 7 执行本地构建、测试和只读诊断；
- 初始化和维护项目的本地 Git 仓库，执行状态检查、暂存、非破坏性本地提交与 SHA 登记；
- 在每个步骤验收后准备脱敏、可上传的阶段快照和同步清单；
- 生成受控合成 IFC；
- 对许可证明确的公开资料进行研究并记录来源；
- 建立本地评价、隐私检查和发布候选；
- 在已批准的 UI/风格范围内实现前端；
- 在 G4AI 数据边界内研究免费额度 API、实现适配器并以合成记录测试；
- 在 VG 通过后按批准脚本生成旁白、录制公开合成数据画面并制作视频候选。

### 必须由用户确认或授权

- 总纲正式批准；
- UI、前端和设计风格；
- 视频脚本、视觉风格、旁白样例和音乐方案的 VG 确认；
- 核心范围变更；
- 使用任何可能涉及个人或非公开数据的文件；
- GitHub 仓库首次创建、仓库名称和可见性，以及任何删除、Release、Tag 或其他重要外部状态变更；
- 常规步骤完成后的 GitHub 网页上传已由用户作出持续指示，但仍只能利用用户已登录的 Chrome 会话执行，并须让用户能够看到和控制页面操作；
- Sites 或其他平台的公开部署/访问级别变化；
- 公开发布、邮件发送和简历附件；
- 删除重要数据或执行不可恢复操作。

---

## 17. 状态词与记录规范

统一使用以下状态：

- `NOT_STARTED`：尚未开始；
- `IN_PROGRESS`：正在执行；
- `PASS`：验收通过；
- `FAIL`：明确失败；
- `BLOCKED`：存在外部阻塞；
- `NOT_EVALUATED`：无法可靠判断；
- `DEFERRED`：明确延期，不属于当前截止范围；
- `NOT_PERMITTED`：当前约束禁止；
- `DRAFT_FOR_USER_REVIEW`：等待用户审阅；
- `APPROVED`：用户已批准。
- `TECH_PASS_SYNC_BLOCKED`：技术验收已通过，但该步骤的 GitHub 阶段同步尚未通过，步骤不得视为完整完成。

日志必须区分：

- 已验证事实；
- 当前推断；
- 待验证假设；
- 用户决定；
- 外部阻塞。

不得把计划描述为已完成，不得把单个样本结果描述为通用能力。

---

## 18. 决策记录

| 决策 ID | 日期 | 决策 | 状态 | 理由与影响 |
|---|---|---|---|---|
| D-001 | 2026-08-25 | 项目使用 Web 微原型路线 | APPROVED_BY_USER_DISCUSSION | 与旧桌面项目区分，强调浏览器交互与部署 |
| D-002 | 2026-08-25 | 核心方向选择几何硬碰撞 | APPROVED_BY_USER_DISCUSSION | 技术含量、可视化和工程判断信号更强 |
| D-003 | 2026-08-25 | 核心规则限定 MEP 管线对结构梁/墙 | APPROVED | 随 0.2.0-draft 总纲获用户明确批准；避免梁墙正常连接误报，保持规则精确 |
| D-004 | 2026-08-25 | 使用三层案例库 | APPROVED_BY_USER_DISCUSSION | 合成真值、官方兼容性、异常失败关闭相互补充 |
| D-005 | 2026-08-25 | 项目根目录固定为 `<PROJECT_ROOT>` | USER_MANDATED | 隔离项目活动 |
| D-006 | 2026-08-25 | GitHub 远程操作只通过用户 Chrome 网页 | USER_MANDATED | 避免本机 GitHub 工具失败循环 |
| D-007 | 2026-08-25 | PowerShell 只允许 7.x | USER_MANDATED | 避免 PowerShell 5 兼容与执行错误 |
| D-008 | 2026-08-25 | UI/前端/风格必须先获用户批准 | USER_MANDATED | 防止未经确认的视觉返工和 Token 浪费 |
| D-009 | 2026-08-25 | 公开材料必须进行个人信息脱敏 | USER_MANDATED | 保护用户隐私 |
| D-010 | 2026-08-25 | 项目正式开始后全量使用本地 Git 管理进度与变更 | USER_MANDATED | 建立可恢复、可审计的本地版本历史 |
| D-011 | 2026-08-25 | 每个已登记步骤验收后均通过 Chrome GitHub 网页上传更新一次 | USER_MANDATED | 展示阶段性进度变化，避免截止日一次性提交 |
| D-012 | 2026-08-25 | 公开项目名采用 `IFC ClashTrace`，GitHub 仓库名采用 `ifc-clashtrace` | USER_MANDATED | 避免 `Agent` 对 LLM/自主能力的误导；保留 IFC、Clash 与证据可追溯性的核心表达 |
| D-013 | 2026-08-25 | 首个 GitHub 仓库可见性采用 `Public` | USER_MANDATED | 用户明确要求全量公开；仍只上传已脱敏公开候选，不改变隐私停止门 |
| D-014 | 2026-08-26 | G1 后续浏览器核心暂定 `web-ifc 0.0.77 + three-mesh-bvh 0.9.14`，IfcOpenShell 0.8.5 作为参考路线 | APPROVED_BY_EXISTING_PLAN | Node 与实际 Chrome 均稳定返回已知 GUID 碰撞对，满足 O-002 的 Spike 决策要求；G3 仍须通过完整规则契约 |
| D-015 | 2026-08-26 | 浏览器 Clash Record 的穿透距离在可靠算法验证前保持 `null` | APPROVED_BY_EXISTING_PLAN | G1 只验证三角表面相交；不把未验证的近似距离冒充精确证据，解决 O-003 |
| D-016 | 2026-08-26 | 未经用户明确决定，不给项目代码或生成数据擅自授予公开许可证 | GOVERNANCE_ENFORCED | G1 公开检查点排除无许可证生成 IFC；生成器和证据可公开，项目级许可证最迟在 G2 决定 |
| D-017 | 2026-08-26 | G1 GitHub 检查点采用 Chrome 直接控制、根文件批量上传及真实文件逐层建立目录 | USER_APPROVED | 用户明确建议在线建目录后上传并要求减少 Computer Use；全程未配置 Git 远程，生成 IFC 继续排除 |
| D-018 | 2026-08-26 | 项目代码采用 MIT；本项目程序生成的 IFC 与配套真值数据采用 CC0-1.0 | USER_MANDATED | 用户明确批准推荐方案；MIT 版权主体使用非个人化的 `IFC ClashTrace contributors`，不登记用户真实身份；第三方材料继续遵循各自许可证 |
| D-019 | 2026-08-26 | 将契约测试加固登记为 G3A 强制前置子 Gate | USER_MANDATED | 用户要求把全量审计发现的测试覆盖缺口纳入计划；G3A 必须证明冻结基线不可被静默覆盖且关键契约漂移会失败 |
| D-020 | 2026-08-26 | 将容差语义与 AABB 守卫隔离登记为 G3B 强制前置子 Gate | USER_MANDATED | 用户要求把 AABB 通用化风险纳入计划；C04 守卫只作为受控证据，G3 必须验证一般语义或失败关闭 |
| D-021 | 2026-08-26 | 将管线与结构构件小于 50 mm 的净距预警由候选项升级为必做规则，并登记 G3C | USER_MANDATED | 项目进度超前，用户明确要求拓展功能；G2 冻结真值不改写，另建净距补充夹具与停止门 |
| D-022 | 2026-08-26 | 将免费额度 API 驱动的可选 AI 结果解读登记为必做增强步骤 G4AI | USER_MANDATED | 改善确定性机器结果的自然语言可读性；AI 只解释、不判定，用户可关闭，无 API 时核心闭环保持可用 |
| D-023 | 2026-08-26 | 视频采用无用户出镜/原声的 Codex 辅助制作路线，并新增 VG 与完整预制—录制—后期—QA 流程 | USER_MANDATED | 降低个人信息披露，确保画面流畅干净，并把脚本、风格、旁白、音乐、字幕和发布核验纳入排期 |
| D-024 | 2026-08-27 | 产品展示结构增加公开主页、独立功能页与研发历程页；主页两个入口均在新标签页打开，向下为图文介绍，底部为贡献者/GitHub/许可证/隐私等链接 | USER_MANDATED | 用户明确要求先建立可理解的项目入口，再将实际工具与研发证据分离；不得把主页发展成无关营销站 |
| D-025 | 2026-08-27 | 全站必须支持大众版/黑白灰极简版、简中/英语、亮色/暗色切换；功能页提供默认关闭的 AI 解读开关 | USER_MANDATED | 用户希望兼顾个人极简审美与大众接受度，同时让语言、外观和 AI 使用权由访问者控制；偏好不得改变确定性记录 |
| D-026 | 2026-08-27 | 大众版选择 C 编辑科技并修订为深重大地色、保留克制赭黄色强调、统一现代无衬线字体与受控圆角；主页功能栏下居中展示 Logo/名称；Logo 以墙体—管线—碰撞—提示气泡形成静态图标并允许一次性有限揭示动效 | USER_MANDATED | 用户以既有合作动画作为节奏参考但要求为 IFC ClashTrace 形成原创标志；参考不得复制，最终概念仍须用户批准且减少动态偏好直接显示静态终态 |
| D-027 | 2026-08-27 | Logo 图标与 `IFC ClashTrace` 字标之间保留约一条管线外径的安全间距；原黄色/赭黄碰撞环与提示气泡改为克制大地感橘红色；其余图形、字体与五阶段动效不变 | USER_MANDATED | 用户对 R3 概念基本满意并只要求两项定向修订；不得借此扩大品牌或正式 UI 范围 |
| D-028 | 2026-08-27 | 批准完整 DG R4，并授权仅通过已登录 Chrome 网页上传 8 个已审计文件及其后仅限公开总纲/进度台账的证据映射尾部 | USER_APPROVED | 授权范围明确排除本地权威总纲、所有本地审计脚本、参考/生成媒体、凭据、浏览器状态、缓存与任何 G4 资产；完成 DG 后必须停止 |
| D-029 | 2026-08-27 | 批准严格限于三项的 DG-R1 审计修复：消除 DG/G4 授权歧义、强制精确 8 文件发布集合、扩大正式 UI 缺失守卫 | USER_APPROVED | 只授权本地修复与验证，不推定 Chrome 外部写入授权；不得进入 G4 或扩展设计/功能范围 |
| D-030 | 2026-08-27 | 集体授权仅通过已登录 Chrome 上传 DG-R1 的 5 个公开修复文件，并在核验后仅以公开总纲/进度台账闭合映射尾部 | USER_APPROVED | 5 文件为公开总纲、进度台账、PROMPTS、设计简报和公开 DG 测试；本地总纲、audit-dg 及全部既有排除项仍禁止上传 |
| D-031 | 2026-08-27 | 用户明确授权开始基础 G4，并要求完成后严格停止在 G4 | USER_MANDATED | 授权仅覆盖 DG R4/R1 已冻结的三页 Web 纵向切片、正式 UI、双风格/双语/亮暗/AI 默认关闭偏好、真实确定性闭环与本地验收；不授权 G4AI 提供商/API、部署、公开访问变更或视频；任何 Chrome 外部上传仍须行动时一次集体授权 |
| D-032 | 2026-08-27 | 在任何 G4 外部上传前继续主页人工审阅，并先闭合首轮 12 项视觉与文案反馈 | USER_MANDATED | 修复自定义下拉的圆/方角契约与风格命名、语义断行、介绍字号和扩写、正向适用范围叙述、较明亮的暗色调、按钮对比度、介绍区悬停动效、页尾区分及 DG 品牌 Logo 动效落点；原 18 文件上传请求未获授权且不得执行 |
| D-033 | 2026-08-27 | 继续闭合主页第二轮视觉与语言反馈，并将两种风格命名冻结为“大众体验/工程极简” | USER_MANDATED | 暗色采用中性近黑灰分层，青色仅作为品牌/模型管线识别色；产品保证改为高对比编号卡片，页眉/页脚取消常驻下划线，三段标题统一两行，冲突记录和辅助文本完整双语，感叹号几何居中，所有页面页脚统一深黑灰；继续等待用户审阅，不执行外部上传 |
| D-034 | 2026-08-28 | 重构主页下滑信息架构，并把 AI 开关限定到功能工作台 | USER_MANDATED | 删除主页中部导航和 AI 控制；融合三段介绍为同色模块，悬停/聚焦变色并展示四张真实本地页面预览；英语采用简洁达意表达，先修复工作台排版；AI 状态内置于放大轨道；继续等待用户审阅且不执行上传、部署、G4AI 提供商/API 或视频 |
| D-035 | 2026-08-28 | 一次闭合主页与功能工作台的当前全部注释，并把真实 IFC 输入、演示入口与 AI 控件重新分级 | USER_MANDATED | 品牌图增强立体层次；四张预览全部刷新并在小气泡内裁去滚动条；移除含义不清的主页底部横幅；工作台以双 IFC 为主、受控示例为次；AI 唯一控件移入输入工作流且明确标识；1024/1280/1433 px 中英文排版均须通过；继续禁止上传、部署、G4AI 提供商/API、移动端计算扩展或视频 |
| D-036 | 2026-08-28 | 集体授权仅通过已登录 Chrome 上传 G4-R1 的 6 个公开修复/证据文件，并以公开总纲与进度台账闭合映射尾部 | USER_APPROVED | 精确文件为 `app/ui/app.mjs`、`scripts/test-g4.ps1`、`README.md`、`docs/g4-vertical-slice.md`、公开总纲与进度台账；本地总纲、全部 `audit-*.ps1`、部署、G4AI 与视频继续排除 |
| D-037 | 2026-08-28 | 用户在审阅三个候选后明确选择 GroqCloud Free Plan 的 `openai/gpt-oss-20b`，并批准同源 Node 代理、服务端环境密钥、最小字段别名化、严格 JSON Schema 与保守额度预留策略 | USER_APPROVED | 官方免费层当前列出 `30 RPM / 1,000 RPD / 8K TPM / 200K TPD`；默认推理不留存但可靠性/滥用日志可能最多 30 天且可启用 ZDR。自动测试零 live、开发日最多 5 次、审查/演示前 24 小时停止非必要调用并至少预留 40K 日 token；额度、地区、模型与条款均为 2026-08-28 时点边界，不作永久承诺 |
| D-038 | 2026-08-28 | 用户逐次批准四次受控 G4AI live smoke，并要求界面选英文即输出英文、选中文即输出中文 | USER_APPROVED | 每次只发送同一生成 review-pack 的最小派生字段；前三次分别因自由文本语义、动态数组 schema 与引用集合可靠性失败关闭，第四次 exact-key/enum-only schema 成功。用户可见文案改由受信任本地双语模板生成，语言切换撤销旧预览/结果；第四次后停止 live 调用 |
| D-039 | 2026-08-29 | 根据首次人工试用反馈启动 G4AI-R1：提升入口可发现性，并把过度克制的 enum 碎片文案适度放宽为受约束的成段协调分析 | USER_MANDATED | 入口移至确定性结果摘要后并显示开启—预览—确认路径；响应仍使用 exact-key 闭合对象，只允许跨记录综合、逐记录证据解读和下一步复核重点；禁止状态/规则/证据改写、数值新断言、原因/位置/方案臆测，错误语言或覆盖不全继续失败关闭。该授权不含新 live、GitHub 上传、部署、G5 或视频 |
| D-040 | 2026-08-29 | 根据第二轮试用批注收敛 G4AI 入口：删除运行前重复开关和大卡，前移能力、供应商与条款提示；运行后只保留唯一轻量开关和短按钮 | USER_MANDATED | 运行前以一句话说明可选 AI、GroqCloud/模型、字段预览/二次确认及 2026-08-28 条款时点边界；结果摘要后使用“可选 AI 解读 + 开关 + AI 解读/Interpret”紧凑行，不再显示重复大标题、三步路径或长 CTA。发送前预览和新同意仍是强制步骤；不含第五次 live、外部写入、部署、G5 或视频 |
| D-041 | 2026-08-29 | 根据第三轮试用批注把 AI 说明改为面向大众的服务、发送范围和隐私文案，并停止向用户暴露内部错误码 | USER_MANDATED | 保留 GroqCloud/模型、字段预览、新同意、最小字段和失败关闭；公开界面删除“当前账户”、ZDR、API 配置状态、开发时点说明及 `{code}`，改为双语服务可用性与本地解读提示。内部错误分类仍保留用于适配、重试判定和测试；不配置密钥、不发起第五次 live、不外部写入 |
| D-042 | 2026-08-29 | 用户批准 G4AI-R2 仅修复重试同意状态机、异步并发状态和容量边界，并单独授权恰好一次当前 R1 成段分析合同 live smoke | USER_APPROVED | 重试必须回到字段预览并取得新同意；旧请求不得覆盖或清除新语言/预览/结果；单次最多六条记录。live 只使用生成 review-pack 最小派生字段，不自动重试，不授权上传、部署、公开访问变更、移动端计算、视频或 G5 |
| D-043 | 2026-08-29 | 初次 R2 live 连接类失败关闭后，用户明确批准恰好一次当前合同重试 | USER_APPROVED | 重试仍只发送生成 review-pack 的四条最小字段，重新预览并取得新同意；启动器在本地 Node 路径解析失败的一次预备启动未产生提供商请求。修正后只发送一次，随后无论结果均停止服务；不授权第三次调用、上传、部署或扩展阶段 |
| D-044 | 2026-08-29 | 两次当前成段合同 live 均失败关闭后，用户要求修复错误 | USER_APPROVED | 仅在本地修复可复现的提供商合同风险：严格 schema 使用 Groq 当前文档明确列出的结构子集，本地继续执行长度与语义边界；completion cap 调整为 `1,600`，temperature 固定为零，并区分输出截断、拒绝、提供商拒绝、畸形响应与本地语义拒绝。该授权不包含新的 live 调用、密钥使用、GitHub 上传、部署、公开访问变更、移动端计算、视频或 G5 |
| D-045 | 2026-08-29 | 用户明确批准修复后冒烟测试“直到功能成功实现” | USER_APPROVED | 连续 live 只使用生成 review-pack 最小派生字段，每次均重新预览并取得新同意；允许依据安全错误类别和人工内容审计作有限本地修复并重试，成功后立即停止。该授权不包含 IFC/GUID/名称/路径发送、原始响应持久化、GitHub 上传、部署、公开访问变更、移动端计算、视频或 G5 |
| D-046 | 2026-08-29 | G5 关闭 O-005，正式支持仍限 IFC4；IFC4X3 保持探索状态 | AGENT_DECIDED_FROM_EVIDENCE | 8 个公开受控案例和 9 个净距夹具只能证明冻结 IFC4 契约；buildingSMART PCERT 样例虽可由 web-ifc/IfcOpenShell 打开，但使用毫米单位且 HVAC 文件没有 `IfcPipeSegment`，产品正确失败关闭。不得以兼容性样例扩展 Schema、单位、类型或任意 IFC 支持承诺 |
| D-047 | 2026-08-29 | 批准严格限定的 G5-R1 一致性修复 | USER_APPROVED | 只纠正 G5 完整 SHA、过期公开状态、官方样本几何/GUID 完整性谓词与测试、G5 审计和映射台账；不改检测算法、冻结结果、AI、依赖或产品范围。该本地修复授权不替代 Chrome 公共提交的行动时确认 |
| D-048 | 2026-08-29 | G6 采用现有三页 Vite 多入口与最小 Worker-compatible ESM，第一版只允许所有者私有预览且不配置托管 AI 密钥 | AGENT_DECIDED_FROM_EVIDENCE | 保持 G3/G4 权威与浏览器本地 IFC 计算；只部署 16 个 CC0 合成 IFC、MPL-2.0 web-ifc WASM 和许可资料。因非必需 Cloudflare 本地工具包引入上游未修复开发期漏洞而删除，保留零漏洞的 Vite + Sites 元数据插件；公开访问仍由 O-006 和用户人工验收决定 |
| D-049 | 2026-08-29 | 批准完成 G6 中除 GitHub 上传外的操作 | USER_APPROVED | 授权创建唯一 Sites 项目、写入项目 ID、重测/审计、本地提交、使用短期凭据推送精确源、保存版本、所有者私有部署和回查；明确不授权 GitHub 上传、托管 AI 密钥、Groq live、公开访问、VG、G7、移动端计算或视频 |
| D-050 | 2026-08-29 | G6 静态站点产物必须位于 Sites 的 `dist/client` 资产绑定边界 | AGENT_DECIDED_FROM_EVIDENCE | 用户截图与生产 Worker 日志共同证明版本 1 的已认证根请求返回 404；版本内容存在但 `ASSETS` 未绑定。把三页静态产物、生成 IFC、WASM 与许可文件移入 `dist/client`，保留 `dist/server/index.js`，不改 G3/G4 权威、AI 契约、访问策略或公开边界 |
| D-051 | 2026-08-29 | 开发页的验证入口改为部署内受控验证，不再依赖尚未同步的 GitHub 文档 | USER_MANDATED | 用户人工试用指出“验证方法”跳转内容为空；入口改为新标签打开 `/app/#controlled-review`，直接定位 C01/C03/C05/C08 冻结示例。只改善公开证据可用性，不更改确定性结果、规则、AI 权限、访问策略或 GitHub 停止门 |
| D-052 | 2026-08-29 | G6 所有者私有版本 4 的基本使用体验人工验收通过 | USER_APPROVED | 用户明确反馈“基本使用体验我觉得没问题”，满足 G6 私有预览人工验收门；该反馈只授权整理待上传 GitHub 内容，不构成 GitHub 写入、Sites 公开、托管密钥、VG/G7、移动计算或视频授权 |
| D-053 | 2026-08-29 | 在 G6 与 VG 之间新增强制 `PG` 展示就绪 Gate，统一完成全站事实口径审计、GitHub 动效 Logo 与近真实工程样例人工 UAT | USER_MANDATED | 用户发现研发历程仍停在 DG，并要求在视频前修复所有页面进度、完善品牌展示并以接近工程情境的文件人工试用；PG 未通过不得冻结脚本或录制 |
| D-054 | 2026-08-29；2026-08-30 扩展 | 允许提前编写从当前 Gate 之后直至项目结束的所有后续阶段文案与组件，但只能保持隐藏或明确标为 `DRAFT/PLANNED`、计划中或尚未开始，不得在证据形成前表述为已完成 | USER_MANDATED_WITH_GOVERNANCE | 预写范围由仅 G7 扩展至项目结束前全部后续阶段；不得伪造完成状态、日期、指标、链接、验收、公开可用性、权限或证据。预写不启动阶段、不通过 Gate、不替代用户验收，不授权外部写入、部署或公开访问，也不得绕过任何停止门 |
| D-055 | 2026-08-29 | GitHub README 动效 Logo 延续 DG 已批准品牌，不重新设计品牌；最终格式以 GitHub 实际渲染试验决定，并强制静态终态、替代文本和体积预算 | USER_MANDATED | 用户要求 GitHub 首页增加动效 Logo；现有 Logo 概念与有限揭示动效已经批准，因此本步骤是媒介适配与质量验收而非重开视觉方向 |
| D-056 | 2026-08-29 | 把视频成片制作与首页回嵌拆为 G7A/G7B；只有成片 QA 通过后才能回嵌，并必须完成香港未登录访问复验 | USER_MANDATED | 用户计划把最终视频放到首页；拆分可避免未定稿视频污染稳定站点，并把播放回退、性能、字幕、权限与地区可达性设为独立停止门 |
| D-057 | 2026-08-29 | GitHub 网页技术链按真实目录能力由 7 组拆为 9 笔，但 27 文件技术范围不变 | AGENT_DECIDED_FROM_EVIDENCE | 用户授权 0.4.0 重审后的精确 29 文件方案；首笔回查通过后，GitHub 文件选择器确认 `multiple=true`、`webkitdirectory=false`，跨 `app` 子目录混选会丢失相对路径。因此把 `app` 六文件安全拆为根入口、AI 客户端和预览图三笔；无新增文件、内容或权限，逐笔路径/父链核验继续强制 |
| D-058 | 2026-08-30 | 批准一次严格限定的 G6-R1 公开证据一致性修复 | USER_APPROVED | 只修正 README/隐私报告中过期的私有预览待验收措辞、最终台账时态与 Markdown 标题，并给本地 G6 审计增加对应语义和空同步守卫；允许同步更新权威/公开总纲与 `PROMPTS.md`。不改产品代码、确定性规则、AI 契约、依赖、样例、媒体、部署或访问；本地批准不替代精确 GitHub Chrome 写入的行动时授权 |
| D-059 | 2026-08-30 | 批准 G6-R1 精确 5 路径、4 笔 Chrome 公开映射 | USER_APPROVED | 用户在看到唯一公开路径与分组后明确“批准提交”。公开范围仅为 `docs/g6-privacy-license-audit.md`、`README.md`、`PROMPTS.md`、`BIMCLASH_AGENT_MASTER_PLAN.public.md` 与 `PROGRESS_SYNC.md`；本地权威总纲和 `scripts/audit-g6.ps1` 继续排除。授权不包含 Sites 公开、权限、托管密钥、PG/VG/G7 或任何其他外部写入 |
| D-060 | 2026-08-30 | 立即启动并完整执行 PG-C，完成后严格停在 PG-C | USER_MANDATED | 授权只覆盖全站内容/进度主张台账、三页与 README/元数据/链接/错误空状态/历史截图校准、双语/双风格/亮暗/1024–1433 px Chrome 验收、自动守卫、审计与本地提交；不得启动 PG-B、PG-E、VG 或 G7，不自动授权 GitHub、Sites 部署、访问变化、托管密钥或其他外部写入 |
| D-061 | 2026-08-30 | PG-C 视口复验只使用 Chrome 插件能力，不再使用 Windows 控制 | USER_MANDATED | Windows 控制因无法可靠确认浏览器 URL 而在输入前安全停止；用户要求改用 Chrome 插件重试。Chrome 原生 viewport capability 完成 1024/1280/1433 px × 三页 × 双语/双风格/亮暗共 72 状态验收，不绕过浏览器安全边界 |
| D-062 | 2026-08-30 | 批准 PG-C 精确外部闭环，并要求完成后展示需查看内容 | USER_MANDATED | 授权仅覆盖 15 个已审计公开路径的 Chrome GitHub 上传、所有者私有 Sites 候选保存/部署，以及外部证据成立后 6 个状态闭合路径的 Chrome 尾部上传和最终所有者私有部署；不得公开 Sites、改变访问/权限/密钥、配置 Git 远程、使用 GitHub CLI/API、发布本地总纲/审计脚本或进入 PG-B/PG-E/VG/G7 |
| D-063 | 2026-08-30 | 每个新浏览器标签页首次打开站点必须从主页开始，并默认大众体验、英语、亮色；修复主页 A/B 图注在亮色模式下的低对比度 | USER_MANDATED | 偏好只在当前标签页会话内保存；新标签页直接访问工作台或研发页时先回主页，同一标签页内站内导航仍正常。A/B 图注采用固定深色底与高对比浅色字，WCAG 对比度自动守卫不得低于 4.5:1。仅作为 PG-C 定向修复，不启动后续 Gate，不改变 Sites 所有者私有访问边界，也不授权 GitHub 写入 |
| D-064 | 2026-08-30 | 取消成片回嵌项目首页及由此产生的 Sites 重部署；G7B 改为 YouTube `Unlisted` 上传、双语字幕/封面/处理检查与香港未登录直链复验 | USER_MANDATED_SUPERSEDES_D-056_EMBED | 测试要求只要求 GitHub 与三分钟视频两个链接，首页播放器增加布局、性能、隐私、跨平台和重部署成本但不形成相称评审价值。保留 G7A/G7B 分离：G7A 冻结成片，G7B 经用户行动时确认频道身份后上传 YouTube，并把直接 watch URL 用于 README 与邮件；D-056 中“先通过成片 QA”的前半部分继续有效，首页回嵌部分由本决定取代 |
| D-065 | 2026-08-30 | 重新解释并取代 D-063 的入口语义：审查团队每次点击共享主页链接都必须打开 `/` 并恢复大众体验、英语、亮色；站内功能入口恢复新标签页，功能页必须从顶部开始 | USER_MANDATED_SUPERSEDES_D-063_ENTRY | 共享链接本身固定为主页根路由，每次主页加载都重置显示默认值；不再把合法的 `/app/` 或 `/development/` 新标签页重定向回主页。主页与跨页功能入口使用 `target="_blank"` + `noopener`。非 hash 页面在 `pageshow` 强制顶部，受控验证 hash 保持锚点。同步修复存储失败、动态入口测试、实际 CSS 对比度和可持续审计；Sites 可按原所有者私有边界部署，GitHub 只整理清单并等待一次集体授权 |
| D-066 | 2026-08-30 | 启动 PG-B：以用户提供的浅色产品 Logo 为品牌身份基准，只提取参考 GIF 的入场、停顿、字标显现与终态收束逻辑，完成有限动效与 PG-B 其余媒介/README/审计工作 | USER_MANDATED | 不复制参考 GIF 的旧建筑图标或旧名称；复用 D-026/D-027/D-028 墙—管—碰撞—气泡系统。PG-B 本地实现不授权 GitHub/Sites 写入、权限变化、PG-E、VG、G7 或视频。任何远端动作仍须展示精确文件集并取得行动时授权 |
| D-067 | 2026-08-30 | 纠正 PG-B 动效与执行路径：以本地 main 为唯一项目主流程，重做纯白底直接 GIF，并缩小左标、扩大安全间距 | USER_MANDATED_SUPERSEDES_PG-B_FIRST_CANDIDATE | 放弃粗糙 AI 分镜/WebP 中间路径；按墙体 → 绿色管线插入 → 橙色碰撞圈 → 提示气泡 → 橙色字标 → 黑色终态的稳定图层序列，接近参考 GIF 的 1120 × 360 / 4.8 秒构图与节奏。当前 Codex worktree 只作为执行载体，每个本地提交立即快进 main；公开 GitHub 写入仍须行动时确认。 |
| D-068 | 2026-08-30 | 放慢 PG-B 动效、用源图重新映射高质感图层、修正错位，并让完整管线沿延伸轴平移入场 | USER_MANDATED | 时间轴从 4.8 秒放慢到 6.4 秒；墙/管/环/气泡改用同一高保真 3D 源坐标，删除粗糙绘制图层；管线以完整实体沿约 9° 延伸方向从左下向右上平移。Logo 与字标同步放大，仍保持纯白底、有限播放、静态回退和公开 GitHub 行动时确认边界。 |
| D-069 | 2026-08-30 | 取消 GIF，直接采用用户提供的透明静态产品图作为 GitHub Logo | USER_MANDATED_SUPERSEDES_D-066_D-068_OUTPUT | README 只引用逐字节复制的 1672 × 941 RGBA PNG，显示宽度 520 px；删除 GIF、旧静态锁定图、动画图层、构建脚本和动效文档。D-066 至 D-068 仅保留为历史，不再定义当前输出。每个本地提交仍立即快进 main；公开 GitHub 写入仍须对精确文件集取得行动时确认。 |
| D-070 | 2026-08-30 | 启动 PG-E，并以程序生成的公开缩减工程情境双 IFC 作为唯一当前样例路线 | USER_MANDATED_WITH_AGENT_EVIDENCE | 在本地 `main` 主流程创建单一场地/建筑/楼层、6 段管线、4 道墙、2 根梁、36 候选对和6个独立哨兵；CC0-1.0、字节确定性、49/50 mm、200 mm、墙/梁碰撞与缺几何失败关闭必须自动守卫。技术通过不替代用户 Chrome UAT；任何 GitHub/Sites/权限/密钥/VG/G7/视频动作仍须遵守原停止门。 |
| D-071 | 2026-08-30 | 否决 PG-E 首版分散测试体量，改为简单但拟真的单层建筑 | USER_MANDATED_SUPERSEDES_D-070_GEOMETRY | 保留程序生成、CC0、双 IFC 和六哨兵方法，但将场景重建为 12 m × 8 m、层高 3.2 m 的单层社区诊所：完整外围护、带门洞内隔墙、地坪、6 根柱、3 根梁、7 条轴网和8个管段记录（7段具象吊顶管线与1个缺表示的失败关闭夹具）。墙/梁规则构件形成88条候选记录；柱、板和轴网只增强建筑上下文与显示，不暗改冻结的检测类型边界。用户确认建筑拟真与 Chrome UAT 前 PG-E 仍为 `IN_PROGRESS`。 |
| D-072 | 2026-08-30 | 接受拟真诊所效果，准备实例选项与 Logo v2 的一次性在线更新 | USER_MANDATED_COLLECTIVE_AUTHORIZATION_PENDING | 将 PG-E 双 IFC 加入网页实例下拉、运行时路径和静态构建；把用户提供的 1024 × 1024 RGBA Logo v2 逐字节覆盖规范 GitHub README Logo 路径。先完成本地实现、全回归和精确上传清单，再等待一次集体授权；源命名 v2 PNG/SVG、本地总纲和审计不得上传。旧 Logo v1 的公开证据不得冒充 v2 复验。 |
| D-073 | 2026-08-30 | 批准拟真诊所实例与 Logo v2 的精确29路径集体在线更新 | USER_MANDATED_COLLECTIVE_AUTHORIZATION | 授权覆盖已展示的29个公开路径、目录保持的登录态 Chrome GitHub 上传、29/29字节映射、所有者私有 Sites 保存/部署、在线 Logo/IFC/运行/3D/控制台复验和非递归证据尾。源命名 v2 PNG/SVG、本地总纲与审计继续排除；不得公开 Sites、改变访问/权限/密钥、调用 live provider 或启动 VG/G7、移动计算、视频。 |
| D-074 | 2026-08-30 | 批准 PG-E 审计问题与 Logo 比例的一次有限本地修复，并要求停在公开发布门前 | USER_MANDATED_LOCAL_REPAIR_WITH_PUBLICATION_STOP | 修复七条具象管线/一条缺表示记录与88条候选记录口径，新增结构化 UAT 和远端活体映射守卫，区分 Sites 版本9功能预检/版本10证据尾；Logo 只裁减透明画布并缩为240 px，保留全部可见像素。允许本地实现、测试、浏览器验收和提交，无需逐项审批；禁止 GitHub/Sites 写入或部署，完成后报告精确公开候选并等待一次集体授权。 |
| D-075 | 2026-08-30 | VG 采用共同创作模式，冻结产品优先、学术可信为支撑的高质感方向，并选定 M2 与 V2 | USER_MANDATED_VG_SELECTION | 成片目标保持165–175秒、16:9、1080p、英语女声与人工校对英中字幕；视觉节奏参考 Apple 产品片的克制、精确与高级感但保持原创品牌语言。音乐选 `M2 Masking the Masters`，声线选 `V2 Warm Modern / Ava`；D-077 后续以零费用本地音源取代原付费发行路径。PG-E 只用于确定性产品展示，不进入 AI 请求；AI 仅使用 C01/C03/C05/C08 四记录最小化包并逐次新鲜授权。 |
| D-076 | 2026-08-30 | 重申 VG 只冻结可审核预制作方案，最终成片生成及其全部执行工作归 G7A | USER_MANDATED_STAGE_BOUNDARY | VG 可完成逐句脚本、翻译稿、镜头表、风格帧、音乐/声线/音效试听、节拍与混音目标、录制/隐私/许可/事实清单；不得在 VG 生成最终旁白、正式录屏、实时 AI 调用、Remotion 成片、最终音效/混音/字幕/渲染。完整 VG 包必须再次由用户明确确认后才进入 G7A。 |
| D-077 | 2026-08-30 | VG 增加关键帧预处理与中文主审阅稿，SFX 选 P1，并用零费用本地女声取代付费语音路径 | USER_MANDATED_VG_REFINEMENT | 生成13张1080p静态关键帧与一张4K总表，以降低 G7A 构图和资产整合难度；英语台本保留原文并与简体中文待人工复核字幕逐句对照。Azure付费层即使非商业使用也会产生资源用量费用，因此默认改为零费用本地合成；最终 H/D 决选由 D-080 冻结。P1 `Dry Precision` 冻结为 SFX 方向。 |
| D-078 | 2026-08-30 | G7A 以质量优先使用现有工具和免费额度，并采用 `gpt-5.6-sol` + `xhigh` 为主配置 | USER_MANDATED_G7A_TOOL_AUTHORITY_WITH_COST_BOUNDARY | Remotion、video-shotcraft、其他已安装技能/插件和免费额度可在 G7A 范围内自由使用；机械性循环可临时降到 `high`，若工期和额度允许可用 `max` 做一次最终有界审计。该授权不包含任何付费购买、额度外费用、公开发布、访问变化或新增隐私暴露；这些仍须单独报告并取得许可。 |
| D-079 | 2026-08-30 | 修订正式录屏入口与关键帧占位治理，并把 H/C 作为零费用女声决选 | USER_MANDATED_VG_CAPTURE_AND_ASSET_IDENTITY | PG-E 诊所成片镜头必须依次使用 A1 `Choose MEP IFC`、A2 `Choose structural IFC` 的原生文件选择器，从无身份信息的中性演示文件夹选择两份匹配 IFC，确认共享坐标后运行；禁止从示例下拉框载入 PG-E。C01/C03/C05/C08 必须在 B · DEMO 中点选 review pack 并点击 `Load example`，禁止走原生文件选择器。关键帧中复用旧素材仅为 VG 快速构图占位，任何现有位图均不得直接作为产品证据进入 G7A；逐镜替换为当前对应产品画面并取得 `FINAL_SOURCE_VERIFIED` 后才能入时间线。用户认为 H 不错，其余审核内容基本无异议；新增无参考音频/无真人克隆的 MIT Chatterbox 默认女声 C 与 H 决选，B 降为备用。 |
| D-080 | 2026-08-30 | 淘汰实际为男声的 C，新增 Parler-TTS 命名女声 D，与 H 进行最后决选 | USER_MANDATED_VOICE_GENDER_CORRECTION | 用户听辨确认 C 是男声；基频交叉检查得到 C 中位117.6Hz、H中位193.5Hz，故C重命名为male-rejected并禁止成片使用。新增Apache-2.0 Parler-TTS Mini v1.1命名女声`Laura` D：不提供参考音频、不克隆真人，以女性/温暖/克制/中速/稍低音高/近讲/无播音腔描述在官方免费Space生成，PCM16单声道44.1kHz、16.974秒、中位180.0Hz。用户确认除人声外其他VG事务没有问题；VG只剩H/D听感选择与最终放行。若D入选，G7A冻结官方权重与描述并本机完成最终合成。 |
| D-081 | 2026-08-31 | 最终选择 H `Kokoro af_heart` 并批准 VG 创意包；G7A 未启动 | USER_MANDATED_FINAL_VOICE_AND_VG_CONTENT_APPROVAL | 用户在已确认其余 VG 事务无异议的基础上最终回复“H”。冻结本地 Apache-2.0 `Kokoro-82M af_heart` 为英语女声发行路径，沿用 V2 Warm Modern 表演方向与 M2 下方10–14 LU的混音目标；D仅作未采用备份，C保持淘汰。中英台本、172.2秒/13镜、SF01–SF03、13张关键帧及替换门、PG-E双文件选择器、受控AI点选、M2、P1与成本/隐私/许可边界获得内容批准；正式 VG `PASS` 仍须完成阶段性公开检查点，G7A=`NOT_STARTED`。本决定不生成最终旁白、不录屏、不调用实时AI、不实现/混音/渲染成片，也不授权外部发布。 |
| D-082 | 2026-08-31 | 批准一次 VG 有限本地逻辑修复，并在精确公开候选就绪后等待集体上传授权 | USER_MANDATED_LOCAL_REPAIR_WITH_COLLECTIVE_UPLOAD_STOP | 修复仅覆盖台本逐镜时长、M2 源相位/裁切相位契约、S11 四记录批次可行性、9 个 video-shotcraft 卡片实现追溯、H/P1 音频台账、总纲状态和公开/私有资产边界；不生成成片、不启动 G7A。公开候选冻结为 `docs/vg-publication-manifest.md` 中 25 个路径；试听音频、单张 KF 占位、私有总纲、源音乐和任何最终视频均排除。完成本地/公开双审计后停止，等待用户另行集体授权 GitHub 上传。 |

---

## 19. 未决问题清单

以下问题在总纲批准后按阶段解决，不阻塞当前审阅：

| ID | 问题 | 最迟决策时间 | 当前建议 |
|---|---|---|---|
| O-001 | 产品最终名称 | 已决（D-012） | `IFC ClashTrace`；仓库名 `ifc-clashtrace` |
| O-002 | 浏览器核心采用 web-ifc + 哪一种 BVH 实现 | 已决（D-014） | `web-ifc 0.0.77 + three-mesh-bvh 0.9.14`；G3 契约失败时再按停止门收窄或回退 |
| O-003 | 浏览器是否可靠输出穿透距离 | 已决（D-015） | 当前字段为 `null`；仅在后续独立验证可靠时开放 |
| O-004 | 是否加入 IfcDuctSegment | G3 通过后 | 默认不加入 |
| O-005 | 正式支持 IFC4X3 与否 | 已决（D-046） | 正式支持保持 IFC4，且仅限无前缀米制、共享坐标、`IfcPipeSegment × (IfcWall | IfcBeam)` 与已冻结几何证书；IFC4X3 继续探索，不宣传为支持 |
| O-006 | 最终 Sites 是否公开 | G7 | 先保持所有者私有；网站公开与 YouTube 上传解耦，最终提交前由用户另行批准，并只需保证香港教授团队未登录可达 |
| O-007 | UI 风格、颜色、语言和品牌 | 已决（D-026/D-027/D-028） | 大众版为 C 编辑科技：深重大地色、橘红强调、统一现代无衬线与受控圆角；极简版保持黑白灰；Logo 图文安全间距与有限动效已批准 |
| O-008 | 是否加入运行时 AI 解释 | 已决（D-022） | 加入；功能必须实现但由用户主动触发，确定性结果始终为权威且无 API 可完成核心闭环 |
| O-009 | 项目代码与程序生成 IFC 的公开许可证 | 已决（D-018） | 代码 MIT；本项目程序生成 IFC 与配套真值数据 CC0-1.0；第三方数据不适用该授权 |
| O-010 | 测试是否能证明冻结基线和批准契约未被再生成掩盖 | G3A（强制前置） | 使用隔离生成、路径—哈希映射、Git 前后断言和契约突变负向测试；未通过不接受 G3 绿灯 |
| O-011 | 2 mm 容差的一般几何语义及 AABB 守卫适用范围 | 已决（G3B-R1 `PASS`） | 在非退化结构内蚀核心上采用最大欧氏内部深度严格 `> 0.002 m`；有体积相交但内蚀核心为空/退化时不安全报 `CLEAR`，而是 `NOT_EVALUATED`；解析证书以十进制精确比较阈值；AABB 守卫仅限 C04 证据 |
| O-012 | G4AI 采用哪一家免费额度 API 及何种代理/密钥方案 | 已决（D-037/D-038；基础 enum-only live `PASS`；G4AI-R1 `PASS`；D-042/D-043/D-044/D-045 当前成段路径 `PASS`） | GroqCloud Free Plan / `openai/gpt-oss-20b`；同源 Node 代理从 `GROQ_API_KEY` 环境读取密钥，浏览器无提供商端点/认证头/密钥访问；exact-key 成段分析使用当前官方 strict 结构子集、`1,600` completion cap、temperature 零、六记录上限、重试新同意、异步身份隔离、双语与未支持概念本地拒绝。最终英文与简体中文 live 均通过，确定性记录不变；外部条件变化即退出或更换适配器，不改检测核心 |
| O-013 | 视频最终语言/字幕、视觉风格、旁白音色与音乐 | VG | 默认普通话、工程审查风格、清晰中性 AI 旁白；以风格帧、10–15 秒声音样例和音乐对照供用户确认 |
| O-014 | 近真实工程样例采用纯生成、公开样本改造还是两者组合 | 已决（D-070/D-071/D-072/D-073；用户已接受拟真效果） | 采用程序生成、CC0-1.0、可公开缩减的拟真单层社区诊所双 IFC，并已作为公开网页实例和所有者私有 Sites 候选；不引入外部或真实项目数据。六个哨兵独立冻结，外部同步与自动托管预检已闭合；最终用户托管试用前保持 `IN_PROGRESS`。 |
| O-015 | GitHub README Logo 的最终格式、尺寸与体积上限 | 已决（D-069；PG-B `PASS`） | 只使用用户选定、逐字节复制的 1672 × 941 RGBA PNG（468,496 B，SHA-256 `b56f3a2a3d3ef5d37599a8a84c54cf1a5f56527057db487601d7bced24793142`），README 居中显示宽度 520 px；GIF、动效回退与构建链全部取消。GitHub 桌面渲染 520 × 293、窄屏渲染 279 × 157，均无溢出。 |
| O-016 | 最终视频采用同源静态资产还是外部承载 | 已决（D-064） | 使用 YouTube `Unlisted` 作为正式视频直链；不回嵌网站首页、不因视频重部署；保留本地 MP4/字幕/封面/哈希，备用承载仅在主链失败且用户另行决定时启用 |
| O-017 | YouTube 使用哪个频道及对外显示身份 | G7B 上传前 | 优先使用名称、头像和说明均适合教授查看的中性项目频道；上传前由用户现场确认，不擅自创建频道、改名或暴露个人账号信息 |

---

## 20. 进展日志

### 日志格式

每次实质工作必须追加一条日志，不覆盖历史记录。每条至少包含：

- 时间；
- 所处 Gate；
- 本次目标；
- 执行动作；
- 使用环境与关键命令；
- 新增/修改文件；
- 验证结果；
- 失败和异常；
- 隐私/许可证影响；
- 当前结论；
- 下一步；
- 是否需要用户决定。
- 对应本地提交主题与 SHA（形成后补记）；
- GitHub 阶段提交 URL/远程 SHA、上传时间与回查结论（形成后补记）。

### L-0001 — 项目约束登记与总纲创建

**时间：** 2026-08-25  
**Gate：** G0 — 总纲人工审批  
**目标：** 将用户约束与已讨论方向整理为可审阅、可量化、可追溯的项目总纲。  

**已执行：**

- 核对 `<PROJECT_ROOT>` 当前为空目录；
- 核对该目录内没有额外 `AGENTS.md` 项目指令；
- 使用 PowerShell 7 检查环境，版本为 `7.6.4`；
- 创建本总纲文件；
- 未创建代码、依赖、UI、站点或 GitHub 内容。

**新增文件：**

- `BIMCLASH_AGENT_MASTER_PLAN.md`

**验证状态：**

- 项目根目录约束：PASS；
- PowerShell 7 约束：PASS；
- GitHub 未操作：PASS；
- UI/前端未实施：PASS；
- 当前公开内容：无；
- 隐私风险：本地总纲含工作区绝对路径，仅用于本地治理；公开前必须改为相对路径或脱敏版本。

**当前结论：**

项目仍处于 `DRAFT_FOR_USER_REVIEW`。用户批准前不得开始实现。

**下一步：**

等待用户审阅并提出修改；收到明确批准后，将 G0 标记为 PASS，并从 G1 的最小技术可行性验证开始。

**需要用户决定：** 是。

### L-0002 — 纳入本地 Git 与逐步骤 GitHub 上传要求

**时间：** 2026-08-25 晚间  
**Gate：** G0 — 总纲人工审批  
**目标：** 根据用户首轮审阅指示，将本地版本管理与阶段性公开进度展示写入项目治理流程。  

**用户决定：**

- 本地项目进度与变更必须使用 Git 管理；
- 每个步骤完成后，均须向 GitHub 上传更新一次，以展示项目进度变化。

**已执行：**

- 新增本地 Git 的初始化、状态检查、提交粒度、提交命名、非破坏性历史和提交前审计规则；
- 新增 G0A，规定正式启动后的本地 Git 基线与首次 GitHub 网页检查点；
- 将“步骤完成”量化为验收、日志、脱敏、本地提交、网页上传、回查和映射登记全部通过；
- 将 GitHub 流程从“最终阶段集中上传”改为“每个已登记步骤通过后上传一次”；
- 增加 `PROGRESS_SYNC.md` 同步台账规划，以及本地 SHA 与 GitHub 网页提交的映射规则；
- 更新 G7、时间表、责任边界、状态词和决策记录。

**验证状态：**

- 本轮仅修改总纲：PASS；
- 本地 Git 尚未初始化：PASS，符合当前停止门；
- GitHub 未访问、未创建、未修改：PASS；
- UI、代码、依赖和部署均未开始：PASS；
- 当前公开内容：无；
- 当前总纲仍含本地绝对路径，仅限本地治理使用；首次上传前必须生成或检查可公开版本。

**当前结论：**

总纲已形成 `0.2.0-draft` 修订稿，等待用户确认本次增补。用户明确批准并开始后，先完成 G0A，再进入 G1。

**下一步：**

等待用户确认修订稿；确认后将 G0 标记为 `PASS`，启动 G0A。

**需要用户决定：** 是。

### L-0003 — 总纲批准与 G0A 启动

**时间：** 2026-08-25 22:43:08 +08:00  
**Gate：** G0 `PASS`；G0A `IN_PROGRESS`  
**目标：** 执行已批准总纲规定的本地版本库初始化、公开候选审计与首次 GitHub 网页检查点。  

**用户决定：**

- 用户已明确批准 `0.2.0-draft` 修订稿并指示立即开始 G0A；
- 不重新征求总纲批准；
- G0A 未完整通过前禁止开始 G1、产品代码、依赖安装、数据下载、UI/视觉设计和站点部署。

**已验证事实：**

- 项目唯一工作区为本文件登记的项目根目录；
- 权威总纲已完整读取；
- PowerShell 版本为 `7.6.4`，满足主版本不低于 7 的约束；
- 启动时工作区仅包含本总纲，尚无本地 Git 仓库、依赖、缓存、产品代码或数据。

**当前动作：**

- 将 G0 标记为 `PASS`；
- 将 G0A 标记为 `IN_PROGRESS`；
- 下一步初始化仅限本地的 Git 仓库，创建并审查 `.gitignore` 与 `PROGRESS_SYNC.md`。

**失败和异常：**

- 首次版本探测命令因外层 shell 引号解释未返回有效版本号；未基于该结果执行项目变更；随后以隔离引号重新核验并确认 `7.6.4`。

**隐私/许可证影响：**

- 本总纲包含仅供本地治理使用的绝对工作区路径，保持不可直接公开；
- 尚未访问或修改 GitHub，未公开任何文件。

**当前结论：** G0 `PASS`；G0A `IN_PROGRESS`。  
**下一步：** 建立本地基线与公开候选清单，完成专项审计。  
**需要用户决定：** GitHub 首次仓库名称与可见性将在本地基线通过后请求。  
**对应本地提交：** 待形成。  
**GitHub 检查点：** 待用户确认仓库名称与可见性后执行。

### L-0004 — G0A 本地仓库、公开候选与提交前审计

**时间：** 2026-08-25 22:51:53 +08:00  
**Gate：** G0A `IN_PROGRESS`  
**目标：** 建立不含远程的本地 Git 基线，形成可安全网页上传的脱敏候选并完成提交前审计。  

**已执行：**

- 在项目根目录初始化 `main` 分支本地 Git 仓库；
- 将仓库级 Git 作者设置为非个人名称与不可投递的 `.invalid` 占位邮箱，未读取或沿用用户全局真实身份；
- 核验 Git 远程数量为 0，未配置 `origin`；
- 创建并审查 `.gitignore`、`PROGRESS_SYNC.md` 与可复跑的本地 G0A 审计脚本；
- 从本地权威总纲机械生成 `BIMCLASH_AGENT_MASTER_PLAN.public.md`，仅将两个本地绝对根路径替换为中性占位符；
- 将三份网页上传候选限定为 `.gitignore`、`PROGRESS_SYNC.md` 和脱敏公开版总纲；本地权威总纲、`.git/` 与审计脚本不属于网页上传候选；
- 使用 PowerShell 7 对公开候选执行绝对路径、邮箱、常见密钥格式、凭据赋值模式扫描，并对全工作区执行大文件、忽略规则、远程与作者审计。

**验证结果：**

- 脱敏公开版与本地总纲除登记的路径替换及末尾换行外内容一致：PASS；
- 公开候选绝对路径命中：0；邮箱命中：0；凭据/密钥命中：0；
- 工作区文件数：5；超过 1 MiB 文件数：0；最大文件：51,833 bytes；
- 6 类忽略规则样本失败数：0；
- Git 远程数：0；仓库级非个人作者检查：PASS；
- G0A 本地提交前专项审计：PASS。

**失败和异常：**

- 初次 Git 初始化由沙箱执行身份创建，触发 Git 所有权保护并阻断后续工具刷新。修复前已验证该仓库没有分支引用、打包对象、提交或远程，仅移除失败的空 `.git` 后在相同工作区重新初始化；项目文件未删除；
- 首轮公开扫描将审计脚本自身纳入候选，因此检测到脚本内的检测正则与非个人作者占位邮箱。未降低规则；将审计脚本保留为本地证据并移出网页候选后，三份候选复扫为零命中。

**隐私/许可证影响：**

- 未安装依赖、未下载数据，不产生第三方许可证变化；
- 本地权威总纲继续包含治理所需绝对路径，明确禁止原样上传；
- 未在项目文件中记录用户姓名、真实邮箱、账号、浏览器资料或凭据。

**当前结论：** 本地基线内容已通过提交前审计；G0A 仍为 `IN_PROGRESS`，尚未形成本地提交或 GitHub 检查点。  
**下一步：** 暂存经审查的本地基线，复核提交范围，形成 `step(G0A): establish local governance baseline` 提交并登记 SHA。  
**需要用户决定：** 本地提交通过后，需要确认 GitHub 仓库名称与可见性。  
**对应本地提交：** 待形成。  
**GitHub 检查点：** 待执行。

### L-0005 — G0A 本地基线提交形成

**时间：** 2026-08-25 22:54:43 +08:00  
**Gate：** G0A `IN_PROGRESS`（本地部分 `PASS`；GitHub 决策待定）  
**目标：** 对通过专项审计的 G0A 本地治理基线形成可追溯提交并核对提交后状态。  

**步骤开始状态：** `## No commits yet on main`，5 个明确未跟踪治理文件；Git 远程数为 0。  

**已执行：**

- 仅显式暂存 `.gitignore`、本地权威总纲、脱敏公开版总纲、`PROGRESS_SYNC.md` 与本地 G0A 审计脚本；
- 复核暂存文件数为 5，未使用宽泛暂存；
- 使用仓库级非个人作者形成根提交；
- 再次核验提交主题、完整/短 SHA、远程数量与工作树状态。

**验证结果：**

- 本地提交主题：`step(G0A): establish local governance baseline`；
- 本地完整 SHA：`f2c457a0327b3cf3727916fa0919e36a47aa526d`；
- 本地短 SHA：`f2c457a0327b`；
- 提交文件数：5；
- Git 远程数：0；
- 提交后状态：`## main`，工作树干净；
- 本地 G0A 基线：PASS。

**失败和异常：**

- 普通沙箱账户首次尝试暂存时无法在工作区所有者创建的 `.git` 内建立 `index.lock`，暂存未发生；随后在工作区所有者上下文按同一显式文件清单完成暂存与提交；
- Git 提示当前工作副本的 LF 在未来由 Git 触碰时可能转换为 CRLF；本次提交成功，未进行无关的全文件换行重写。该提示记录为非阻塞性环境差异。

**隐私/许可证影响：**

- 提交作者为非个人占位身份；
- 本地提交包含权威总纲，但 GitHub 网页候选仍排除该本地文件，只允许上传脱敏公开版；
- 未产生依赖、数据或第三方许可证变化。

**当前结论：** G0A 本地基线已验证并提交；因仓库首次名称、可见性和 Chrome 登录状态尚待用户确认，G0A 仍为 `IN_PROGRESS`，不得进入 G1。  
**下一步：** 请求用户确认 GitHub 仓库名称、可见性，并确认 Google Chrome 已稳定登录 GitHub；随后只通过 Chrome 网页创建/打开仓库并上传三份候选。  
**需要用户决定：** 是。  
**对应本地提交：** `step(G0A): establish local governance baseline` / `f2c457a0327b3cf3727916fa0919e36a47aa526d`。  
**GitHub 检查点：** 待用户决定。

### L-0006 — 公开命名与 GitHub 首次状态决定

**时间：** 2026-08-25 23:05:40 +08:00  
**Gate：** G0A `IN_PROGRESS`（本地基线 `PASS`；GitHub 上传待执行）  
**目标：** 记录首次 GitHub 仓库创建所需的名称、可见性与浏览器会话决定。  

**用户决定：**

- 公开项目名选择 `IFC ClashTrace`；
- GitHub 仓库名选择 `ifc-clashtrace`；
- GitHub 仓库可见性选择 `Public`；
- 用户确认 Google Chrome 中 GitHub 登录稳定。

**命名解释：**

- `IFC` 明确输入与技术边界；
- `Clash` 指向 BIM 中的空间冲突问题，具体范围仍由副标题和规则限定为管道—结构硬碰撞；
- `Trace` 强调每条结果映射双方 GUID、模型哈希、容差与确定性证据的可追溯性；
- 删除 `Agent`，避免暗示项目具有未实现的运行时 LLM、自主规划或工具编排能力。

**已执行：**

- 新增决策 D-012 与 D-013；
- 将总纲中的公开项目名更新为 `IFC ClashTrace`；
- 将 O-001 标记为已决；
- 更新 G0A 同步台账中的仓库名称、公开可见性和当前状态。

**验证与隐私影响：**

- `Public` 决定不豁免脱敏要求；仅三份已扫描候选可进入首次网页上传；
- 本地权威总纲仍禁止原样上传；
- Chrome 登录确认只记录为布尔状态，不记录账号、Cookie、个人资料或凭据。

**当前结论：** GitHub 首次创建所需用户决定已齐备；G0A 仍为 `IN_PROGRESS`。  
**下一步：** 重新生成并扫描公开版总纲，提交本次命名决定；随后只通过 Chrome GitHub 网页创建公开仓库、上传三份候选并回查。  
**需要用户决定：** 无。  
**对应本地提交：** 待形成。  
**GitHub 检查点：** 待执行。

### L-0007 — G0A GitHub 公开检查点与网页回查

**时间：** 2026-08-25 23:14:54 +08:00  
**Gate：** G0A `PASS`；G1 `NOT_STARTED`  
**目标：** 只通过用户已登录的 Google Chrome GitHub 网页创建公开仓库、上传脱敏基线、回查并登记本地—远程映射。  

**步骤开始状态：** `## main`，工作树干净；Git 远程数为 0。  

**已执行：**

- 在 Google Chrome GitHub 网页创建公开仓库 `ifc-clashtrace`；
- 仓库描述设置为 `Deterministic IFC hard-clash detection with traceable GUID evidence.`；
- 未让 GitHub 自动创建 README、许可证或 `.gitignore`；
- 仅通过网页文件选择器上传 `.gitignore`、`PROGRESS_SYNC.md` 与 `BIMCLASH_AGENT_MASTER_PLAN.public.md`；
- 网页提交信息设置为 `G0A governance baseline | local f2c457a0327b`；
- 上传后打开仓库首页、提交详情和三份文件逐项回查。

**本地—远程映射：**

- 步骤 ID：`G0A`；
- 本地基线主题：`step(G0A): establish local governance baseline`；
- 本地基线 SHA：`f2c457a0327b3cf3727916fa0919e36a47aa526d`；
- 本地命名决定提交：`7abcb02d4ac89eb29fb2a387d580552cadb9a621`；
- GitHub 远程 SHA：`12c78eee68c28ac3dd4bc33ccd437ced9bd6d5b2`；
- GitHub 提交 URL：`https://github.com/delongwangshu49-hub/ifc-clashtrace/commit/12c78eee68c28ac3dd4bc33ccd437ced9bd6d5b2`；
- GitHub 仓库 URL：`https://github.com/delongwangshu49-hub/ifc-clashtrace`；
- 网页提交时间：2026-08-25 23:12 +08:00（GitHub 页面显示）；
- 回查时间：2026-08-25 23:14:54 +08:00。

**网页回查结果：**

- 仓库名称、描述与 `Public` 可见性：PASS；
- 默认分支 `main`、首次提交与提交摘要：PASS；
- 提交范围为 3 个预期文件：PASS；
- `.gitignore` 的秘密、本地配置、私有产物与本地输出忽略规则：PASS；
- `PROGRESS_SYNC.md` 标题渲染、本地基线 SHA 与可见性记录：PASS；
- 脱敏总纲标题渲染、`<PROJECT_ROOT>` 与 `<LEGACY_REFERENCE_ROOT>` 占位符：PASS；
- 三份网页文件中的绝对本地路径命中：0；
- 脱敏总纲中的旧 `BIMClash Agent` 品牌残留：0；
- 本地 Git 远程数仍为 0，未配置 `origin`：PASS。

**失败和异常：**

- 打开上传页面后首次快照仍显示仓库首页，但链接已进入活动状态；未重复点击。等待 1.2 秒后 URL 正常转为 `/upload`，确认是延迟导航而非失败；
- 未发生重复 GitHub 失败操作、命令行远程回退、隐私异常或上传范围偏差。

**隐私/许可证影响：**

- 仓库已按用户决定公开；公开内容严格限制为三份已脱敏治理候选；
- 本地权威总纲、审计脚本、本地 Git 配置和 `.git/` 未上传；
- 无依赖、数据或第三方许可证变化。

**当前结论：** G0A 的本地基线和 GitHub 检查点均为 `PASS`；G0A 正式完成。  
**下一步：** 准备进入 G1，但本任务未启动 G1、产品代码、依赖安装、数据下载、UI/视觉设计或部署。  
**需要用户决定：** 无。  
**对应本地提交：** G0A 最终同步审计提交待形成。  
**GitHub 检查点：** `PASS` / `12c78eee68c28ac3dd4bc33ccd437ced9bd6d5b2`。

### L-0008 — G1 启动门核验与技术可行性阶段开始

**时间：** 2026-08-26 11:01:08 +08:00
**Gate：** G1 `IN_PROGRESS`
**目标：** 在不越过 Design Gate 的前提下，验证 IfcOpenShell 参考路线与浏览器/Node IFC 几何路线的最小可行性。

**步骤开始状态：** `## main`，工作树干净；HEAD 为 `6348458b42b64db7b524ac034f01b6b387df707c`；Git 远程数为 0。

**已验证事实：**

- 唯一 Git 根目录与项目根目录一致；
- 项目内未发现额外 `AGENTS.md`；
- PowerShell 版本为 `7.6.4`，满足主版本不低于 7 的约束；
- G0A 最终本地提交为 `sync(G0A): record GitHub checkpoint verification`；
- Python `3.13.15` 与 pip `26.2.1` 可用；系统 PATH 中没有 Node.js 或 npm；
- 官方资料确认 IfcOpenShell `0.8.5` 提供 Python 3.13 Windows wheel 与 `clash_intersection_many(..., tolerance=0.002)`；web-ifc `0.0.77` 要求 Node 16+；Node.js 官方当前 LTS 为 `24.19.0`。

**当前动作：**

- 将 G1 从 `NOT_STARTED` 转为 `IN_PROGRESS`；
- 仅开展无样式命令行与自动测试 Spike；不创建正式 UI、品牌、视觉样式或部署内容；
- 所有运行时、依赖、样本与测试产物只允许位于项目根目录内。

**失败和异常：**

- 本任务首次合并读取命令因外层 PowerShell 提前展开版本变量而解析失败；该命令未读取到项目内容，也未产生任何项目变更；修正引号后核验通过。

**隐私/许可证影响：**

- 尚未下载数据或安装依赖；
- 本地权威总纲继续包含治理所需绝对路径，不属于公开上传候选；
- 后续引入的运行时与依赖必须记录来源、版本、许可证和哈希，并由忽略规则排除缓存与本地环境。

**当前结论：** G1 启动门 `PASS`，技术可行性结论尚未形成。
**下一步：** 在工作区内建立隔离 Python 环境与本地 Node LTS 运行时，构造一个已知管道—墙体硬碰撞 IFC，并分别验证参考与 web-ifc 路线。
**需要用户决定：** 无。
**对应本地提交：** 待 G1 技术验收完成后形成。
**GitHub 检查点：** 待 G1 本地验收通过后执行。

### L-0009 — G1 双路线技术验收与公开候选冻结

**时间：** 2026-08-26 11:24:55 +08:00
**Gate：** G1 `IN_PROGRESS`（技术验收 `PASS`；版本检查点待完成）
**目标：** 用同一确定性 IFC4 管道—墙体案例证明参考与浏览器路线可行，并冻结可复跑、可审计的 G1 证据。

**已执行：**

- 在项目内创建隔离 Python 环境并安装 IfcOpenShell `0.8.5`；
- 从 Node.js 官方发行站下载 Windows x64 Node.js `24.19.0` LTS，使用官方 `SHASUMS256.txt` 验证归档 SHA-256 为 `57f71ab3652e797d84acddc79c81cc9ff1c6ddb2a1974cdb83f00fee9bff4c73`；
- 锁定 `web-ifc 0.0.77`、`three 0.185.1` 与 `three-mesh-bvh 0.9.14`；运行时、虚拟环境、依赖和缓存均位于项目目录内且被 Git 忽略；
- 建立可重复生成的 IFC4 双模型：4.0 m × 0.2 m × 3.0 m 墙体与半径 0.1 m、沿 Y 轴穿墙的 2.0 m 管段；两模型使用米制单位、共同项目坐标和固定 GUID；
- 固定 IFC Header、项目 GUID 和产品 GUID；连续两次生成结果按字节 SHA-256 完全一致；
- 运行 IfcOpenShell geometry tree `clash_intersection_many` 参考检测；
- 运行 Node.js 中的 web-ifc/WASM 解析、网格提取、GUID 回映射与 three-mesh-bvh 精确三角表面相交；
- 启动无样式本地技术页，并在实际桌面 Google Chrome 中执行初次运行、三次连续重载及确定性 Header 修复后的最终重载；
- 创建一键本地复跑脚本、G1 证据文档、README、脱敏 AI 辅助记录和公开候选审计脚本。

**新增/修改文件：**

- `README.md`、`PROMPTS.md`、`package.json`、`package-lock.json`；
- `requirements-g1.txt`、`requirements-g1.lock.txt`；
- `data/generated/g1/` 下本地受控 IFC、清单与哈希；
- `scripts/setup-g1.ps1`、`scripts/test-g1.ps1`、两条检测脚本、生成脚本、本地静态服务器与 G1 审计脚本；
- `spikes/g1-browser/` 下无样式 Chrome 技术页；
- `docs/g1-feasibility.md`。

**验证结果：**

- `scripts/test-g1.ps1`：`PASS`；
- IfcOpenShell：解析两个 IFC4、各生成 1 个几何、返回唯一固定 GUID 对，类型为 `pierce`、距离为 `0.2 m`、容差为 `0.002 m`；
- Node/web-ifc/BVH：解析两个 IFC4、返回同一唯一 GUID 对、GUID 完整率 100%、两模型协调矩阵均为单位矩阵；
- Google Chrome：初次运行与 4 次复跑均为 `PASS`，页面报告同一 GUID 对，warning/error 日志数为 0；
- G1 五项必须证明内容全部技术通过；浏览器路线无需触发 Python Web 回退。

**失败和异常：**

- 普通沙箱下载 Node 时出现认证失败；获批后按相同官方 URL 重试并通过哈希校验；
- npm 首次使用用户级默认缓存被拒绝；将缓存显式设为项目内忽略目录后通过；
- 一次 Python 元数据探测命令发生引号解析错误，一次 `pip show` 在 GBK 控制台遇到第三方作者字符编码错误；均未改变依赖或项目结论，依赖版本与许可证改由锁文件、包元数据和官方来源核验；
- 参考 clash 包装器不支持属性式 GUID 读取，按官方文档改用 `get_argument(0)` 后通过；
- web-ifc 声明文件列出 `FlatMesh.delete()`，运行时回调对象没有该方法；移除无效调用后通过，几何逻辑未改变；
- Chrome 连接不支持 `networkidle` 等待模式；改用受支持的 `load` 状态后验证通过；
- 初版生成器的 Header 时间与项目 GUID 不固定，导致文件哈希跨运行变化；固定 Header 与 GUID 并加入双生成哈希断言后通过；
- 本地静态服务器的 PTY 未响应两次 Ctrl+C，读取并校验项目内 Node 进程路径后只终止该服务器进程；未影响其他 Node 进程。

**隐私/许可证影响：**

- 受控模型完全程序生成，不含私人或工程项目数据，IFC Header 不含绝对路径、真实姓名或邮箱；
- 项目代码与生成数据尚无用户批准的公开许可证；不做 CC0 或开源授权声明，G1 GitHub 上传候选明确排除 `data/generated/g1/`；
- 本地权威总纲、审计脚本、依赖目录、运行时、缓存和本地输出不进入公开候选；
- 公开候选只包含相对路径、依赖来源/许可证、可复跑生成器和技术证据，仍须通过专项扫描。

**当前结论：** G1 技术验收 `PASS`，Gate 仍为 `IN_PROGRESS`，等待公开候选审计、本地步骤提交与 GitHub 网页检查点。
**下一步：** 机械更新脱敏公开版总纲，执行 G1 专项审计，形成 `step(G1): prove dual geometry feasibility` 本地提交。
**需要用户决定：** G1 完成不需要；项目代码与生成数据许可证须在 G2 开始前决定。
**对应本地提交：** `55ed3a2f07d525e9c14f182bc2213fe2ef36694c`（`step(L-0018): expand clearance AI and video plan`）。
**GitHub 检查点：** 待执行；生成 IFC 与本地治理/审计文件明确不上传。

---

### L-0010 — G1 本地步骤提交与 GitHub 网页检查点闭环

**时间：** 2026-08-26 11:55:33 +08:00
**Gate：** G1 `PASS`
**目标：** 将已通过技术验收和公开候选审计的 G1 内容固化为本地权威提交，并通过 Chrome GitHub 网页完成公开检查点、回查和映射登记。

**已执行：**

- 形成并回查本地步骤提交 `d262474cc6820c7bae9ba77acea2aa754af5bdd0`，主题为 `step(G1): prove dual geometry feasibility`；提交后工作树干净且 Git 远程数为 0；
- 按用户明确批准的恢复思路，仅使用 Chrome 直接控制完成 GitHub 网页同步，不再使用 Computer Use；
- 根目录批量上传 8 个文件；随后用真实文件分别建立 `docs/`、`scripts/` 与 `spikes/g1-browser/`，再在已有目录批量上传其余文件；
- 共公开 17 个已审计文件；继续排除 `data/generated/g1/`、本地权威总纲、审计脚本、运行时、依赖、缓存和本地输出；
- 在 GitHub 网页逐层回查根目录及三个子目录，并回查完整提交历史与公开可见性。

**GitHub 提交链：**

1. `1be60cb2f3608703ba0c9334c018041ea88bab3a` — 根目录 8 个 G1 文件；
2. `5f84159521eb3bb3e6497e195b791a807287de0d` — `docs/g1-feasibility.md`；
3. `c5ff02ed4c3da7156b85429c7a5a66892a06b882` — `scripts/g1-generate-controlled.py` 与目录建立；
4. `777d4c05d12c443e5d36ec18d18878bc2f5a6d57` — 其余 5 个 G1 脚本；
5. `47adbf98628abd85e340a5219b2117f0fbc705f1` — `spikes/g1-browser/index.html` 与目录建立；
6. `0b338141c33ce3add5f81e6d5251c175a67a074a` — `spikes/g1-browser/app.mjs`，G1 远程检查点最终 SHA。

**网页回查结果：**

- 仓库仍为 `Public`，默认分支仍为 `main`；
- 根目录存在 `docs`、`scripts`、`spikes/g1-browser` 及全部 8 个预定根文件；
- `docs` 含 1 个预定文件，`scripts` 含 6 个预定文件，`spikes/g1-browser` 含 2 个预定文件；
- 公开根目录不存在 `data/`，无生成 IFC 被上传；
- GitHub 提交历史显示上述 6 个连续 SHA，均位于原 G0A 检查点 `12c78eee68c28ac3dd4bc33ccd437ced9bd6d5b2` 之后；
- 根文件、批量脚本、文档与最终浏览器逻辑提交均带 G1/本地短 SHA 映射；GitHub 新建文件页的 Copilot 在两个种子文件提交时异步覆盖了已填写标题，分别显示为 `Add IFC model generation script` 与 `Add initial HTML structure for G1 Browser`。该网页行为不改变文件内容、提交 SHA 或连续检查点映射，已作为非阻塞异常保留。

**当前结论：** G1 的技术、本地 Git、公开审计、GitHub 网页同步、远程回查和映射登记全部 `PASS`。
**下一步：** 不自动开始 G2；先由用户决定 O-009（项目代码与程序生成 IFC 的公开许可证）。
**需要用户决定：** O-009。
**对应本地提交：** `d262474cc6820c7bae9ba77acea2aa754af5bdd0`。
**GitHub 检查点：** `PASS` / `0b338141c33ce3add5f81e6d5251c175a67a074a`（提交链起点 `1be60cb2f3608703ba0c9334c018041ea88bab3a`）。

---

### L-0011 — G1 状态一致性一次性修复

**时间：** 2026-08-26 12:20:56 +08:00
**Gate：** G1 `PASS`（一致性复核与修复 `PASS`；G2 未启动）
**目标：** 消除本地证据页、公开总纲、公开同步台账与实际 G1 完成状态之间的口径差异，不扩大技术或产品范围。

**已执行：**

- 将 `docs/g1-feasibility.md` 从过期的 `TECH_PASS ... pending` 状态改为完整 `PASS`，并写明原 G1 本地步骤提交和远程检查点；
- 将 Chrome 复跑口径统一为“初次运行及后续 4 次重载”，与 L-0009 的实际记录一致；
- 形成本地修复提交 `b4e010f7dec1b6fb1d2b2736efd0c6e63fdd9295`，主题为 `fix(G1): reconcile checkpoint status evidence`；
- 重新运行 G1 技术测试、发布审计、Python 依赖检查、npm 安全审计与 Git diff 检查，全部通过；
- 经用户在动作时明确批准后，仅通过 Chrome GitHub 网页更新公开总纲、同步台账和证据页；
- 回查确认公开总纲显示 G1 `PASS` 且不再显示 `IN_PROGRESS`，公开台账包含 G1 本地—远程映射，公开证据页显示完整 `PASS`、不含 pending 文案并记录 4 次后续重载；仓库仍为 `Public`，根目录仍无 `data/`。

**修复检查点：**

- 治理文件提交：`2de8c45684a3d1d894527674bb644510e4881483`；
- 证据页提交及最终远程 HEAD：`30b01b5679145298ca0aa1bd02697e61eae9a767`；
- 两个提交标题均为 `G1 consistency repair | local b4e010f`，满足步骤 ID 与本地短 SHA 映射要求。

**当前结论：** 审阅中发现的公开状态、台账与证据页不一致均已关闭；G1 现为无保留 `PASS`。
**下一步：** 等待用户下一指令；不得自动开始 G2，O-009 许可证停止门继续有效。
**对应本地提交：** `b4e010f7dec1b6fb1d2b2736efd0c6e63fdd9295`。
**GitHub 修复检查点：** `PASS` / `2de8c45684a3d1d894527674bb644510e4881483` → `30b01b5679145298ca0aa1bd02697e61eae9a767`。

---

### L-0012 — O-009 许可证决定与 G2 启动

**时间：** 2026-08-26 12:54:29 +08:00
**Gate：** G2 `IN_PROGRESS`
**目标：** 关闭 G2 启动前许可证停止门，并在不进入正式 UI/视觉设计的前提下冻结 8 个可复现受控案例、独立操作台账、机器真值、哈希与参考结果。

**用户决定：**

- 项目代码采用 MIT；
- 本项目程序生成的 IFC 与配套真值数据采用 CC0-1.0；
- MIT 版权主体采用非个人化的 `IFC ClashTrace contributors`，不写入用户真实身份；
- 第三方依赖和未来外部数据不因本决定改变许可证，仍须逐项登记来源、许可与署名。

**步骤开始状态：** `## main`，工作树干净；HEAD 为 `bc54cf03f070b6e787ef8931052fd3100a179e45`；Git 远程数为 0；PowerShell 为 `7.6.4`。

**当前动作：**

- 新增决策 D-018，将 O-009 标记为已决；
- 将 G2 从 `NOT_STARTED` 转为 `IN_PROGRESS`；
- 仅开展程序生成数据、机器真值、参考实现、无样式自动测试、许可证与证据文档工作；
- Design Gate 仍未通过，不实施正式 UI、品牌、视觉样式或产品结果页。

**隐私/许可证影响：**

- 合成案例不得包含私人或工程项目数据；
- 生成 IFC Header 不得包含本地绝对路径、真实姓名或邮箱；
- CC0-1.0 只覆盖本项目程序生成的 IFC 与配套真值数据，不覆盖第三方数据；
- 公开候选仍须通过密钥、个人信息、本地路径、大文件、许可证和临时产物审计。

**当前结论：** O-009 `PASS`；G2 已获准启动，技术与版本验收尚未形成。
**下一步：** 建立 8 个确定性受控案例、操作台账、机器可读标准答案与参考检测结果，并明确不做隐藏留出评测的限制。
**需要用户决定：** 无。
**对应本地提交：** 待 G2 验收完成后形成。
**GitHub 检查点：** 待 G2 本地验收通过后，仅通过已登录 Chrome 网页执行。

---

### L-0013 — G2 受控案例库与参考结果技术验收

**时间：** 2026-08-26 13:06:56 +08:00
**Gate：** G2 `IN_PROGRESS`（技术验收 `PASS`；版本检查点待完成）
**目标：** 冻结总纲规定的 8 个受控案例、独立操作台账、机器真值、清单/哈希/许可证、参考结果及无隐藏留出限制。

**已执行：**

- 建立人工声明的 `data/g2-operation-ledger.json`，固定 C01–C08 的构造操作、GUID 种子、预期状态、理由、2 mm 容差与公开验收集策略；
- 建立确定性生成器，生成 8 对共 16 个 IFC4 文件，全部采用米制单位、共同项目坐标、非个人 Header 与 CC0-1.0 标记；
- 建立 8 条机器可读标准答案，分布为 3 `CLASH`、4 `CLEAR`、1 `NOT_EVALUATED`；
- 建立 `data/dataset-manifest.json`，逐案登记来源、许可证、署名、Schema、相对路径、SHA-256、真值依据、再分发许可与限制；
- 建立 IfcOpenShell 参考验证器和 PowerShell 一键验收；
- 将历史 G1 生成器与生成文件的许可证状态同步为 D-018 决定，并保持 G1 技术复跑通过；
- 建立 MIT 代码许可证、CC0-1.0 生成数据声明、G2 数据/许可证据文档与脱敏 AI 辅助记录。

**验证结果：**

- `scripts/test-g2.ps1`：`PASS`；
- 8 个案例、16 个 IFC、16 个唯一产品 GUID、8 个唯一记录 ID：`PASS`；
- 连续两次生成的 IFC、真值与清单按字节 SHA-256 完全一致：`PASS`；
- 清单逐文件哈希、相对路径、许可证、再分发标记与限制完整：`PASS`；
- IfcOpenShell 参考最终分类与 8/8 状态、3/3 碰撞 GUID 对和类型一致：`PASS`；
- C03 touching、C04 低于容差、C06 真开洞均为 `CLEAR`；C08 缺失 MEP 几何明确为 `NOT_EVALUATED`；
- `scripts/test-g1.ps1` 回归：`PASS`；
- 未建立隐藏留出集；8 例明确为公开契约验收集，不用于声称通用准确率。

**失败和异常：**

- 参考脚本首次对普通 IFC 实体沿用碰撞包装器的 `get_argument(0)`，在 GUID 预检查阶段失败；改用实体 `GlobalId` 后恢复，未改变几何或真值；
- IfcOpenShell 原始表面相交将 C04 的 1 mm 侵入返回为 `CLASH`，其约 `0.199 m` 距离是截面交线而非侵入深度；未修改真值迎合输出，改由解析后世界坐标网格的最小 AABB 重叠 `0.0009999999999998899 m` 与 2 mm 阈值比较。该守卫只适用于受控套件，不声称是通用穿透距离算法；
- 首次确定性复跑发现 C06 的 `IfcRelVoidsElement` 由库生成随机 GUID；固定关系 GUID 后，完整字节复跑通过。

**隐私/许可证影响：**

- 项目代码许可证为 MIT，版权主体为非个人 `IFC ClashTrace contributors`；
- 本项目生成的 IFC 与配套真值数据为 CC0-1.0，允许再分发；第三方材料不适用该授权；
- 所有 IFC Header 均无邮箱、本地绝对路径和个人身份；未引入外部、客户、雇主、学校或真实项目数据；
- Design Gate 仍未通过，本阶段没有正式 UI、品牌、样式或产品结果页。

**当前结论：** G2 技术验收 `PASS`；Gate 仍为 `IN_PROGRESS`，等待公开候选审计、本地步骤提交与 Chrome GitHub 网页检查点。
**下一步：** 机械更新脱敏公开版总纲，执行 G2 专项审计，形成 `step(G2): freeze controlled dataset and ground truth` 本地提交。
**需要用户决定：** 无。
**对应本地提交：** 待形成。
**GitHub 检查点：** 待执行。

---

### L-0014 — G2 公开候选审计与提交范围冻结

**时间：** 2026-08-26 13:10:59 +08:00
**Gate：** G2 `IN_PROGRESS`（技术与公开候选审计 `PASS`；本地提交和 GitHub 检查点待完成）
**目标：** 对完整 G2 公开候选执行隐私、密钥、路径、大文件、许可证、数据范围、依赖与 Git 治理审计，并冻结单一步骤提交范围。

**验证结果：**

- `scripts/audit-g2.ps1`：`PASS`；
- 公开候选 46 个文件；绝对路径、邮箱和凭据命中均为 0；超过 1 MiB 文件为 0；
- MIT 代码许可证、CC0-1.0 数据声明、清单许可证与再分发字段全部通过；
- 16 个 G2 IFC 全部纳入公开候选，未纳入任何 `data/external/`、`data/stress/`、私有或第三方数据；
- 脱敏公开总纲与本地权威总纲除登记路径替换外内容一致；
- Git 远程数为 0，仓库级作者仍为非个人占位身份，忽略的运行时/依赖/本地输出被跟踪数为 0；
- npm 生产依赖审计：0 个已知漏洞；
- `git diff --check`：无空白错误；仅保留既有 Windows LF/CRLF 非阻塞提示。

**失败和异常：**

- 首次 G2 公开审计在公开测试脚本自身的路径检测表达式中命中 2 个敏感路径字面量；未降低扫描规则，将测试脚本改为运行时组合检测表达式后复扫为 0 命中。审计脚本本身继续作为本地证据，不属于公开候选。

**当前结论：** G2 技术和提交前公开审计均为 `PASS`；提交范围只包含 G2 数据、真值、生成/参考/测试代码、许可证、证据文档，以及 D-018 引起的 G1 生成数据许可证同步。
**下一步：** 显式暂存已审查范围，形成 `step(G2): freeze controlled dataset and ground truth` 本地提交，回查 SHA、工作树和远程数。
**需要用户决定：** 无。
**对应本地提交：** 待形成。
**GitHub 检查点：** 待本地提交通过后执行。

---

### L-0015 — G2 本地步骤提交与 GitHub 网页检查点验收

**时间：** 2026-08-26 13:19–13:35（Asia/Hong_Kong）
**Gate：** G2 `PASS`
**目标：** 将已验收的 G2 受控数据集、独立真值、参考比较、许可证和证据形成权威本地步骤提交，并仅通过已登录 Chrome GitHub 网页建立和回查公开检查点。

**完成：**

- 形成权威本地步骤提交 `step(G2): freeze controlled dataset and ground truth`，完整 SHA 为 `7da4adfcdaef7729ba52d2a2c98c8741fdcc9c01`；
- 提交后工作树洁净、Git 远程数仍为 `0`，G1 回归、G2 专项测试、确定性再生成、清单哈希、8/8 真值匹配、失败闭合、公开候选审计和依赖审计全部复验通过；
- 经用户明确确认后，仅通过已登录 Chrome GitHub 网页在 `main` 上建立连续 11 提交检查点；远程范围从 `d4b974f101348d5707418c4078965dea0d8d7fc2` 到 `4fcc3cd47197a10771f5ce52b2e0a039d6434dc1`，每个提交标题均含 `G2` 与本地短 SHA `7da4adf`；
- G2 转为 `PASS` 的公开总纲、公开同步台账与数据证据由本地同步提交 `c6765d54ffbc776cb82576867ae93846c851e118` 映射到连续网页证据尾链 `b19235fc35d7dc3d41f10d1c1b50f94bff387c43` → `1fb4dee9a2dda1396b3ddf8fdf6bbc21d575f7c4` → `822a86bb2ca49e02a34175fb74038ba62b4c0562`；
- 尾链登记修订由本地同步提交 `732cf5fc159da4d3b939b7d6c7587c183c5dfa0b` 映射到网页提交 `093af832e9c80863a663938531c3d65fe54a190a` → `044c598deedbdd8854367378db3fcc870d9aa0d1`，后者是本次一致性审计开始前的实际远程 HEAD；
- Chrome 回查确认仓库仍为 `Public`，许可证、根文档、脚本、清单、操作台账、机器真值、生成数据许可证、历史 G1 生成夹具和 16 个 G2 IFC 均存在；G2 目录恰含 C01–C08 的 MEP/结构成对文件；
- 对 GitHub 大文件代码页的按需渲染假阴性改用同页 `file content` 文本框完整值复核，确认清单末尾 `c08-structure.ifc` 与真值中的 `NOT_EVALUATED` 实际存在；README 渲染页确认 G2 复现、MIT 和 CC0-1.0 说明存在；
- 未使用 `gh`、GitHub API、命令行远程、`origin` 或任何本地 Git 远程配置，未进入 G3、Design Gate 或正式 UI/视觉设计。

**量化结果：**

- 受控案例：`8`；状态分布：`CLASH 3 / CLEAR 4 / NOT_EVALUATED 1`；
- 生成 IFC：`16`；确定性再生成：`PASS`；清单 SHA-256：`PASS`；
- 参考结果与真值：`8/8`；失败闭合：`PASS`；
- 公开候选：`46` 个文件；绝对路径、个人邮箱、凭证命中均为 `0`；大于 1 MiB 文件为 `0`；外部数据为 `0`；
- GitHub G2 技术/数据检查点：连续 `11` 个网页提交；PASS 与尾链登记证据：连续 `5` 个网页提交；一致性审计开始前最终远程 SHA `044c598deedbdd8854367378db3fcc870d9aa0d1`。

**当前结论：** G2 本地技术、治理、许可证、数据、提交与 GitHub 网页同步全部 `PASS`，Gate 可从 `IN_PROGRESS` 转为 `PASS`。
**下一步：** 停止在 G2 边界并等待用户下一指令；不得自动开始 G3。
**需要用户决定：** 无。
**对应本地提交：** `7da4adfcdaef7729ba52d2a2c98c8741fdcc9c01`。
**GitHub 检查点：** [`d4b974f101348d5707418c4078965dea0d8d7fc2`](https://github.com/delongwangshu49-hub/ifc-clashtrace/commit/d4b974f101348d5707418c4078965dea0d8d7fc2) → [`4fcc3cd47197a10771f5ce52b2e0a039d6434dc1`](https://github.com/delongwangshu49-hub/ifc-clashtrace/commit/4fcc3cd47197a10771f5ce52b2e0a039d6434dc1)，Chrome 回查 `PASS`。

---

### L-0016 — G2 全量审计与严格限定的一致性修复

**时间：** 2026-08-26 13:47–14:15（Asia/Hong_Kong）
**Gate：** G2 `PASS`（技术、数据与治理证据一致性修复均为 `PASS`）
**目标：** 只修复全量审计发现的状态、网页尾链映射和证据统计口径，不修改数据、生成器、参考算法、测试逻辑、许可证、产品规则或 Design Gate 边界。

**全量审计事实：**

- G1 回归、G2 专项测试、8/8 参考匹配、确定性再生成、清单哈希、失败闭合、46 文件公开候选审计、`pip check`、npm 生产依赖审计和 Git 对象完整性全部通过；
- 本地工作树洁净、HEAD 为 `62dcbbf5e31febdc470c275c8a6579413f1b1ef4`、Git 远程数为 `0`；GitHub 仓库仍为 `Public`，公开树包含全部 46 个预定文件；
- 发现 G2 Gate 正式状态栏仍为过期 `IN_PROGRESS`、最终两笔网页提交未进入同步台账、`PROMPTS.md` 少记录一次 G1 最终重载、公开候选最大文件统计仍停留在 G0A 数值；
- 发现测试契约断言和通用 AABB 容差逻辑存在后续强化空间，但它们不是证据一致性修复，明确不纳入本次变更。

**已批准修复范围：**

- 将 G2 Gate 正式状态栏统一为 `PASS`；
- 登记 `732cf5f` 对应的 `093af832…` → `044c598…` 网页尾链；
- 将 G1 Chrome 证据统一为初次运行及后续 4 次重载；
- 将审计前公开候选最大文件统计从过期值更新为 `84,445` 字节，并确认加入完整修复日志后的最大候选仍小于 `89,000` 字节；
- 明确同步台账自身发布造成的自引用尾链由下一笔本地空 `sync` 提交登记并终止，不递归改写公开台账。

**修复执行：** 本地提交 `3d43890902106e477799e00d19b290bc5e09077d` 只修改本地/公开总纲、同步台账和 `PROMPTS.md`；完整回归与审计通过后，仅通过 Chrome 形成连续网页提交 `1943544a685918c4d506dd76b3489aeb5b50d18b` → `92ee708f9dc4caddbc902d0f09878e08f631a6ef` → `f6fc9516b57092d38eefd3f93e868a68b1cfe873`，标题均含 `G2` 与本地短 SHA `3d43890`。

**当前结论：** 全量审计发现的 Gate 状态、网页尾链映射、重载次数和文件统计口径均已修复；未触碰明确排除的测试强化和算法范围，G2 可恢复为无保留 `PASS`。
**最终网页映射：** 本地同步提交 `d069ea5654ccf330d2e3702456ceddae4592d84a` 对应网页提交 `eb349918a79e734a494483e97a59edf574c169ef` → `5b86d20d71561c28cd0b5d1030079f57a23da26e`；Chrome 已回查 `Public`、`main`、修复标记和连续提交链，闭合登记提交为 `cfe9d2070aad75aa0a840460533952bb55d2187a`。

**下一步：** 停止在 G2 边界并等待用户下一指令；不得开始 G3。
**需要用户决定：** 无。
**对应本地提交：** `3d43890902106e477799e00d19b290bc5e09077d`。
**GitHub 修复检查点：** [`1943544a685918c4d506dd76b3489aeb5b50d18b`](https://github.com/delongwangshu49-hub/ifc-clashtrace/commit/1943544a685918c4d506dd76b3489aeb5b50d18b) → [`f6fc9516b57092d38eefd3f93e868a68b1cfe873`](https://github.com/delongwangshu49-hub/ifc-clashtrace/commit/f6fc9516b57092d38eefd3f93e868a68b1cfe873)，最终映射发布与 Chrome 回查 `PASS`。

---

### L-0017 — G3A/G3B 前置治理计划登记

**时间：** 2026-08-26 14:29（Asia/Hong_Kong）
**Gate：** G2 `PASS`；G3A/G3B `NOT_STARTED`；G3 `BLOCKED`（G3A/G3B 前置条件）
**目标：** 将全量审计保留的测试契约缺口和 AABB 通用化风险变为有产出、有负向测试、有停止门的正式项目步骤。

**用户决定：** 用户明确要求把两个问题纳入项目计划。

**计划变更：**

- 新增 D-019、O-010、R-13 与 G3A，要求隔离生成、路径—哈希冻结基线、Git 前后不变断言、关键顶层契约断言和突变负向测试；
- 新增 D-020、O-011、R-14 与 G3B，将现有 AABB 守卫限定为 C04 受控证据，并要求薄构件、旋转/斜交与阈值三点案例验证一般容差语义；
- 将 G3 状态设为 `BLOCKED`（G3A/G3B 前置条件），两个子 Gate 未通过时不得验收核心引擎、不得进入 Design Gate；
- 本次只更新治理计划，不修改任何数据、生成器、参考算法、测试实现或产品代码。

**当前结论：** 两个问题已进入正式计划与停止门；计划文件本地审计、提交和 Chrome GitHub 网页同步均为 `PASS`。
**下一步：** 停止在计划登记边界并等待用户下一指令；不得自动开始 G3A/G3B 技术实现。
**需要用户决定：** 无。
**对应本地提交：** `9f365c85e34fbdc396708b30d763477b354d6213`（`step(G3): register contract and tolerance preflight`）。
**GitHub 计划检查点：** 公开计划 [`45b84541c64625494c8a61edde827415cb567fa7`](https://github.com/delongwangshu49-hub/ifc-clashtrace/commit/45b84541c64625494c8a61edde827415cb567fa7) → 公开台账 [`ab8472017f5ce34f937f6c9af40dc4f3342d5668`](https://github.com/delongwangshu49-hub/ifc-clashtrace/commit/ab8472017f5ce34f937f6c9af40dc4f3342d5668)，Chrome 回查 `Public`、`main`、G3A/G3B/D-019/D-020/O-010/O-011/R-13/R-14/L-0017 与尺寸审计口径均为 `PASS`。

---

### L-0018 — 功能扩展与视频工作流总纲修订

**时间：** 2026-08-26（Asia/Hong_Kong）

**Gate：** G2 `PASS`；G3A/G3B/G3C `NOT_STARTED`；G3 `BLOCKED`（三个前置条件）

**目标：** 利用 G2 提前完成形成的时间余量，将 50 mm 净距预警、可选 AI 结果解读和无用户出镜/原声的视频制作流程纳入正式范围与倒排计划。

**用户决定：**

- 原候选项“管线与结构构件小于 50 mm 的净距预警”升级为开发阶段必做内容；
- 必须引入至少一个经实施当日核验、具有可用免费额度的 API，实现可由用户主动触发的结果自然语言解读；
- 视频采用 Codex 辅助录制/生成与 AI 旁白，不包含用户本人出镜或原声，并细化脚本、风格、画面、配音、音乐、字幕和 QA 排期。

**计划变更：**

- 新增 D-021、O-012、R-15 与 G3C；保持 G2 冻结真值不变，另建 `0/49/50/51 mm`、旋转/斜交、开洞、去重和失败关闭净距夹具；
- 新增 D-022、R-16 与 G4AI，将 AI 定位为“必做但运行时可选”的解释层，明确用户主动触发、数据最小化、密钥隔离、提供商核验、模拟测试和确定性降级；
- 新增 D-023、O-013、R-17 与 VG，并将视频工作拆分为预制作、画面/音频制作、后期与发布 QA；
- 更新成功标准、核心范围、技术路线、自动测试、各 Gate、风险、隐私清单、仓库结构、倒排日程、时间预算、视频章节和脱敏 AI 辅助记录；
- 本次只修订治理与执行计划，不开始 G3A/G3B/G3C，不选择具体 API 提供商，不创建前端、AI 集成或视频产物。

**验证结果：** 本地权威总纲与脱敏公开版除登记路径替换外逐字一致；Markdown diff 检查与 G2 公开候选专项审计通过；G2 代码、数据、真值和已完成历史不作修改。

**隐私/许可证影响：** 新增 API 密钥、最小化出站数据、提供商条款、AI 旁白、音乐/素材许可证和逐帧隐私审计要求。

**当前结论：** 0.3.0 扩展范围已明确；G3 现在同时受 G3A、G3B、G3C 阻断，AI 与视频均有独立验收和停止门。

**下一步：** 等待用户指令；后续按修订后的倒排顺序从 G3A 开始执行。

**需要用户决定：** 当前无；具体 API 提供商在 G4AI 以当日官方资料决策，视频样例在 VG 提交用户确认。

**对应本地提交：** `55ed3a2f07d525e9c14f182bc2213fe2ef36694c`（`step(L-0018): expand clearance AI and video plan`）；本地计划登记闭合提交 `c1c5e66ba8ca1253947edab6aa5da3f2df08eca3`。

**GitHub 计划检查点：** 0.3.0 公开计划内容并入下一次 G3A 阶段网页检查点核验，不另行配置远程或使用命令行补传。

---

### L-0019 — G3A 契约测试加固技术验收

**时间：** 2026-08-26 15:58（Asia/Hong_Kong）

**Gate：** G3A `PASS`；G3B/G3C `NOT_STARTED`；G3 `BLOCKED`

**目标：** 关闭测试原位再生成可能掩盖已提交基线漂移的风险，建立独立、精确、可突变验证的 G3 验收基线。

**步骤开始核验：**

- PowerShell `7.6.4`，项目根目录与唯一 Git 根目录一致；
- HEAD 为 `c1c5e66ba8ca1253947edab6aa5da3f2df08eca3`，`main` 工作树洁净，Git 远程数为 `0`；
- 项目内无额外 `AGENTS.md`；
- 既有 G2 参考验证为 `PASS`、案例 `8/8`、碰撞构件对/类型一致，G2 公开候选审计为 `PASS`；
- 启动前 16 个 G2 IFC 的仓库相对路径—SHA-256 映射与已冻结清单一致。

**已执行：**

- 为 G1/G2 生成器增加隔离输出根；默认输出位于已忽略的 `outputs/local-only/`，对仓库根写入必须显式给出 `--allow-baseline-write`，测试从不使用该选项；
- 新增独立的 `g2-frozen-baseline.json`，冻结批准的顶层契约、C01–C08 状态及 16 个 IFC 的精确仓库相对路径—SHA-256 映射；
- 新增契约验证器，对 `rule_id=MEP_STRUCTURE_HARD_CLASH_V1`、`schema=IFC4`、`length_unit=metre`、`coordinate_system=shared_project_coordinates`、`tolerance_m=0.002`、案例/状态/角色/路径/哈希及实际 IFC Schema/米制单位执行精确断言；
- 将 G1/G2 测试改为两个隔离生成根并与冻结基线比较，运行前后同时断言受保护路径哈希和 Git 工作树状态不变；
- 新增 8 类契约突变负向测试，分别覆盖规则 ID、Schema、单位、坐标系、容差、状态、案例路径和文件哈希；
- 新增 G3A 聚合复跑、公开证据文档和专项发布审计；未修改任何已提交 G1/G2 IFC 字节、G2 机器真值或参考容差算法。

**验证结果：**

- `scripts/test-g3a.ps1`：`PASS`；
- G1 IfcOpenShell 与 web-ifc/BVH 双路线：`PASS`，固定 GUID 对一致；
- G2 案例/状态：`8`，`CLASH 3 / CLEAR 4 / NOT_EVALUATED 1`；参考结果与真值 `8/8`；失败闭合 `PASS`；
- 两次隔离生成均与 16 条冻结路径—SHA-256 映射完全一致；受保护基线哈希与 Git 工作树状态前后不变；
- 8/8 契约突变均以预期的字段级诊断失败；
- Python 语法检查与 `pip check` 通过；没有依赖或许可证变化。

**失败和异常：** 无阻塞性失败；首次实现复跑即通过，未发生基线回写、真值修正或失败后的 GitHub 重试。网页回查中一次只读定位因同名链接产生严格模式歧义、一次本地只读 JavaScript 匹配表达式有语法错误，修正定位/表达式后通过，均未产生远端副作用。

**隐私/许可证影响：** 新增内容只引用仓库相对路径和程序生成数据；公开候选不含本地绝对路径、个人邮箱、凭据或外部/私人 IFC。代码继续使用 MIT，冻结基线继续描述 CC0-1.0 的项目生成数据。

**当前结论：** G3A `PASS`。本地实现提交、专项审计、Chrome GitHub 网页上传、网页回查与路径—SHA/提交链映射均已通过；G3 仍由尚未开始的 G3B/G3C 阻断。

**下一步：** 明确停止并等待用户决定是否继续 G3B；不得自动开始 G3B、G3C、G3 或正式 UI。

**需要用户决定：** 是否开始 G3B 需用户决定；本次 G3A 完成记录本身无需额外决定。

**对应本地提交：** `d0ad55528c5e95148c3facfcc924543f23028ef4`（`step(G3A): harden frozen contract tests`）。

**GitHub 技术检查点：** 根文档 [`c12c654a2842aab2f7dc84bf7c1b85e1e2101f53`](https://github.com/delongwangshu49-hub/ifc-clashtrace/commit/c12c654a2842aab2f7dc84bf7c1b85e1e2101f53) → 契约测试脚本 [`2c980a8e5542de50349d3406fb3d59d5eb24bce2`](https://github.com/delongwangshu49-hub/ifc-clashtrace/commit/2c980a8e5542de50349d3406fb3d59d5eb24bce2) → 证据文档 [`fc0a7c28580121a30d1694aef7a18a665c5ec924`](https://github.com/delongwangshu49-hub/ifc-clashtrace/commit/fc0a7c28580121a30d1694aef7a18a665c5ec924) → 冻结基线 [`c0b8ff3eed8b1f7394a164b9f9a825544f83d618`](https://github.com/delongwangshu49-hub/ifc-clashtrace/commit/c0b8ff3eed8b1f7394a164b9f9a825544f83d618)，2026-08-26 16:06–16:10 +08:00；Chrome 已核验 `Public/main`、连续父提交、关键契约、16 条唯一路径—SHA、8 案例状态、公开脚本集合及 `audit-g3a.ps1` 未公开。PASS 状态与台账映射通过后续同步尾部发布，并按自引用闭合规则在本地空提交中登记其最终 SHA。

---

### L-0020 — G3A 审计修复：一次性隔离目录与文档一致性

**时间：** 2026-08-26 22:59（Asia/Hong_Kong）

**Gate：** G3A `PASS`；G3A-R1 `PASS`；G3B/G3C `NOT_STARTED`；G3 `BLOCKED`

**目标：** 按用户批准的严格范围关闭固定 `run-1/run-2` 目录可能保留旧输出的防御缺口，并修正 G3A 完成记录中的两处状态措辞矛盾。

**步骤开始核验：** PowerShell `7.6.4`；HEAD `7200a74ec72938aed7799919f6b0b128fc627d8f`；`main` 工作树洁净；Git 远程数 `0`。

**已执行：**

- G1/G2 测试每次在 `outputs/local-only/g3a-tests/<gate>/` 下创建 GUID 命名的唯一根，两个确定性运行只位于该根内部；
- 创建和递归清理前均使用规范化绝对路径及批准父目录前缀执行失败关闭校验；精确临时根在 `finally` 中删除，测试结束后再次断言不存在；
- 精确删除旧实现遗留的四个可再生成 `run-1/run-2` 忽略目录；未删除任何已跟踪文件；
- 将 L-0019 的“等待用户决定是否继续 G3B”与“需要用户决定：无”修正为一致表述；证据文档明确 G3A 已不再阻断 G3，剩余阻断项为 G3B/G3C。

**验证结果：**

- G1/G2 新增 `ISOLATED_TEMP_ROOT_CLEANUP=PASS`；全套运行后两个隔离父目录的子项合计为 `0`；
- `scripts/test-g3a.ps1`、`scripts/audit-g3a.ps1` 与 `scripts/audit-g2.ps1` 全部 `PASS`；
- 16 个 IFC 路径—SHA、G2 真值 `8/8`、失败闭合、8/8 契约突变、基线写入守卫 `2/2`、受保护哈希与 Git 工作树不变性全部保持通过；
- 公开候选绝对路径、邮箱、凭据与超限文件命中均为 `0`；Git 远程数保持 `0`；公开计划等价性通过。

**范围与影响：** 只修改 G1/G2 测试临时目录生命周期及 G3A 证据/治理文档；未修改生成器、冻结 IFC、真值、规则语义、依赖、许可证、G3B/G3C/G3 或 UI。

**当前结论：** G3A-R1 `PASS`；本地修复提交、Chrome GitHub 网页上传、网页回查与映射登记均已完成，固定隔离目录复用风险与两处文档矛盾已关闭。

**下一步：** 明确停止并等待用户决定是否开始 G3B；不得自动开始 G3B、G3C、G3 或正式 UI。

**需要用户决定：** 是否开始 G3B 需用户决定；无其他待决事项。

**对应本地提交：** `839365a7693fa389c5e54d45a068f6a71a591002`（`fix(G3A): make isolated test roots ephemeral`）。

**GitHub 修复检查点：** 测试脚本 [`7b8c5cdc02e804d58f34dd1c6c2cbd3699c1b2cd`](https://github.com/delongwangshu49-hub/ifc-clashtrace/commit/7b8c5cdc02e804d58f34dd1c6c2cbd3699c1b2cd) → 证据文档 [`378b3647ff8b6c105e9df2c894a13f2ff8fcbdcb`](https://github.com/delongwangshu49-hub/ifc-clashtrace/commit/378b3647ff8b6c105e9df2c894a13f2ff8fcbdcb) → 公开治理记录 [`9dc218a9075062dffe719d3563d1063abeb95835`](https://github.com/delongwangshu49-hub/ifc-clashtrace/commit/9dc218a9075062dffe719d3563d1063abeb95835)，2026-08-26 23:05–23:08 +08:00；Chrome 已核验唯一 GUID 根、`finally` 路径守卫与清理标记、修正文档、`Public/main` 及连续父提交。最终 PASS/台账映射通过后续同步尾部发布并按自引用闭合规则登记。

---

### L-0021 — G3B 容差语义与 AABB 守卫隔离技术验收

**时间：** 2026-08-26（Asia/Hong_Kong）

**Gate：** G3B `PASS`

**目标：** 保留 C04 的受控参考证据，将其世界轴 AABB 守卫从一般分类路径中物理隔离，并为 `2 mm` 容差建立旋转不变、可审计且失败关闭的几何语义。

**步骤开始核验：**

- PowerShell `7.6.4`；唯一项目/Git 根目录一致；
- `main` HEAD 为 `fdff93bc2bd9d0664a5496da80fae3d9cddb962f`，工作树洁净，Git 远程数为 `0`，Git 对象检查通过；
- G0A、G1、G2、G3 预检、G3A、G3A-R1 与最终 G3A 同步提交均存在且为当前 HEAD 祖先；
- G1、G2、G3A/G3A-R1 回归和 G1/G2/G3A 当前发布审计通过；历史 `audit-g0a.ps1` 因其按 G0A 空基线扫描整个成熟工作区并计入已忽略本地运行时大文件而返回 `FAIL`，不代表当前公开候选失败；G0A 提交树、三文件脱敏候选和零远程证据另行核验。

**已执行：**

- 将 `scripts/g2-reference-clash.py` 的世界轴 AABB 守卫改为只有显式 `case_id == C04` 时才可执行，并为每案输出 `aabb_guard_applied` 与 `classification_path`；任何非 C04 案例都不得获得 AABB 重叠值或继承该分类；
- 定义 `STRUCTURE_EROSION_INTERIOR_DEPTH_V1`：管段实体只有在结构实体向内侵蚀 `0.002 m` 后仍有交集，亦即存在结构内部欧氏表面深度严格大于 `0.002 m` 的管段实体点时才为 `CLASH`；恰好等于阈值为 `CLEAR`；
- 建立 12 个独立 G3B 解析证明夹具，覆盖 touching、`1.9/2.0/2.1 mm`、`3.0/4.0/4.2 mm` 薄结构、整体旋转、斜交和三类失败关闭；
- 实现封闭凸 OBB 与有限圆柱的受控解析证书：中心穿越使用结构内切半径，面法向中心布置使用有限轴区间；无闭合内部、坐标未核验或超出证书族时返回 `NOT_EVALUATED`；
- 增加 G3B 聚合测试、公开语义文档、README/PROMPTS 记录与专项发布审计；未开始 G3C、G3 浏览器引擎、正式 UI、AI 接入或视频工作。

**验证结果：**

- `scripts/test-g3b.ps1`：`PASS`；12 案分布为 `CLASH 3 / CLEAR 6 / NOT_EVALUATED 3`；
- `1.9 mm` 与 `2.0 mm` 为 `CLEAR`，`2.1 mm` 为 `CLASH`；`3.0/4.0/4.2 mm` 薄结构的最大内部深度分别为 `1.5/2.0/2.1 mm`，状态为 `CLEAR/CLEAR/CLASH`；
- 旋转薄构件的可证内部深度为 `0.0015 m`，而最小世界轴 AABB 重叠约为 `0.36544 m`，明确证明 AABB 不能充当一般深度；旋转斜交中心穿越深度为 `0.006 m` 并判为 `CLASH`；
- C04 原始表面状态继续为 `CLASH`、受控守卫后为 `CLEAR`；C04 是唯一 `aabb_guard_applied=true` 的 G2 案例，其余 7 案 AABB 守卫与 AABB 重叠值均为空；
- 三类不可靠/不受支持输入全部为 `NOT_EVALUATED` 且包含诊断；G1/G2/G3A/G3A-R1、16 IFC 路径—SHA、8/8 真值、8 类突变、隔离根清理、失败闭合和 Git 状态不变性继续通过。

**边界与影响：** 解析证明只覆盖已登记的封闭凸 OBB/有限圆柱证书族，是 G3B 参考证据而非浏览器产品引擎。一般网格若不能建立闭合最终实体、共享坐标与可靠点到表面/体内证书，必须 `NOT_EVALUATED`。浏览器 `penetration_distance_m` 继续保持 `null`；AABB 未来最多用于保守 broad phase，不得输出碰撞、深度或一般 `CLEAR`。

**隐私/许可证：** 新增代码沿用 MIT；新增程序化/机器可读证明夹具登记为 CC0-1.0；未引入外部或私人 IFC、依赖、密钥、个人身份、本地路径公开内容或 UI 资产。

**当前结论：** G3B `PASS`。专项测试与发布审计、本地步骤提交、Chrome GitHub 四提交连续技术链、网页回查和本地—远程映射登记均已通过；一般 `2 mm` 容差语义已证明，C04 AABB 守卫已物理隔离，可靠性不足时失败关闭。

**下一步：** 明确停止，等待用户决定是否开始 G3C；不得自动开始 G3C、G3 浏览器核心引擎或正式 UI。

**需要用户决定：** 是否开始 G3C。

**对应本地提交：** `step(G3B): define auditable tolerance semantics` / `a443a0d875a2d46a7af3e8589d571fd42375ee17`。

**GitHub 检查点：** 2026-08-26 23:54–23:56 +08:00，通过用户已登录 Chrome 网页形成并回查连续公开技术链：根文档 [`983c6e0b3114a8e4356ea2cf204954d4c727546e`](https://github.com/delongwangshu49-hub/ifc-clashtrace/commit/983c6e0b3114a8e4356ea2cf204954d4c727546e) → 测试与证明脚本 [`e586659a766a106bbc90e2aaf6d247fce0ac64e9`](https://github.com/delongwangshu49-hub/ifc-clashtrace/commit/e586659a766a106bbc90e2aaf6d247fce0ac64e9) → 12 案夹具 [`3f1c0855596d78b4f8617a155d0b59211188852c`](https://github.com/delongwangshu49-hub/ifc-clashtrace/commit/3f1c0855596d78b4f8617a155d0b59211188852c) → 专项语义证据 [`54c1bab44042573a4d7e719ffc67b7cc3373121f`](https://github.com/delongwangshu49-hub/ifc-clashtrace/commit/54c1bab44042573a4d7e719ffc67b7cc3373121f)。四提交标题均含 G3B 与本地短 SHA `a443a0d`；父链、`Public`/`main`、C04-only 守卫、阈值/薄构件/旋转斜交/失败关闭标记和公开内容均为 `PASS`。

> 后续审计说明：L-0021 与上述网页链是当时已完成的原始 G3B 检查点。2026-08-27 的独立审计发现其薄构件与极近阈值证据存在逻辑缺口，现由 L-0022/G3B-R1 取代其相关结论；原始提交与记录保留为可追溯历史。

---

### L-0022 — G3B-R1 审计修复：退化内蚀核心、严格阈值与发布扫描

**时间：** 2026-08-27（Asia/Hong_Kong）

**Gate：** G3B-R1 `PASS`；G3C `NOT_STARTED`；G3 `BLOCKED`

**目标：** 修复独立审计发现的薄构件假阴性、严格 `>` 阈值被浮点死区吞没、跨阶段审计候选污染和 Windows 正斜杠绝对路径漏检，同时保持 C04-only AABB 边界及全部既有回归。

**审计发现：**

- 原 `STRUCTURE_EROSION_INTERIOR_DEPTH_V1` 在结构厚度不超过 `2t` 时会把完整贯穿判为 `CLEAR`，不足以安全区分容差内侵入与薄构件穿透；
- 原分类使用 `math.isclose(..., abs_tol=1e-12)` 抵消严格 `>`，可将数学上略高于 `0.002 m` 的值判为 `CLEAR`；
- `audit-g3a.ps1` 静态列举本地审计脚本，新增 `audit-g3b.ps1` 后会把其检测表达式计入公开候选并失败；
- 绝对路径正则只覆盖 Windows 反斜杠，漏检“盘符加正斜杠”形式；
- G3B10/G3B11 只消费夹具预置的可靠性信号，不构成拓扑或坐标验证实现；G3B12 的 unsupported 证书未被专项断言。

**已执行：**

- 保留结构内蚀/内部深度语义，但当确有体积相交且结构内蚀核心为空或退化时返回 `NOT_EVALUATED` 与明确诊断，不再输出不安全的 `CLEAR`；
- 对人工解析证书使用十进制精确深度和严格 `>` 比较，单独验证世界/局部变换往返误差；新增仅比阈值高 `0.5 nm` 的 G3B13 回归；
- 将夹具升级为 13 案，分布为 `CLASH 4 / CLEAR 3 / NOT_EVALUATED 6`；G3B05/G3B06/G3B08 因空/退化内蚀核心失败关闭，G3B07 继续为 `CLASH`；
- 显式记录可靠性信号来源，断言 G3B10/G3B11 为 caller-supplied fixture precondition，G3B12 为 unsupported analytic certificate；
- G3A/G3B 发布审计改为按 `scripts/audit-*.ps1` 模式排除所有当前与未来本地审计脚本；G1/G2/G3A/G3B 的路径扫描统一覆盖 Windows 两种斜杠、Unix 与 UNC，并加入必须识别路径且不得误报 HTTPS URL 的自测。

**本地验证：**

- `scripts/test-g3b.ps1`：`PASS`；严格阈值亚 epsilon 回归、薄构件失败关闭、旋转 AABB 反例、斜交证书和 C04-only 守卫全部通过；
- G1/G2/G3A/G3A-R1 聚合回归在 G3B 套件内通过；
- G1/G2/G3A/G3B 四阶段发布审计全部通过：绝对路径、个人邮箱、凭据和超限文件命中均为 `0`，路径扫描自测均为 `TRUE`；G3A 不再因后续审计脚本污染候选；
- 独立对抗性探针证明数学上高于阈值 `1e-19 m` 的解析深度为 `CLASH`、低于阈值 `1e-19 m` 为 `CLEAR`、3 mm 全贯穿为 `NOT_EVALUATED`，未来 `audit-g3c.ps1` 会被排除而非审计脚本仍保留；
- `pip check`、Python 语法、`git fsck --full`、`git diff --check`、Git 远程数 `0` 与公开计划等价性全部通过；换行转换提示为既有非阻塞 Windows 工作副本行为。

**范围与影响：** 未修改 G2 冻结 IFC、G2 真值、C04 受控守卫、浏览器产品引擎或 `penetration_distance_m`；未开始 G3C、G3、Design Gate、UI、AI 或视频工作。G3B10/G3B11 仅证明下游失败关闭响应，不声称检测拓扑或完成坐标配准。

**网页验证：** 只通过用户已登录 Chrome 网页形成连续技术链 `af2cbcf98275b846dd5de38489fa9c5b7706df3f` → `b92fed1f33189682d9d58c9b92c94115a28333d2` → `a09b71ab578fd365d136b1ae4ad88edf416ca063` → `ac3f5069e16a3a36816748714373649693899827`。四笔标题均含 `G3B-R1` 与本地短 SHA `03b6e67`；父链从既有 G3B 最终公开提交 `2ff49d2e361535eb572284ae354d53db7d5ec2cc` 连续延伸。网页回查确认 `Public/main`、7 个预期公开文件、13 案夹具、G3B13 精确值、退化核心失败关闭、caller-supplied 可靠性边界、unsupported 证书断言、无 `math.isclose` 分类死区、C04-only AABB 与 G3C blocker 均存在。

**当前结论：** G3B-R1 本地技术、回归、对抗性探针、发布审计、权威提交、Chrome 技术链、网页内容回查和本地—远程映射全部通过，Gate 恢复为无保留 `PASS`。

**下一步：** 明确停止并等待用户决定是否开始 G3C；不得自动开始 G3C、G3 浏览器核心引擎或正式 UI。

**需要用户决定：** 当前无；G3B-R1 闭合后是否开始 G3C 需用户决定。

**对应本地提交：** `03b6e67b7ae19a758021f29ba779c83f5e5efffb`（`fix(G3B): harden tolerance and audit evidence`）。

**GitHub 修复检查点：** 根证据 [`af2cbcf98275b846dd5de38489fa9c5b7706df3f`](https://github.com/delongwangshu49-hub/ifc-clashtrace/commit/af2cbcf98275b846dd5de38489fa9c5b7706df3f) → 精确阈值脚本/测试 [`b92fed1f33189682d9d58c9b92c94115a28333d2`](https://github.com/delongwangshu49-hub/ifc-clashtrace/commit/b92fed1f33189682d9d58c9b92c94115a28333d2) → 13 案夹具 [`a09b71ab578fd365d136b1ae4ad88edf416ca063`](https://github.com/delongwangshu49-hub/ifc-clashtrace/commit/a09b71ab578fd365d136b1ae4ad88edf416ca063) → 语义证据 [`ac3f5069e16a3a36816748714373649693899827`](https://github.com/delongwangshu49-hub/ifc-clashtrace/commit/ac3f5069e16a3a36816748714373649693899827)，Chrome 回查 `PASS`；最终 PASS/台账发布按自引用闭合规则另行登记。

---

### L-0023 — G3C 50 mm 净距规则技术验收

**时间：** 2026-08-27 12:41（Asia/Hong_Kong）

**Gate：** G3C `PASS`；G3 `NOT_STARTED`（前置 Gate 已满足，等待用户决定）

**目标：** 在不改变 G2 冻结硬碰撞真值、不进入 G3 浏览器产品引擎的前提下，实现 `IfcPipeSegment` 对 `IfcWall`/`IfcBeam` 的确定性 50 mm 表面净距规则、独立补充验收集、硬碰撞去重与失败关闭记录。

**步骤开始核验：**

- PowerShell `7.6.4`；唯一项目/Git 根目录一致；
- `main` HEAD 为 `408e5ef3f54f58a7e3a168bbbddbe204af11df7b`，工作树洁净，Git 远程数为 `0`，`git fsck --full` 通过；
- G0A、G1、G2、G3A、G3A-R1、G3B、G3B-R1、最终 PASS 映射与空闭合提交均存在且为 HEAD 祖先；
- G3B 聚合回归及 G1/G2/G3A/G3B 四阶段当前发布审计全部通过。

**已执行：**

- 建立 9 案人工构造台账、逐案稳定 GUID 几何工件和冻结路径—SHA-256 基线，覆盖 touching、`49/50/51 mm`、硬碰撞去重、旋转斜交、真实开洞邻近、缺失几何和坐标未核验；
- 实现 `G3C_CONTROLLED_ANALYTIC_SURFACE_DISTANCE_V1`，在验证世界/局部变换后，以 Decimal 精确计算有限圆柱到封闭 OBB 表面或矩形开洞边缘的最小表面净距；严格 `< 0.05 m` 为 `WARNING`，等于阈值为 `CLEAR`；
- 实现独立 `three-mesh-bvh 0.9.14` 三角表面距离参考；其 `1e-7 m` 仅为 Float32/圆柱离散对照容差，不进入权威阈值分类；
- 输出包含双方角色、类型、稳定 GUID、名称、实测/精确净距、阈值、单位、证书、工件相对路径与 SHA、算法边界和失败诊断的 Clearance Warning Records；已为 `CLASH` 的构件对不生成重复净距记录；
- 增加唯一隔离根双重生成、基线写入守卫、逐路径哈希、记录契约、独立参考、全阶段回归与清理断言，并建立 G3C 专项发布审计和公开语义/许可证证据；
- 未修改 G2 IFC、G2 真值或 G3B 容差语义；未开始 G3、Design Gate、正式 UI、AI、部署或视频工作。

**验证结果：**

- `scripts/test-g3c.ps1`：`PASS`；9 案、8 条输出记录，分布为 `WARNING 4 / CLEAR 2 / NOT_EVALUATED 2 / SUPPRESSED 1`；
- `0 mm` touching 与 `49 mm` 为 `WARNING`，恰好 `50 mm` 与 `51 mm` 为 `CLEAR`；旋转斜交 `30 mm` 和开洞邻近 `40 mm` 为 `WARNING`；
- 硬碰撞对由解析和独立网格交集证据共同确认后抑制净距记录；缺失几何和坐标未核验均以 `NOT_EVALUATED`、空净距和明确诊断失败关闭；
- 解析结果 `9/9`、独立三角网格结果 `9/9`、路径—SHA `9/9`、规则 ID/阈值/工件哈希/预期状态突变拒绝 `4/4`、两次隔离字节确定性生成、一次性目录清理与工作树不变性全部通过；
- G1/G2/G3A/G3A-R1/G3B/G3B-R1 全量回归通过；`scripts/audit-g3c.ps1` 对 71 个公开候选审计通过，绝对路径、邮箱、凭据与超限文件命中均为 `0`，Git 远程数为 `0`。

**失败和异常：** 首个合并 Git 祖先检查因 PowerShell 表达式语法错误未执行且未产生变更，拆开语句后复验通过；独立网格参考首轮暴露 Three.js 旋转约定差异与 Float32/圆柱离散误差，改为与生成器一致的显式 `Rz·Ry·Rx` 矩阵并把网格对照容差限定为 `1e-7 m` 后通过，权威 Decimal 阈值保持严格且无死区；一次只读系统进程枚举被操作系统拒绝访问，不影响项目进程或验收。

**隐私与许可证：** 新增代码沿用 MIT；程序生成 JSON 几何工件、台账与真值基线为 CC0-1.0；公开候选不含本地绝对路径、个人信息、凭据、外部/私人模型或本地运行输出。

**网页验证：** 仅通过用户已登录 Chrome 网页形成连续技术链 `121d4c109b7518d5ab7b09f14aba95999d952927` → `413e09b5f7c57fd462cef0be6384c3305fc8e815` → `8de2097e5c7d81a709dc48cb5cf351d7560d9660` → `29454ae46d5e7069549dcf88b71270a52246e3c7` → `9a707582ec0a489bb1a0bfeb33b8dbd16eff65d1` → `e73edc168a17fb2a1d7786750c8831168c24bc6f` → `3113f47ac427eee82c40bced12ffb9b0a686c517`。七笔标题均含 `G3C` 与本地短 SHA `8cf588f`，并由既有证据尾 `4f9ecb6592570a722ea080a3fd3ad7aafd1b0ee8` 连续延伸；逐提交回查确认 `Public/main`、23 个预期公开文件与 `5/1/1/9/1/2/4` 文件分布。

**当前结论：** G3C `PASS`。本地技术验收、发布审计、权威步骤提交、Chrome GitHub 连续技术链、逐提交网页回查与本地—公开 SHA 映射均已通过；G3 前置 Gate 已满足，但未开始任何 G3 或正式 UI 工作。

**下一步：** 严格停止在 G3C，等待用户决定是否开始 G3；不得自动开始 G3、Design Gate 或正式 UI。

**需要用户决定：** 是否开始 G3。

**对应本地提交：** `8cf588f77812caa672f1491e838d386cfb3c50e7`（`step(G3C): validate deterministic clearance warnings`）。

**GitHub 检查点：** 技术链 `121d4c109b7518d5ab7b09f14aba95999d952927` → `413e09b5f7c57fd462cef0be6384c3305fc8e815` → `8de2097e5c7d81a709dc48cb5cf351d7560d9660` → `29454ae46d5e7069549dcf88b71270a52246e3c7` → `9a707582ec0a489bb1a0bfeb33b8dbd16eff65d1` → `e73edc168a17fb2a1d7786750c8831168c24bc6f` → `3113f47ac427eee82c40bced12ffb9b0a686c517`；最终 PASS 映射发布按账本自引用闭合规则登记。

---

### L-0024 — G3C-R1 上游状态与生成路径审计修复

**时间：** 2026-08-27 13:52（Asia/Hong_Kong）

**Gate：** G3C-R1 `PASS`；G3 `NOT_STARTED`（前置 Gate 已满足，等待用户决定）

**目标：** 仅关闭 G3C 完成后审计发现的两个边界：独立网格参考对未知上游硬碰撞状态未失败关闭，以及生成器未显式验证 case ID 和解析后工件路径 containment。不得改变冻结九案例、阈值、距离证书或开始 G3/UI。

**已执行：**

- 独立 `three-mesh-bvh` 参考现在只允许上游 `CLEAR` 进入净距计算，保留 `CLASH` 抑制路径，其他状态统一以 `NOT_EVALUATED`、空距离和诊断失败关闭；
- 生成器新增严格 `G3C` 加两位数字 case ID 校验、重复 ID 拒绝、目标路径预解析和输出根 containment 守卫；在任何工件写入前完成全体路径计划；
- 新增未知上游状态联合探针与 `../` 路径穿越探针，前者要求解析和网格两条路径同时失败关闭，后者要求生成器非零退出且不产生逃逸文件；
- 未修改 9 个生成工件、冻结基线、操作台账、50 mm 比较、解析证书、依赖或许可证；未开始 G3、Design Gate、UI、AI、部署或视频工作。

**验证结果：**

- `scripts/test-g3c.ps1`：`PASS`；正式九案例分布保持 `WARNING 4 / CLEAR 2 / NOT_EVALUATED 2 / SUPPRESSED 1`；
- 契约/对抗突变拒绝由 `4/4` 增至 `6/6`；未知上游状态在两条路径均为 `NOT_EVALUATED`，路径穿越无逃逸工件；
- 两次隔离生成与冻结 9/9 路径—SHA 映射保持不变；G1/G2/G3A/G3A-R1/G3B/G3B-R1/G3C 全量回归通过；
- G3C 发布审计通过，公开候选无本地绝对路径、邮箱、凭据或超限文件，Git 远程数为 `0`。

**网页验证：** 仅通过用户已登录 Chrome 网页形成连续技术链 `6c931eff44ce156824b2f727106a810fe6092e9b` → `6ea3311c407671bc059755c857ab046d0312ee9f` → `1089ae442e8625f6e4838639ac49705d29493d7a`，由既有 G3C 最终证据尾 `f0268f92619d52ef23ce7f9ba3bdd6fd4dd5df25` 连续延伸。三笔标题均含 `G3C-R1` 与本地短 SHA `a32cd49`；逐提交回查确认 `Public/main`、7 个预期公开文件与 `3/1/3` 文件分布。

**当前结论：** G3C-R1 `PASS`。两个审计边界已关闭，本地测试、发布审计、权威提交、Chrome GitHub 连续技术链、逐提交网页回查与本地—公开 SHA 映射均已通过；G3 未开始。

**下一步：** 严格停止在 G3C-R1，等待用户决定是否开始 G3；不得自动开始 G3、Design Gate 或正式 UI。

**需要用户决定：** 是否开始 G3。

**对应本地提交：** `a32cd496c6b6632da42ed22f822cbc609dfa4f4a`（`fix(G3C-R1): close upstream and path guards`）。

**GitHub 检查点：** 技术链 `6c931eff44ce156824b2f727106a810fe6092e9b` → `6ea3311c407671bc059755c857ab046d0312ee9f` → `1089ae442e8625f6e4838639ac49705d29493d7a`；最终 PASS 映射发布按账本自引用闭合规则登记。

---

### L-0025 — G3 浏览器双规则核心技术验收

**时间：** 2026-08-27（Asia/Hong_Kong）

**Gate：** G3 `PASS`

**目标：** 在 G3A/G3B/G3C/G3C-R1 全部通过后，将两模型 IFC 输入、硬碰撞与 50 mm 净距规则集成为浏览器兼容的确定性核心；不得进入 Design Gate、G4 或正式 UI。

**步骤开始核验：**

- PowerShell `7.6.4`；唯一项目/Git 根目录一致；
- `main` HEAD 为 `0a25e71da9a044c11aed13d385c9301367be7d1e`，工作树洁净，Git 远程数为 `0`；
- `git fsck --full` 成功，仅保留既有 dangling tree `48b9fdfe0f25e0bffebafe7810817275ee7bb0a4`；
- G1/G2/G3A/G3B/G3C 全量测试与 G1/G2/G3A/G3B/G3C 当前发布审计重新运行并全部通过；G3C 冻结 `9/9` 路径—SHA、`6/6` 对抗拒绝和两个 R1 守卫保持通过；
- G3C-R1 本地修复、PASS 映射与空闭合提交均为当前 HEAD 祖先；GitHub 公开技术链与 PASS 证据尾依照现有账本重新核对。

**已执行：**

- 新增浏览器可直接导入的 `web-ifc 0.0.77 + three-mesh-bvh 0.9.14` 双模型核心，输入为 MEP/结构 IFC 字节，筛选 `IfcPipeSegment` 对 `IfcWall`/`IfcBeam`；
- 输入前置验证限制为 IFC4、单一无前缀 metre 长度单位、显式 `shared_project_coordinates` 与两模型一致的有限 web-ifc coordination matrix；违反契约时运行级失败关闭；
- 硬碰撞路径从真实变换后三角网格提取直管主轴，在不大于 `0.5 mm` 的步长上验证结构体内点和点到表面深度；只有严格大于 `0.002 m` 才为 `CLASH`，等于/低于阈值为 `CLEAR`，轴外相交等不受支持情况为 `NOT_EVALUATED`；
- 世界轴 AABB 不参与任何硬碰撞或净距分类；`penetration_distance_m` 继续为 `null`，受控最大内部深度只作为状态证据，不宣称通用物理穿透距离；
- 只有权威硬碰撞 `CLEAR` 才进入三角表面最小距离；`CLASH` 抑制重复净距记录，`NOT_EVALUATED` 传播失败关闭；严格小于 `0.05 m` 为 `WARNING`；
- 输出双方 GUID、类型、名称、规则、阈值、位置/最近点、模型 SHA-256、证书、算法边界和诊断；Three.js 坐标在记录中转换回 IFC 项目轴顺序；
- 新增 C01–C08 Node 验收、当前 Chrome 无样式运行夹具、4 类对抗守卫、确定性复跑、G3 聚合回归、专项发布审计、公开语义文档与脱敏 AI 辅助记录；
- 未修改 G2/G3C 冻结数据、规则真值、依赖版本或许可证；未开始 DG、G4、正式 UI、AI、部署或视频工作。

**验证结果：**

- `scripts/test-g3.ps1`：`PASS`；C01–C08 硬状态 `8/8` 精确一致，受控 `CLASH` Precision/Recall 均为 `100%`；
- C01/C02/C07 三个 `CLASH` 均抑制净距记录；C03/C04 为零表面距离 `WARNING`，C05/C06 约为 `0.20/0.15 m` 的 `CLEAR`，C08 为净距 `NOT_EVALUATED`；
- 未核验坐标、非法阈值、畸形 IFC、模型角色错配 `4/4` 均运行级 `NOT_EVALUATED`；C01 重复运行的记录（除耗时字段外）逐字段一致；
- 当前桌面 Chrome 直接运行无样式 G3 技术页：`PASS`、C01–C08 `8/8`、预期净距/抑制状态一致、控制台错误 `0`；
- G1/G2/G3A/G3A-R1/G3B/G3B-R1/G3C/G3C-R1 全量回归通过；G2 的 16 个 IFC 与 G3C 的 9 个工件/两份真值契约哈希前后不变；
- `scripts/audit-g3.ps1`：`PASS`；78 个公开候选，绝对路径、邮箱、凭据、超限文件与误跟踪运行时命中均为 `0`，7/7 G3 必需公开文件、依赖契约、核心契约、公开计划等价性与 `pip check` 全部通过；
- `git diff --check` 通过；换行转换提示为既有非阻塞 Windows 工作副本行为；Git 远程数保持 `0`。

**失败和异常：** G3 专项审计首次运行暴露其新建路径扫描正则在补丁转义中丢失反斜杠匹配，扫描器自测正确返回 `FALSE` 并阻止通过；修复为既有全平台模式后 Windows 两种斜杠、Unix、UNC 与 HTTPS 反例自测通过，候选扫描命中为 `0`。随后新增 package-lock 版本一致性检查时，PowerShell 对空字符串包根键的对象解析安全失败；改用 `ConvertFrom-Json -AsHashtable` 与显式空键索引后通过。未发生冻结数据回写、规则放宽或外部副作用。

**边界与影响：** 直有限管轴证书是当前硬碰撞支持族；曲管、分支、退化拓扑、未验证坐标或无法由轴证书解释的侧向相交失败关闭。受控集不是任意 IFC 导出器、真实项目、规模性能或通用几何正确性的证明。G3 只提供确定性核心与无样式技术夹具，不包含文件选择 UX、结果页面、3D 审阅交互或视觉设计。

**隐私与许可证：** 代码沿用 MIT；既有生成数据沿用 CC0-1.0；未引入第三方/私人 IFC、个人身份、凭据、本地绝对路径、UI 资产或新依赖。公开候选继续排除本地总纲及全部 `scripts/audit-*.ps1`。

**版本检查点：** 权威本地步骤提交为 `437251fda583944c8b4c250186277dfd21b8e8ce`（`step(G3): integrate browser clash and clearance core`）。经用户一次集体授权后，仅通过已登录 Chrome 将 12 个已审计公开候选分五批上传，形成连续技术链 `822bf26d75464ea28e40fdab3f11cfdb65a16c00` → `65e0c4928b824996e777ffc67ab1cf8ddab80fab` → `c161edfd5ad8fc028889a9a139e5feed19d9a028` → `ecd5a7bc302e5c2028546d157d6417d21a4c15ee` → `48b681ac0d4542877c06f23e3421388460a58145`。Chrome 回查确认仓库为 `Public/main`、父链连续、批次文件数为 `5/1/3/1/2`、提交标题与本地短 SHA 一致；公开 `scripts/` 不含任何 `audit-*.ps1`，本地总纲未上传。脚本批次曾在 GitHub 显示 `3/3` 处理中时超过等待窗口；未重复上传，待网页显示三文件已完整暂存后再提交。随后读取提交链接遇到严格定位器多匹配，仅以 `.first()` 做只读回查，未产生额外写入。

**当前结论：** G3 本地实现、Node 与当前 Chrome 运行时验收、全量回归、专项公开候选审计、权威本地步骤提交、GitHub 技术链和网页回查均为 `PASS`。公开 PASS 映射按账本自引用闭合规则发布后，由随后一个不改文件的本地空 `sync(G3)` 提交登记最终远端 SHA，不递归重发。

**下一步：** 严格停止在 G3，等待用户决定是否进入 Design Gate；不得开始 G4、正式 UI、视觉设计、AI、部署或视频工作。

**需要用户决定：** 是否进入 Design Gate。未获明确指令前不得开始下一阶段。

**对应本地提交：** `437251fda583944c8b4c250186277dfd21b8e8ce`（`step(G3): integrate browser clash and clearance core`）。

---

### L-0026 — G3-R1 阈值与几何完整性审计修复

**时间：** 2026-08-27（Asia/Hong_Kong）

**Gate：** G3-R1 `PASS`（本地技术、当前 Chrome、发布审计、Git 完整性、权威提交与 GitHub 网页技术检查点均已通过；最终映射按台账自引用闭合规则登记）

**目标：** 仅关闭 G3 完成后独立审计发现的固定规则阈值、部分几何失败关闭、退化/非法索引网格和 SSOT 状态矛盾；不得改变冻结真值、扩展支持族或进入 Design Gate、G4、正式 UI。

**审计发现：**

- v1 规则 ID 可与调用方自定义或非有限阈值并存，导致规则身份与实际语义不一致；
- 一个元素的部分 placed geometry 解析失败时，其余有效部分仍可参与分类，形成不完整表示上的结论；
- 全退化三角网格可因有效边集合为空而绕过闭合二流形检查，索引三元组和源索引也缺少显式完整校验；
- 总纲正式 G3 状态栏与已闭合的 L-0025、同步台账和文档页首结论矛盾。

**已执行：**

- 将 `MEP_STRUCTURE_HARD_CLASH_V1` 与 `MEP_STRUCTURE_CLEARANCE_WARNING_V1` 分别严格绑定到有限常量 `0.002 m` 与 `0.05 m`；任何零值、非有限值或自定义值运行级失败关闭；
- 校验 web-ifc 交错顶点缓冲、三角索引数量、源/几何索引整数性与范围；要求每个网格至少保留一个非退化三角形，并对其有效边执行闭合二流形检查；
- 保留对导出器冗余退化面的兼容：只有在跳过退化面后仍形成可靠闭合表面才允许继续；全退化部件失败关闭；
- 任一 placed geometry 部件失败时，将受影响构件对整体标记为 `NOT_EVALUATED`，不再使用残缺表示分类；拒绝路径中的临时 `BufferGeometry` 同步释放；
- 新增非有限/自定义阈值、全退化网格、非三角索引、越界/非整数源索引和部分几何失败等对抗回归，并修正正式 G3 状态为既有 `PASS`。

**当前验证结果：**

- `scripts/g3-engine-tests.mjs`：`PASS`；C01–C08 状态保持 `8/8`，12/12 失败关闭守卫与确定性复跑通过；
- `scripts/test-g3.ps1`：`PASS`；既有 G1/G2/G3A/G3A-R1/G3B/G3B-R1/G3C/G3C-R1 回归、冻结哈希和测试前后 Git 状态不变性保持通过；
- 当前桌面 Chrome 无样式技术夹具：`PASS`；C01–C08 `8/8`、净距抑制/预警/清晰/失败关闭状态一致，控制台错误 `0`；
- G1/G2/G3A/G3B/G3C/G3 当前发布审计全部 `PASS`；G3 审计覆盖 78 个候选，绝对路径、邮箱、凭据、超限文件与误跟踪运行时命中均为 `0`，7/7 必需公开文件、依赖/核心契约、公开计划等价性与 `pip check` 通过；Git 远程数为 `0`；
- `git diff --check` 通过；`git fsck --full` 成功，仅有既有 dangling tree `48b9fdfe0f25e0bffebafe7810817275ee7bb0a4`。历史 `audit-g0a.ps1` 仍按启动期“整个已安装工作区不得有 >1 MiB 文件”的旧假设失败（当前依赖/运行时共 23 个超限文件），不属于当前公开候选判定且未在本次越权修改；
- 权威本地修复提交 `75e6b276cad4ef9506d5bef1b0cf7525900e947b` 已形成，提交后工作树洁净、分支仍为 `main`、远程数仍为 `0`；Chrome GitHub 技术检查点为 `8c5e61353e36e91ae1dfc5d459b8d32b72ac849e` → `265983759c4f24460ca400614b82477ab4e94fb2` → `f17c8d55cfecdc3c3ae1c7d4bfd97b812bbe71e5` → `718a9ab39e967bf9a14bfe51c1b1ba401847936a` → `1c7fdbabff34bdf143b22f7644bfb46a4c0de89b`，从既有 `c6044cf8cf1d8aea87ea6c6afb7aa5a32234ec5c` 连续衔接，提交标题均映射本地短 SHA `75e6b27`，文件数精确为 `3/1/1/1/1`，共覆盖 7 个获批公开文件，并已在 `Public/main` 回查通过。

**失败和调整：** 首版修复曾拒绝任意退化三角形，导致冻结 C01 的导出器冗余退化面令既有可靠网格失败关闭。该尝试被测试阻断后收窄为“跳过个别退化面，但必须至少存在一个非退化面且有效边仍闭合”；未放宽全退化、非法索引或部分几何失败守卫。GitHub `docs/` 目录上传界面两次返回 HTTP 400，提交历史确认均未形成提交；随后改用已有文件网页编辑器，两个脚本拆为两个单文件提交，获批范围、内容与七文件总数均未改变。

**边界与影响：** 未修改 G2/G3C 冻结数据、真值、规则阈值、支持几何证书、依赖、许可证或 `penetration_distance_m`；未开始 Design Gate、G4、UI、视觉、AI、部署或视频工作。公开候选仍排除本地总纲和全部 `scripts/audit-*.ps1`。

**当前结论：** G3-R1 `PASS`；本地有限实现、聚合测试、当前 Chrome、当前发布审计、Git 完整性、权威修复提交和 GitHub 网页技术检查点均已通过，最终公开计划/台账映射按自引用闭合规则发布并由后续空本地提交登记其远程 SHA。

**下一步：** 严格停止在 G3，等待用户决定是否进入 Design Gate；不得自动开始 Design Gate、G4、正式 UI、视觉、AI、部署或视频工作。

**需要用户决定：** 是否进入 Design Gate；本步骤完成后不自动推进。

**对应本地提交：** `75e6b276cad4ef9506d5bef1b0cf7525900e947b`（`fix(G3-R1): harden thresholds and geometry guards`）。

---

### L-0027 — DG 研究、低保真审批包与本地验收

**时间：** 2026-08-27（Asia/Hong_Kong）

**Gate：** DG `AWAITING_USER_APPROVAL`；G4 `NOT_STARTED`

**目标：** 仅完成 Design Gate 的研究和用户审批材料，冻结候选信息架构、交互、视觉、3D—结果关系、AI 层级、语言/品牌与移动端范围；不得实现正式 UI 或进入 G4。

**前置复核：** 使用 PowerShell 7.6.4 重新确认唯一项目/Git 根、`main`、洁净工作树、HEAD `8e6fd6c51f068221fc9e8818e48e695db099e126`、父提交 `d2ee4a43f45e4eacafbf379405ef60b5a778e997`、远程数 `0` 与 `git fsck --full` 仅有既有 dangling tree。G1/G2/G3A/G3B/G3C/G3 全量测试和当前发布审计全部重跑通过；G3 保持 `8/8`、Precision/Recall `100/100`、失败关闭守卫 `12/12`。历史 G0A 审计仍仅因当前 23 个已安装依赖/运行时文件超过其启动期 1 MiB 全工作区假设而失败，保留为非当前发布门且未扩范围修改。Chrome 只读回查确认 G3-R1 五提交公开父链与最终 `9376712a0fdbdf23326bd9cb5864656e6ebbb486` 两文件 PASS 映射提交。

**已执行：**

- 研究 W3C WCAG 2.2、Autodesk Clash Detective 结果/视点交互与 buildingSMART BCF 3.0 选中、可见性、着色和 IFC GUID 视点契约；
- 建立 `docs/design-brief.md`，覆盖目标用户/场景、首屏优先级、单页信息架构、端到端交互、3D 与结果列表关系、确定性结果/失败关闭/可选 AI 层级、视觉关键词与排除项、配色/字体/密度方向、中文/英文和品牌策略、桌面/移动端边界、无障碍与 G4 交接边界；
- 建立设置态与审阅态两张灰度低保真 SVG 线框，均含文本等价物、无脚本、无外部资源和最终样式声明；
- 建立 `scripts/test-dg.ps1` 与本地 `scripts/audit-dg.ps1`，验证必需审阅章节、研究来源、线框契约、AI 从属、移动端决定、无正式 UI/部署文件、G3 全量回归、公开候选脱敏与禁止文件排除；
- 未创建 `app/ui`、未修改 G3 核心/依赖/冻结数据，未开始 G4、G4AI、部署或视频。

**候选设计方向：** 桌面优先的单页工程审查工作台；首屏只完成受控示例/双 IFC 选择、边界确认与运行；运行后约 60:40 的 3D—结果布局，结果行驱动稳定视点和 A/B 构件高亮；确定性状态与证据高于可选 AI；默认中文界面保留英文机器状态/规则/IFC 术语；产品名保持 `IFC ClashTrace` 文本字标；移动端 IFC 计算与 3D 审阅明确不在本原型范围。

**验证状态：** `scripts/test-dg.ps1` `PASS`：必需审阅章节 `15/15`、研究来源组 `4/4`、低保真线框 `2/2`、正式 UI 文件 `0`、G3 全量回归与测试期间工作树不变性通过。`scripts/audit-dg.ps1` `PASS`：8 个拟公开候选的绝对路径、邮箱、凭据、超限文件命中均为 `0`，公开研究链接 `7`，远程数 `0`，非个人作者与公开总纲等价性通过；本地总纲和全部审计脚本不在拟公开清单。用户确认仍是 Gate 的不可替代停止门。

**隐私与许可证：** 研究只链接公开权威资料；无第三方资产复制、无私人/工程模型、无凭据、个人信息或本地路径进入公开候选。低保真 SVG 为项目自有代码式图形，沿用 MIT；本地总纲与全部 `scripts/audit-*.ps1` 继续禁止上传。

**当前结论：** DG 审批材料已就绪但状态只能为 `AWAITING_USER_APPROVAL`，不能在用户确认前标记 `PASS`，也不能执行 GitHub DG 完成检查点或开始 G4。

**下一步：** 向用户展示 `docs/design-brief.md` 与两张低保真线框，请求逐项批准或修改。批准后仅闭合 DG 日志、测试/审计、本地步骤提交、经行动时集体授权的 Chrome GitHub 检查点与证据映射；随后严格停止等待下一 Gate 决定。

**需要用户决定：** 是；DG 设计方向与逐项清单必须由用户确认。

**对应本地提交：** `6f9aea0ca929cfd6554649e164eb0e267a3aa2f0`（`step(DG): prepare design approval package`）。

**GitHub 检查点：** 用户未批准前禁止执行。

---

### L-0028 — DG R2 主页结构与大众版样式候选

**时间：** 2026-08-27（Asia/Hong_Kong）

**Gate：** DG `AWAITING_MAINSTREAM_STYLE_SELECTION`；G4 `NOT_STARTED`

**用户反馈与决定：** 用户基本认可原结构方向，但指出原低保真图存在字体/字号不统一、按钮内容未居中、说明不完整等问题；个人偏好黑白灰极简但要求同时面向大众审美。用户明确要求新增：带 Logo/名称的主页；`立即使用` 与 `研发进程` 两个新标签页入口；下滑图文功能介绍与常规项目页脚；大众/极简双风格、简中/英语、亮/暗外观和 AI 解读开关。

**已执行：**

- 将上述页面结构与偏好要求登记为 D-024/D-025，并更新 DG 必须审阅项、O-007 和 `docs/design-brief.md` R2；
- 明确三页结构：主页负责解释，功能页负责运行与审阅，研发历程页只展示经脱敏的研究、Gate、测试、失败/修复和公开证据；
- 将两种风格限定为同一内容/语义/可访问性上的表现层切换，不产生两套业务逻辑；简中/英语与亮/暗同理；AI 开关默认关闭，开启本身不发送数据；
- 为大众版建立三个原创候选预览：A 现代 SaaS、B 专业工程、C 编辑科技，并保留黑白灰极简参照；候选只借鉴主流类别，不复制具体网站或第三方资产；
- 新增字体家族与语义字号统一、按钮标签/图标组合居中、双语对齐和说明完整性验收契约；原两张低保真功能图的排版与完整性不再视为已批准。

**研究依据：** 复核 2026 Webflow 趋势观察、Material 3 Expressive、Ant Design 与 Atlassian Design Foundations；与既有 WCAG 2.2、Autodesk clash result/viewpoint 和 buildingSMART BCF 证据共同用于分类与约束。研究只支持候选方向，不证明某一风格对全部用户绝对最优。

**范围影响：** D-024/D-025 会增加后续 G4 的展示与偏好基础设施范围，但不授权在 DG 内实现；若截止压力出现，必须先保持确定性功能页正确性，再简化主页动画/装饰，不得删除用户要求的三页入口、双风格、双语、亮暗或 AI 开关契约。

**验证状态：** 修订后的 `scripts/test-dg.ps1` `PASS`：15/15 必需章节、8/8 研究来源组、2/2 既有结构线框、三页架构、四类偏好、字体/对齐/说明完整性契约、正式 UI 文件 `0`、G3 全量回归与测试期间工作树不变性均通过。`scripts/audit-dg.ps1` `PASS`：8 个拟公开候选无绝对路径、邮箱、凭据或超限文件命中，远程数 `0`，公开总纲等价；可视候选位于任务专属会话展示区，不属于仓库或本次公开候选。

**当前结论：** DG 尚不能 `PASS`。等待用户从 A/B/C 中选择一个作为大众版，并确认极简版及 R2 架构；未选择前不冻结最终 Logo、颜色、字体组合或修订线框，不开始 G4。

**需要用户决定：** 大众版选择 A、B 或 C；可同时指出要混合的具体元素，但不得以“全部都要”形成三套正式主题。

**对应本地提交：** 待 R2 研究、文档、测试与审计闭合后形成。

**GitHub 检查点：** DG 未获完整批准前禁止执行。

---

### L-0029 — DG R3 编辑科技与 Logo 概念方向

**时间：** 2026-08-27（Asia/Hong_Kong）

**Gate：** DG `AWAITING_LOGO_CONCEPT_FEEDBACK`；G4 `NOT_STARTED`

**用户反馈与决定：** 用户选择 C 编辑科技作为大众版，但否定此前过亮、通透和大面积偏黄的处理，要求保留克制黄色强调而将其他页面背景改为更深重的大地色；补齐并统一 R 角，修正功能按键排布，使用与所给品牌动画字标相近且中英文协调的统一现代无衬线体系，以高反差保证易读性。主页顶部只保留功能栏，其下先居中展示 Logo 和产品名称，再出现文字、入口和介绍。

**Logo 与动效方向：** 用户提供的既有合作 GIF 仅作为构图节奏和名称显现方式参考，不复制其建筑堆叠图标、旧产品名称或具体几何。IFC ClashTrace 的原创静态标志由一个完整墙体/结构构件、一条穿入的 MEP 管线、碰撞点赭黄色环及其上方提示气泡组成；有限揭示顺序为完整墙体出现、管线入场、碰撞响应、气泡弹出、图标与 `IFC ClashTrace` 字标收束。动画停止后保持静态终态，减少动态偏好直接显示终态。

**已执行：** 将上述决定登记为 D-026，更新 O-007、DG 必须审阅项及 `docs/design-brief.md` R3；生成的静态概念与五帧分镜仅作为会话内审批材料，未写入仓库、未形成生产 Logo/动画或正式 UI。

**当前结论：** 大众版选择已闭合，但 DG 仍不能 `PASS`。等待用户对静态 Logo 概念、动效叙事、R3 色彩/字体/圆角/主页顺序以及完整 DG 包提出修改或最终批准；不得执行 DG GitHub 完成检查点或进入 G4。

**对应本地提交：** `26105b120aec55946c655289acbb7110cdf3d239`（`step(DG): select editorial brand direction`）。

**GitHub 检查点：** DG 未获完整批准前禁止执行。

---

### L-0030 — DG R4 Logo 定向修订与拟上传规划

**时间：** 2026-08-27（Asia/Hong_Kong）

**Gate：** DG `AWAITING_DG_FINAL_APPROVAL`；G4 `NOT_STARTED`

**用户反馈与决定：** 用户对 R3 Logo 概念基本满意，仅要求图标与字标距离拉开，并将黄色改为橘红色；修改完成后规划整理拟上传内容。

**已执行：** 静态概念与五帧分镜均只修改两项：图标—字标安全间距增加到约一条管线外径，碰撞环和提示气泡统一改为克制橘红色；墙体、管线、气泡形状、感叹号、字标、字体和动效顺序保持不变。概念图继续位于会话生成区，不进入仓库或拟公开集。将决定登记为 D-027，并将 `docs/design-brief.md` 升级为 R4。

**精确拟上传集：** 完整批准且获得行动时集体授权后，仅通过用户已登录 Chrome 网页处理 8 个经审计文件：`BIMCLASH_AGENT_MASTER_PLAN.public.md`、`PROGRESS_SYNC.md`、`PROMPTS.md`、`README.md`、`docs/design-brief.md`、`docs/wireframes/dg-setup.svg`、`docs/wireframes/dg-review.svg`、`scripts/test-dg.ps1`。明确排除本地权威总纲、所有 `scripts/audit-*.ps1`、用户参考媒体、生成的 Logo/分镜 PNG、浏览器状态、凭据、缓存及任何 G4 正式资产。

**当前结论：** DG 尚不能 `PASS`。需用户对 R4 完整包作明确最终批准；随后还需在实际 Chrome 上传行动时，对上述一次性明确的 8 文件集合取得一次集体授权。未获两项授权前不执行外部写入，不进入 G4。

**对应本地提交：** `f92c6a0f53112eed6e84b8207c5f5a2b64174f5d`（`step(DG): refine logo and plan publication`）。

**GitHub 检查点：** 未获最终批准与行动时授权前禁止执行。

---

### L-0031 — DG R4 最终批准与公开检查点闭合

**时间：** 2026-08-27（Asia/Hong_Kong）

**Gate：** DG `PASS`；G4 `NOT_STARTED`

**用户批准：** 用户逐字确认批准 DG R4，并授权仅通过已登录 Chrome 网页上传已列明的 8 个文件，以及随后仅限 `BIMCLASH_AGENT_MASTER_PLAN.public.md` 与 `PROGRESS_SYNC.md` 的证据映射尾部；排除项不得上传。

**本地闭合与验证：** `docs/design-brief.md` 标记为 `APPROVED` 并记录全部审批项；总纲、同步台账、README、PROMPTS 与 DG 测试契约同步更新。PowerShell 7 下 `scripts/test-dg.ps1` 与 `scripts/audit-dg.ps1` 均为 `PASS`，G3 回归保持通过；发布候选精确为 8 个文件，绝对路径、邮箱、凭据、超 1 MiB 文件和禁传文件命中均为 0，Git 远程数为 0，公开总纲等价检查通过。

**Chrome 公开链：** 公开仓库保持 `Public`，从既有 G3-R1 PASS 映射提交 `9376712a0fdbdf23326bd9cb5864656e6ebbb486` 连续产生根文件提交 `8ade46356b249bf1110c08f169164b3f150a88e0`、设计简报提交 `96754f20419e6538056940c800c5665b8e49fd69`、低保真线框提交 `a6a5f8f04de8e6b85e6ece89d80a8ee49651b1fa`、公开 DG 测试提交 `856a6a2339f2b6ef444274c768d1a12452cffda2`，以及仅修正已授权 README 状态矛盾的提交 `489c6b2e25cc8d26127263178f3150bba3b7929b`。最后一个提交的父提交精确为 `856a6a2339f2b6ef444274c768d1a12452cffda2` 且只包含 `README.md`。

**网页核验：** 8 个授权文件均存在且关键批准标记、R4 范围、测试契约和低保真属性正确；两张 SVG 均无 `<script>`；README 已显示 DG R4 获批并继续阻塞未授权 G4，旧的“仍在审阅/仍需批准”文本不存在；公开提交父链、标题、本地短 SHA 与文件范围均通过 Chrome 回查。排除项未上传。

**对应本地提交：** `e416f29d1a70cf1cce642e7ba06ffab0004e972e`（批准记录）→ `3f640b7a52cbd5a6400b94d64c7120fe92d8e59a`（批准证据映射）→ `2d046b2fc09d64dc1704bc89e4d638ff4c3e8d9d`（README 状态一致性修正）。

**当前结论：** DG R4 的用户批准、本地验证、限定 Chrome 公开检查点、逐项网页核验与初始本地—远程映射均为 `PASS`。按公开台账自引用规则，仅继续发布公开总纲与进度台账的证据映射尾部，并以随后一个不改文件的本地空 `sync(DG)` 提交登记最终台账远端 SHA；不递归重发。

**下一步：** 严格停止在 DG，等待用户决定是否授权下一阶段；不得自动开始 G4、正式 UI、运行时 AI、部署或视频。

---

### L-0032 — DG-R1 三项限定审计修复

**时间：** 2026-08-27（Asia/Hong_Kong）

**Gate：** DG-R1 `PASS`；G4 `NOT_STARTED`

**授权范围：** 用户批准只修复审计发现的三项问题，不授权 G4、正式 UI、运行时 AI、部署、视频或任何其他设计扩展；本次批准不推定 Chrome 外部写入授权。

**本地修复：** `docs/design-brief.md` 现在明确 DG 只冻结设计契约，基础 G4 也必须获得单独、明确的用户授权；`scripts/audit-dg.ps1` 使用精确八文件允许集、去重/缺失/多余负向自测和动态禁传计数失败关闭；`scripts/test-dg.ps1` 检查生产 UI 目录、根构建入口、典型 UI 源文件、前端依赖/脚本信号，并只豁免既有无样式 G1/G3 技术验证页。

**验证：** PowerShell 7 下 DG 测试与发布审计均为 `PASS`；发布集合精确守卫自测 `TRUE`，正式 UI 路径守卫自测 `TRUE`，正式 UI 文件与包/脚本信号均为 `0`，G3 全量回归通过，公开候选仍为精确 8 个，隐私/凭据/大小/禁传扫描为 0，Git 远程数为 0。

**对应本地技术提交：** `85ef30eeb833f9356884c4a86a9b0218b8d79387`（`fix(DG-R1): close design and audit guard gaps`），只包含设计简报与两个 DG 脚本；本地审计脚本继续禁止上传。

**公开授权：** 用户已给予一次性 Chrome 集体授权，仅上传 `BIMCLASH_AGENT_MASTER_PLAN.public.md`、`PROGRESS_SYNC.md`、`PROMPTS.md`、`docs/design-brief.md`、`scripts/test-dg.ps1`，随后证据映射尾部只允许再次处理公开总纲与进度台账。`BIMCLASH_AGENT_MASTER_PLAN.md`、`scripts/audit-dg.ps1` 及全部既有排除项不得上传。

**Chrome 公开链：** 从既有 DG 最终台账 `c73d7193d14be5d39b41a485a51535e8129fafd8` 连续产生三文件根证据提交 `b62f3cecaf76ba35b2a6dfab2d194512deadaed2`、单文件设计简报提交 `c29dc0e27f6a6981261696fa9867b6924df4e8ab` 和单文件公开测试提交 `121ad2d4876edebf13f25d325208590bae1d229a`。父链、提交标题、本地短 SHA 和文件树均通过 Chrome 回查。

**网页核验：** 公开设计简报明确 DG 不授权基础 G4且任何基础 G4 工作都需单独用户决定；公开测试包含正式 UI 路径守卫自测、文件计数与包/脚本信号。仓库保持 `Public`；本地总纲、`scripts/audit-dg.ps1` 和全部既有排除项未上传。

**当前结论：** DG-R1 的三项修复、本地验证、限定 Chrome 公开检查点、逐项网页核验与初始映射均为 `PASS`。按台账自引用规则，仅继续发布公开总纲与进度台账映射尾部，并以随后一个不改文件的本地空 `sync(DG-R1)` 提交登记最终台账远端 SHA；不递归重发。

**下一步：** 严格停止在 DG-R1，等待用户决定是否另行授权下一阶段；不得自动开始 G4、正式 UI、运行时 AI、部署或视频。

---

### L-0033 — G4 单独授权、前置复核与本地纵向切片验收

**时间：** 2026-08-27（Asia/Hong_Kong）

**Gate：** G4 `IN_PROGRESS`（本地技术、当前桌面 Chrome、发布审计与权威步骤提交 `PASS`；公开检查点待授权/闭合）

**用户授权与范围：** 用户明确授权立即开始基础 G4，并要求完成后严格停止在 G4。授权覆盖 DG R4/R1 已批准的三页结构、编辑科技大众版与黑白灰极简版、简中/英语、亮/暗、AI 解读默认关闭、统一字体/字号/按钮居中/说明完整性、深重大地色与橘红强调、受控圆角、居中品牌锁定和原创墙体—管线—碰撞—提示气泡方向。明确不授权 G4AI 提供商/API 集成、部署、公开访问变更或视频；任何外部上传仍须行动时取得一次精确集体授权。

**强制前置复核：** 完整读取最新版本地权威总纲与 `PROGRESS_SYNC.md`；PowerShell `7.6.4`、唯一项目/Git 根、`main`、洁净 HEAD `9a3f7b65a0bb7b45afda5e38fd5b51c4b82b737f`、远程数 `0` 与 `git fsck --full` 重新核验通过，后者仅有既有 dangling tree `48b9fdfe0f25e0bffebafe7810817275ee7bb0a4`。G1/G2/G3A/G3B/G3C/G3/DG 全部现行测试和 G1/G2/G3A/G3B/G3C/G3/DG 发布审计重跑通过；G0A 旧审计仍只因成熟工作区 23 个已安装运行时/依赖文件超过其启动期全工作区 1 MiB 假设返回已登记历史型 `FAIL`，其三文件候选自身仍是零路径/邮箱/凭据且公开计划等价。全部本地步骤 SHA 均为当前 HEAD 祖先；Chrome 重新抽查 G0A 至 DG-R1 各阶段公开终点，并逐笔确认 DG-R1 `c73d719 → b62f3ce → c29dc0e → 121ad2d → 68c2e2a → e1ef61e` 父链与文件范围。

**本地实现：**

- 建立 `/`、`/app/` 与 `/development/` 三页结构，主页入口均以新标签页打开；
- 使用纯 HTML/CSS 几何实现原创墙—管—碰撞—气泡品牌标志，不引入生成图片或第三方品牌资产；
- 建立 Mainstream/Minimal、简中/English、Light/Dark 与 AI Off/On 本地偏好；AI 默认关闭且不影响记录；
- 功能页支持受控 review pack 与固定角色的双 IFC 文件选择、`.ifc`/25 MiB/IFC4/metre 预检、共享坐标显式确认、分阶段运行状态与失败关闭；
- review pack 对 C01/C03/C05/C08 分别执行真实 `evaluateIfcPair`，同时展示 `CLASH/WARNING/CLEAR/NOT_EVALUATED`；
- 建立汇总、稳定排序、结果筛选、双方 GUID/类型/阈值/净距/证书/诊断/哈希证据抽屉；
- Three.js 视图重新解析选中案例的真实 IFC 网格，按 GUID 映射并提供聚焦、适合模型、可逆隔离、orbit/pan/zoom 与文本等价物；
- AI 开启状态只显示未发送字段预览和确定性模板，代码不含提供商端点、密钥或出站请求；
- 研发页只使用脱敏公开证据，不显示本地路径或本地审计文件名；移动宽度小于 1024 CSS px 时明确阻断 IFC 计算。

**验证结果：** `scripts/test-g4.ps1` `PASS`：三路由 `3/3`、双文件角色 `2/2`、硬碰撞与净距同时展示、失败关闭 UI、筛选/证据、真实 IFC 3D、双风格/双语/亮暗、AI 默认关闭且无提供商、双语静态契约、本地离线路由/WASM content type、G3 全量回归、测试期间工作树不变性均通过。当前桌面 Chrome review pack 得到 `CLASH 1 / WARNING 1 / CLEAR 1 / NOT_EVALUATED 1`、四条记录、一个真实 WebGL canvas，控制台 warning/error 为 `0`；C03 展示 `2.0 mm` 容差、`0 mm / 50.0 mm` 净距及双方哈希；自选 C01 双 IFC 返回唯一 `CLASH`；共享坐标与 900 px 宽度停止门生效；三页偏好与研发证据页核验通过。`scripts/audit-g4.ps1` 对精确 18 个唯一公开候选及缺失/多余/重复三类负向变体均通过，绝对路径、邮箱、凭据、大文件和提供商/API 信号命中均为 `0`，依赖集合不变、远程数 `0`、公开总纲等价且不存在托管配置。G1/G2/G3A/G3B/G3C/G3/DG/G4 全部现行测试与发布审计最终回归通过。

**失败和调整：** 首次自动化文件选择直接触发隐藏 input 时未产生 file chooser，未写入页面或外部系统；按浏览器文件上传契约改为点击可见 label 后，两份本地冻结 IFC 选择、预检和运行通过。AI checkbox 首版在 change 事件中重建控件，令自动化无法确认 checked 状态；改为原位刷新标签和值后，开/关与默认状态均通过。共享坐标输入守卫同步补充错误 live-state 标记。首次 G4 发布审计正确暴露公开测试脚本中的路径扫描正则字面量与错误依赖名称假设；将公开测试的路径模式改为运行时拼装，并按锁定的实际三依赖修正审计后，命中归零且依赖检查通过。全回归末尾还复现本地服务器已停止但重定向日志句柄尚未释放的临时目录清理竞态；测试改为显式等待进程退出，验证后清除唯一残留的本地临时目录，重跑通过。

**隐私、许可与边界：** 代码继续 MIT，既有生成 IFC/真值继续 CC0-1.0；未新增依赖、外部数据、个人信息、凭据、绝对路径公开内容、生成媒体或部署元数据。自选文件名不进入确定性记录。G4AI、部署、公开访问变更和视频均保持 `NOT_STARTED`。

**本地权威提交：** `36f65def8eeda33d7670d681ab57cda39c2b1f66`（`step(G4): build deterministic web vertical slice`），父提交为 DG-R1 最终本地同步提交 `9a3f7b65a0bb7b45afda5e38fd5b51c4b82b737f`；提交后 `main` 洁净、远程数 `0`，`git fsck --full` 仍只报告既有 dangling tree `48b9fdfe0f25e0bffebafe7810817275ee7bb0a4`。

**当前结论：** G4 本地技术、当前 Chrome 产品验收、精确发布审计和权威步骤提交为 `PASS`，但 Gate 按通用协议仍为 `IN_PROGRESS`，直到行动时精确 Chrome 集体授权、网页上传/核验和本地—公开映射全部闭合。

**下一步：** 集中列明已审计的 18 个网页上传文件并只请求一次行动时集体授权；获准前不得外部写入。不得进入 G4AI、部署、公开访问变更或视频。

---

### L-0034 — G4 主页首轮用户审阅修复

**时间：** 2026-08-27（Asia/Hong_Kong）

**Gate：** G4 `IN_PROGRESS`（主页首轮修复本地 `PASS`；等待用户继续审阅其余页面）

**用户反馈：** 用户在 `1433 × 898` 的本地主页逐项标注三个下拉控件、首屏两段文字、三个介绍区标题与正文以及页尾，并补充暗色过于昏暗、按钮文字与底色同色、介绍区缺少悬停动效及 DG 品牌 Logo 动效落点问题；明确要求先修主页，再继续查看其他区域。

**实现：** 将风格/语言/外观三个原生下拉替换为共享的自定义可访问列表框，编辑科技模式的触发器和弹出菜单均为 `10 px` 受控圆角，黑白极简模式均为 `0 px` 方角，并支持方向键、Home/End、Enter/Space、Escape 与焦点返回；“大众版/极简版”改为“编辑科技/黑白极简”。首屏标题和说明使用显式语义断行；三个介绍标题缩小至最大 `56 px`，正文扩写为从双 IFC 输入、规则执行、结果定位、证据复核到适用条件的完整正向叙述，适用范围标题使用显式语义换行。主暗色从近黑棕色改为更明亮的深青灰大地色，强调色按钮固定为深色文字，极简主按钮固定为黑白反差。三个介绍区增加受限悬停位移与强调线展开，页尾以更深独立底色和橘红上边界区分。DG 的墙—管—碰撞—气泡动效正式落在主页首屏品牌锁定：初次进入按墙体、管线、碰撞环、提示气泡依次揭示，悬停提供轻量反馈；`prefers-reduced-motion` 继续强制静态结果。

**浏览器验收：** 在 `1433 × 898` 验证编辑科技下拉触发器/菜单圆角均为 `10 px`，黑白极简均为 `0 px`，极简主按钮为亮底深字，暗色编辑科技正文背景为 `rgb(32, 43, 40)`、页尾为独立 `rgb(20, 29, 26)` 且强调边界为 `rgb(255, 118, 87)`；三个自定义下拉鼠标与键盘切换、简中/English、Light/Dark 均通过并恢复简中/编辑科技/暗色。介绍区标题最大 `55.9 px`，悬停标题/正文分别位移 `10/16 px`，强调线完整展开；Logo 三段动画名及悬停变换生效。共享偏好在功能页仍为三个自定义控件、零原生 select、AI 默认关闭、运行入口可用，主页和功能页控制台 warning/error 均为 `0`。

**自动验证：** `scripts/test-g4.ps1` 新增自定义 profile 下拉和主页文案/动效/对比度契约后 `PASS`；`scripts/audit-g4.ps1` 继续对精确 18 文件、路径/邮箱/凭据/大文件/提供商信号、依赖和零远程规则返回 `PASS`。

**本地提交与边界：** 技术提交 `9cff1bb92d123011496db16d9bf2eff1442cb213`（`fix(G4): refine homepage feedback and controls`），父提交为 `33cf0d45de19f6ed3f46c16741f0955b0265aace`，仅修改 `index.html`、`app/ui/preferences.mjs`、`app/ui/site.css` 与 `scripts/test-g4.ps1`。用户此前尚未批准 18 文件上传请求，因此没有 GitHub 写入、部署、公开访问变更、G4AI 提供商/API 或视频工作。

**下一步：** 保持本地预览，等待用户继续审阅功能页或研发页；在完整 G4 视觉审阅闭合前不重新请求或执行网页上传。

---

### L-0035 — G4 主页第二轮用户审阅修复

**时间：** 2026-08-27（Asia/Hong_Kong）

**Gate：** G4 `IN_PROGRESS`（主页第二轮修复本地 `PASS`；等待用户继续审阅）

**用户反馈与方向：** 用户要求提高产品保证可读性，重做页眉三个入口与页脚链接，修复冲突记录在英语下仍显示中文及感叹号偏心，令前两段标题与第三段一样保持两行，统一所有页面的深黑灰页脚，并将“大众版/极简版”正式命名为“大众体验/工程极简”。暗色不再以青色作为大面积第一印象，但品牌 Logo 的青色管线保留。

**研究与实现：** 参考 GitHub Primer 官方色彩使用原则，以中性灰阶建立默认、表面和抬升表面层级，把橘红限制为主要动作/冲突等语义强调；主页暗色改为 `#0d1117 / #161b22 / #21262d`，记录示意背景也改为中性灰，青色只保留在品牌及模型管线。页眉入口改为无常驻下划线的分组导航；产品保证改为 01/02/03 高对比卡片；页脚在亮暗外观和共享页面中固定为 `#0b0d10` 与浅色文字，链接以悬停边框/底色反馈替代下划线。冲突记录的状态、标签、字段名、场景标签、导航/页脚辅助名称和品牌可访问名称均纳入双语字典；状态变为中文“硬碰撞 · CLASH”或英语“Hard clash · CLASH”。感叹号改为伪元素绘制并在 `38 px` 圆内精确居中；三段标题均用显式语义换行。

**浏览器验收：** 在 `1433 × 898` 的本地当前桌面浏览器中核验大众体验/工程极简、简中/English 和 Light/Dark 的实际交互。英语状态显示 `Hard clash · CLASH`，规则/容差/构件身份、保证条目、导航、页脚和品牌可访问文本同步切换；中文三段标题均保持两行。大众体验暗色使用中性近黑灰层级，品牌及示意管线保留青色；工程极简保持方角黑白灰。产品保证为三列高对比卡片，页眉与页脚无常驻下划线，统一深色页脚在适用范围末端清晰可辨。感叹号的竖线与圆点均位于 `38 px` 图标的水平中心。

**自动验证与提交：** `scripts/test-g4.ps1` 新增中性暗色、统一页脚、保证卡片、分组导航、居中状态图标、风格命名及英文状态契约后 `PASS`；`scripts/audit-g4.ps1` 的精确 18 文件集合、三类集合负向自测、路径/邮箱/凭据/大文件/提供商信号、依赖与零远程检查均 `PASS`；`git diff --check` 通过。技术提交为 `cfe16c9ae56f6534a8488901137bd1e6c9780c7d`（`fix(G4): refine homepage dark mode and i18n`），父提交为 `048e23da87c78254d57d15f33b032872c8f728de`，修改 `index.html`、`app/ui/preferences.mjs`、`app/ui/site.css`、`development/index.html` 与 `scripts/test-g4.ps1`。

**边界与下一步：** 没有 GitHub 写入、Git 远程、部署、公开访问变更、G4AI 提供商/API 或视频工作。保持本地预览，等待用户继续检查其他位置；在完整 G4 视觉审阅闭合前不请求或执行网页上传。

---

### L-0036 — G4 主页第三轮产品展示重构

**时间：** 2026-08-28（Asia/Hong_Kong）

**Gate：** G4 `IN_PROGRESS`（主页第三轮修复本地 `PASS`；等待用户继续审阅）

**用户反馈与方向：** 用户要求删除主页中央的三段导航与主页 AI 开关，三个显示偏好适度右移；增强亮色首屏品牌墙体的可见边界；解决英文直译造成的臃肿堆叠；判断并融合原三块说明，同时用同色模块、悬停变色和气泡真实预览展示显示模式、确定性功能页与可追溯开源研发历程。AI 开关只应在实际工作功能页操作，外部状态文字删除，轨道放大并在内部显示开关状态。

**实现与媒体边界：** 主页页眉现只保留品牌和风格/语言/外观三个控件；AI 控件仅由 `/app/` 的 `data-ai-control="true"` 挂载。原 CORE/EVIDENCE/SCOPE 长段落合并为一个三卡产品展示区，三卡使用同一基础色，悬停或键盘聚焦时以受限橘红混合色与位移动效强调，并弹出真实页面预览。四张 PNG 均由 `1433 × 898` 本地产品页直接生成，只含程序生成 IFC/产品内容：亮色中文大众体验主页、暗色英文工程极简主页、真实受控四案例运行后的英文暗色结果工作台、亮色中文研发历程。Logo/模型管线青色保留，主暗色继续使用中性 `#0d1117 / #161b22 / #21262d`。亮色品牌墙体增加边框与阴影；中英文首屏和展示卡片改为简洁、语义化断行。功能页标题/输入卡字号与行高收敛，AI 轨道增至 `78 × 34 px`，`关闭/Off` 状态位于轨道内部。

**浏览器验收：** 当前本地浏览器在 `1433 × 898` 完成亮色中文大众体验、暗色英文大众体验、暗色英文工程极简、功能工作台与研发页核验。首屏无中央导航或 AI 控件，亮色墙面边界清楚；英语首屏与功能页没有文字重叠。受控 C01/C03/C05/C08 真实运行返回 `CLASH/WARNING/CLEAR/NOT_EVALUATED` 各一条并生成真实 Three.js 视图；工作台 AI 控件默认 `Off` 且状态位于轨道内。三张产品卡在悬停/聚焦时正确显示对应真实预览，控制台错误为 `0`。

**自动验证与提交：** `scripts/test-g4.ps1` 新增四张真实预览路由、主页无旧导航、工作台唯一 AI 控件、状态内置轨道与新展示结构契约后 `PASS`；`scripts/audit-g4.ps1` 将精确候选集更新为 22 个唯一文件，只单独允许四张已人工浏览器核验、每张小于 1 MiB 的程序生成 PNG，并继续通过缺失/多余/重复负向自测、路径/邮箱/凭据/大文件/提供商信号、依赖与零远程检查。主体技术提交为 `2b940714ede774278d84a3bedffd96c55642bb80`（`fix(G4): rebuild homepage product showcase`）；浏览器复核发现并关闭卡片中文非语义断行后，最终技术提交为 `0d5efbee04dc23abb32d3fa24cca7ff86a592601`（`fix(G4): lock showcase semantic line breaks`）。

**边界与下一步：** 没有 GitHub 写入、Git 远程、部署、公开访问变更、G4AI 提供商/API 或视频工作。保持本地预览，等待用户继续检查其他区域；完整视觉审阅闭合前不请求或执行网页上传。

---

### L-0037 — G4 主页与功能工作台第四轮联合修复

**时间：** 2026-08-28（Asia/Hong_Kong）

**Gate：** G4 `IN_PROGRESS`（两页当前注释本地 `PASS`；等待用户继续审阅）

**用户反馈与方向：** 用户要求一次修改主页和功能工作台的全部当前注释：主页品牌图需要恢复立体感；显示模式中的暗色预览必须使用完整新 Logo；工作台与研发预览不得带页面滚动条且图幅应收敛；第三卡标题固定两行；删除含义不明的独立 `NOT_EVALUATED` 横幅。工作台标题需在不同桌面窗口中保持美观，真实双 IFC 应为主入口，受控示例降为备选，移动端表述更委婉；AI 开关必须离开页眉、在文件工作区明确标识，三个显示控件继续右对齐。

**实现：** 品牌标志新增墙体顶面，并以正面/侧面渐变、管线高光、碰撞环内外阴影、气泡高光和接触阴影形成原创 CSS 立体层次，既有分阶段揭示和减弱动效守卫保留。展示预览宽度由最高 `640/680 px` 收敛为 `520/560 px`，四张 PNG 全部从当前本地路由重新截取；预览增加内部裁切容器，使右侧浏览器滚动条不进入气泡画面。第三卡改为“研发过程公开 / 方法与边界可追溯”两行，底部孤立横幅删除。工作台标题使用 `keep-all`、平衡换行与两级响应式网格；双文件卡成为首要并列入口，受控示例成为次级横条；移动端文字改为后续版本跟进。页眉只渲染三个显示控件，唯一 AI 控件以 `data-control-mode="ai"` 挂载在支持边界与运行栏之间，包含独立“AI 结果解读”说明和 `116 × 42 px` 内置状态轨道。

**浏览器与自动验收：** 当前本地浏览器在 `1024 × 800`、默认 `1280 × 720` 和 `1433 × 898` 验证中文与简洁英语标题、双文件卡、次级示例条和页眉控件均无重叠或孤字。主页亮色新 Logo 与暗色工程极简预览均显示完整墙体、管线、环和气泡；三张卡悬停预览尺寸收敛、无可见滚动条，第三卡两行成立。功能页量测为 `header AI = 0`、`workspace AI = 1`、页眉显示控件 `3`；AI 实际完成 `off → on → off` 且 `html[data-ai]` 同步。`scripts/test-g4.ps1` 与精确 22 文件 `scripts/audit-g4.ps1` 均 `PASS`，四张媒体均低于 1 MiB，`git diff --check` 通过。

**本地提交与边界：** 技术提交 `95dd9d70d73f6e76b04b42a5d08350de0bdecd04`（`fix(G4): refine homepage and workspace review UX`）。没有 GitHub 写入、Git 远程、部署、公开访问变更、G4AI 提供商/API、移动端计算实现或视频工作；G3 确定性规则、冻结数据、依赖和许可证未改变。

**下一步：** 保持本地预览并等待用户继续审阅；不得把本轮本地通过解释为外部上传授权，也不得进入下一阶段。

---

### L-0038 — G4 主页第五轮品牌空间关系与 3D 预览修复

**时间：** 2026-08-28（Asia/Hong_Kong）

**Gate：** G4 `IN_PROGRESS`（本轮两项主页注释本地 `PASS`；等待用户继续审阅）

**用户反馈与方向：** 用户指出首页第二张产品卡的功能页预览没有完整展示 3D 区域，并认为首屏品牌图标的墙体、管线、碰撞环与气泡空间关系仍然错误。修复必须直接改善信息取景与原创标志的物理可读性，而不是仅增加装饰。

**实现与浏览器验收：** 品牌标志改为前后两层墙板、墙后连续管段、墙前截断管段、位于穿越界面的碰撞环及与碰撞点分离的带尾提示气泡；显式层叠顺序让管线在墙体处发生可信遮挡，墙面、侧板、管线高光与接触阴影保持克制立体感，既有分阶段动效和减弱动效守卫保留。功能工作台预览从真实 C01/C03/C05/C08 运行后的英文暗色页面重新截取为 `1433 × 806`，完整包含 3D 工具栏、模型场景、四条结果与右侧记录区；主页气泡裁切继续隐藏浏览器滚动条。亮色中文与暗色英文主页预览同步更新为新标志。当前本地浏览器在 `1433 × 898` 验证首屏与第二卡悬停画面，空间关系清楚且完整 3D 区域可见。

**自动验证、提交与边界：** `scripts/test-g4.ps1` 新增前/后管段及气泡尾部结构契约并 `PASS`；精确 22 文件 `scripts/audit-g4.ps1`、四张程序生成预览媒体检查与 `git diff --check` 均 `PASS`。技术提交为 `fda7dc5d42bc7c97be07bc84864a9a5099fa808d`（`fix(G4): correct hero mark and 3D preview framing`）。没有 GitHub 写入、Git 远程、部署、公开访问变更、G4AI 提供商/API、视频或确定性规则修改。

**下一步：** 保持本地预览并等待用户继续检查主页及其他页面；不得进入下一阶段或把本地通过解释为外部上传授权。

---

### L-0039 — G4 研发页排版与跨页页脚文案修复

**时间：** 2026-08-28（Asia/Hong_Kong）

**Gate：** G4 `IN_PROGRESS`（本轮四项注释本地 `PASS`；等待用户继续审阅）

**用户反馈与方向：** 用户要求改善研发页 `0 / 49 / 50 / 51 mm` 的单位排版，修正公开历史与失败修复两处标题的断句，并删除所有页面页脚中语义空泛的“贡献者”称呼。

**实现与浏览器验收：** 净距边界数值与单位拆为同一不换行基线组，`mm` 使用较小、中性字色的单位层级，不再与主数值争夺视觉重心。失败标题改为“保留失败，/ 边界才可信。”，公开历史标题改为“本地历史与公开历史，/ 是两本互为映射的账本。”；中英文均由两段独立翻译节点维持语义断行。主页与研发页页脚标题统一为产品名 `IFC ClashTrace`，删除中英文“贡献者/contributors”字样。当前本地浏览器在 `1708 × 898` 验证中文数值与标题、英文双行标题、主页/研发页页脚，旧称呼命中为 `0`。

**自动验证、提交与边界：** `scripts/test-g4.ps1` 新增研发页数值/标题结构与产品页旧页脚称呼负向守卫并 `PASS`；精确 22 文件 `scripts/audit-g4.ps1` 与 `git diff --check` 均 `PASS`。技术提交为 `bff6f7ccb4af1f11a44decccb52d56edf3c50f75`（`fix(G4): refine development typography and footer copy`）。没有 GitHub 写入、Git 远程、部署、公开访问变更、G4AI 提供商/API、视频或确定性规则修改。

**下一步：** 保持本地预览并等待用户继续审阅；不得进入下一阶段或把本地通过解释为外部上传授权。

---

### L-0040 — G4 工作台输入路径编号修复

**时间：** 2026-08-28（Asia/Hong_Kong）

**Gate：** G4 `IN_PROGRESS`（本轮三项编号注释本地 `PASS`；等待用户继续审阅）

**用户反馈、实现与验收：** 用户要求将两个正式 IFC 输入卡原 `B1/B2` 改为 `A1/A2`，并将受控示例入口原 `A · DEMO` 改为 `B`。功能页静态结构已按此统一，使 A 组明确表示两份正式模型输入，B 表示备选演示路径。`scripts/test-g4.ps1` 同时新增三个新编号的正向契约与三个旧编号的负向守卫。当前本地浏览器在 `1708 × 898` 验证 `A1 / A2 / B` 的位置、字号和层级，旧编号命中为 `0`。

**自动验证、提交与边界：** `scripts/test-g4.ps1`、精确 22 文件 `scripts/audit-g4.ps1` 与 `git diff --check` 均 `PASS`。技术提交为 `d8d499686b1faec15df7a184d62875ee1edb90aa`（`fix(G4): clarify workspace input indexing`）。没有 GitHub 写入、Git 远程、部署、公开访问变更、G4AI 提供商/API、视频或确定性规则修改。

**下一步：** 保持本地预览并等待用户继续审阅；不得进入下一阶段或把本地通过解释为外部上传授权。

---

### L-0041 — G4 工作台演示属性标签恢复

**时间：** 2026-08-28（Asia/Hong_Kong）

**Gate：** G4 `IN_PROGRESS`（即时编号文案修正本地 `PASS`；等待用户继续审阅）

**用户反馈、实现与验收：** 用户确认 A 组正式输入仍为 `A1/A2`，但要求 B 入口保留原有 `DEMO` 属性字样。受控示例标签已由 `B` 修正为 `B · DEMO`；测试新增该精确值的正向契约并拒绝纯 `B` 及此前旧编号。当前本地浏览器在 `1708 × 898` 验证标签集合精确为 `A1 / A2 / B · DEMO`。

**自动验证、提交与边界：** `scripts/test-g4.ps1`、精确 22 文件 `scripts/audit-g4.ps1` 与 `git diff --check` 均 `PASS`。技术提交为 `7b5d91f294961e5decd68cab1978c2b83d6a40a1`（`fix(G4): retain demo label in workspace index`）。没有外部写入、Git 远程、部署、G4AI 提供商/API、视频或确定性规则修改。

**下一步：** 保持本地预览并等待用户继续审阅；不得进入下一阶段或把本地通过解释为外部上传授权。

---

### L-0042 — G4 研发页公开方法区与指标单位修复

**时间：** 2026-08-28（Asia/Hong_Kong）

**Gate：** G4 `IN_PROGRESS`（本轮两项研发页注释本地 `PASS`；等待用户继续审阅）

**用户反馈与方向：** 用户指出研发页底部“本地历史/公开历史账本”及其本地 Git、登录 Chrome、SHA 映射、PROGRESS/PROMPTS 链接属于个人操作流程，不适合公众展示；同时要求 `0 / 49 / 50 / 51 mm` 中的 `mm` 与数值使用同一橙色。

**实现与浏览器验收：** 该区域整体改为 `OPEN METHOD` 公共说明，标题为“规则、验证与边界，/ 都有公开依据。”，正文仅说明公开碰撞规则、受控案例、已验证边界、许可证、本地浏览器处理与 AI 不改写确定性结果。四个入口改为项目源码、验证方法、MIT 代码许可和 CC0 数据许可；删除个人发布工作流叙述及 PROGRESS/PROMPTS 入口。英文使用独立简洁表达。指标单位继续保持较小层级，但颜色改为同一 `--accent` 橘红。当前本地浏览器在 `1708 × 898` 验证中英文标题、正文、四个入口与橙色单位，旧个人流程标记命中为 `0`。

**自动验证、提交与边界：** `scripts/test-g4.ps1` 将研发页公开证据契约改为公开验证/数据许可入口，并新增个人流程内容负向守卫；本地测试、精确 22 文件审计与 `git diff --check` 均 `PASS`。技术提交为 `62e7717527c53b9820939d36512e4a9cd3421a77`（`fix(G4): replace private workflow copy with open method`）。没有外部写入、Git 远程、部署、G4AI 提供商/API、视频或确定性规则修改。

**下一步：** 保持本地预览并等待用户继续审阅；不得进入下一阶段或把本地通过解释为外部上传授权。

---

### L-0043 — G4 共享坐标显式确认默认值修复

**时间：** 2026-08-28（Asia/Hong_Kong）

**Gate：** G4 `IN_PROGRESS`（本轮功能页注释本地 `PASS`；等待用户继续审阅）

**用户反馈、实现与验收：** 用户要求自选模型的“共享项目坐标”确认框默认不勾选，只能由用户主动点击。静态 `checked` 默认值已删除；运行时代码没有且不得加入自动置真。`scripts/test-g4.ps1` 新增默认勾选属性与 `shared_coordinates.checked = true` 自动确认的负向守卫。当前本地浏览器实测状态为首次打开 `false`、载入受控示例后 `false`、用户点击后 `true`、刷新后恢复 `false`。

**自动验证、提交与边界：** 本地 G4 测试、精确 22 文件审计与 `git diff --check` 均 `PASS`。技术提交为 `5f16dca1303ac8621601aac25773bbf56397553d`（`fix(G4): require explicit coordinate consent`）。共享坐标缺失时的既有失败关闭仍保持；没有外部写入、Git 远程、部署、G4AI 提供商/API、视频或确定性规则修改。

**下一步：** 保持本地预览并等待用户继续审阅；不得进入下一阶段或把本地通过解释为外部上传授权。

---

### L-0044 — G4 主页英文公开证据标题两行修复

**时间：** 2026-08-28（Asia/Hong_Kong）

**Gate：** G4 `IN_PROGRESS`（本轮主页英文排版注释本地 `PASS`；等待用户继续审阅）

**用户反馈、实现与验收：** 用户指出主页第三张产品卡的英文标题 `A public trail. / From method to release.` 在 `1708 × 898` 暗色大众体验中被挤成三行。英文标题现改为更简洁的独立表达 `Built in public. / Fully traceable.`，保留既有中文标题、卡片含义和显式换行。`scripts/test-g4.ps1` 新增新文案存在与旧三行文案缺失的双向守卫。本地浏览器在 `1708 × 898` 下实测大众体验与工程极简两种样式，标题高度均为两行行高（`95.02 px = 2 × 47.52 px`）。

**自动验证、提交与边界：** 本地 G4 测试、精确 22 文件审计与 `git diff --check` 均 `PASS`。技术提交为 `8f84020c9b075ffc91e52528b1a118cac7460b48`（`fix(G4): lock English evidence title to two lines`）。没有外部写入、Git 远程、部署、G4AI 提供商/API、视频或确定性规则修改。

**下一步：** 保持英文暗色主页本地预览并等待用户继续审阅；不得进入下一阶段或把本地通过解释为外部上传授权。

---

### L-0045 — G4 主页悬浮预览圆角一致性修复

**时间：** 2026-08-28（Asia/Hong_Kong）

**Gate：** G4 `IN_PROGRESS`（本轮主页悬浮预览注释本地 `PASS`；等待用户继续审阅）

**用户反馈、实现与验收：** 用户指出第三张主页功能卡的悬浮预览在上、下侧使用了视觉不一致的 R 角。核验发现气泡外框为 `16 px`，内部图片裁切为 `7.5 px`。实现改为共享 `--preview-radius`：大众体验的外框与图片裁切均为 `16 px`，工程极简两者均由既有设计令牌归零为 `0 px`。本地浏览器在 `1708 × 898` 验证简中暗色悬浮实景，并交叉检查大众体验亮/暗与工程极简亮色组合；预览位置、箭头、图片裁切和说明文字保持完整。

**自动验证、提交与边界：** `scripts/test-g4.ps1` 新增共享预览圆角契约；同时显式释放本地静态测试进程对象，避免 Windows 下审计管道调用结束时临时日志句柄延迟释放。直接测试、精确 22 文件审计与 `git diff --check` 均 `PASS`。技术提交为 `efb9614ea01cf09459e77e28ed7b60077cd9dc93`（`fix(G4): unify showcase preview radii`）。没有外部写入、Git 远程、部署、G4AI 提供商/API、视频或确定性规则修改。

**下一步：** 保持简中暗色主页本地预览并等待用户继续审阅；不得进入下一阶段或把本地通过解释为外部上传授权。

---

### L-0046 — G4 本地验收与公开候选准备

**时间：** 2026-08-28（Asia/Hong_Kong）

**Gate：** G4 `IN_PROGRESS`（`LOCAL_ACCEPTANCE_PASS`；公开检查点等待用户明确授权）

**用户决定与收口：** 用户表示目前对前端工作基本满意，要求整理内容、准备 GitHub 上传材料并在真正上传前等待批准。该决定视为 G4 当前三页 UI 与本地功能闭环的本地验收，不构成任何外部写入授权。公开候选继续严格限定为 `scripts/audit-g4.ps1` 已冻结的 22 个唯一文件；本地总纲、所有 `scripts/audit-*.ps1`、依赖、数据、输出、浏览器状态、部署文件、G4AI 和视频仍排除。

**发布前内容整理：** README 的边界标题同步至 G4，并删除让公众运行不发布的本地审计脚本的说明，改为只列出公开可复现的 DG 合同测试及明确的维护者本地审计边界。`package.json` 的 `0.0.0-g3-core` 版本标识属于 DG 冻结的 G3 依赖契约，发布前曾尝试同步到 G4，但被既有 DG 回归正确拒绝，故不放宽测试并恢复原值；G4 身份由 README、页面与 G4 测试明确表达。公开总纲、同步台账、提示记录和 G4 说明同步记录本地验收与等待授权状态。

**回归与审计：** PowerShell 7、`main`、洁净工作树、Git 远程 `0` 和完整性检查已重新核验。G1、G2、G3A、G3B、G3C、G3、DG 与 G4 冻结测试全部 `PASS`；G4 精确候选审计为 `22` 个唯一文件，缺失/多余/重复负向自测通过，绝对路径、邮箱、凭据、提供商/API 信号及超 1 MiB 文件均为 `0`，公开总纲等价。历史 `audit-g0a.ps1` 在后续 Gate 已安装依赖的完整工作区会按其早期大文件规则返回 `FAIL`，不修改该历史证据，也不将其作为当前 G4 发布判据。

**停止门：** 只完成本地候选清单、哈希、分组与提交信息准备；不得打开 GitHub 写入页、选择文件、提交或变更访问状态。行动时必须向用户展示完整 22 文件与分组方案并取得一次明确集体授权，然后才可在已登录 Chrome 网页中执行。

---

### L-0047 — G4 Chrome 公开检查点与映射闭合

**时间：** 2026-08-28（Asia/Hong_Kong）

**Gate：** G4 `PASS`（严格停止在 G4；未进入 G4AI、部署、公开访问变更或视频）

**授权与范围：** 用户在收到完整 22 文件、七个初始上传组及两个映射尾更新的集中清单后明确回复批准，并指定使用已登录的 Google Chrome。Chrome 行动时再次确认仓库为 `Public`、当前分支为 `main`、登录账号具有写入权限，远端起点为 DG-R1 最终台账 `e1ef61ee96e50e7034b2449198f6349f75066565`。上传严格限于获批集合；本地总纲、所有 `scripts/audit-*.ps1`、依赖、数据、输出、浏览器状态、部署文件、G4AI 提供商/API 和视频均未上传。

**七组初始公开链：** [测试与本地服务器 `07a89385c5e655965cf8a9e052e72bcd6c5f1fb7`](https://github.com/delongwangshu49-hub/ifc-clashtrace/commit/07a89385c5e655965cf8a9e052e72bcd6c5f1fb7) → [UI 运行时 `301bbe5e0648cd9fe9fe458cd7490a7746614164`](https://github.com/delongwangshu49-hub/ifc-clashtrace/commit/301bbe5e0648cd9fe9fe458cd7490a7746614164) → [四张预览媒体 `89c9592e936ffdc118e192710c4e45c91a2aea92`](https://github.com/delongwangshu49-hub/ifc-clashtrace/commit/89c9592e936ffdc118e192710c4e45c91a2aea92) → [功能页路由 `71d4b20eefa1f1043a55449a80d5f545fad11759`](https://github.com/delongwangshu49-hub/ifc-clashtrace/commit/71d4b20eefa1f1043a55449a80d5f545fad11759) → [研发页路由 `d1351d74bd466b2dd9e101b616b3f4fc3a46fe65`](https://github.com/delongwangshu49-hub/ifc-clashtrace/commit/d1351d74bd466b2dd9e101b616b3f4fc3a46fe65) → [G4 纵向切片证据 `15cf379603b9d25f52f1904469a899d1526be72d`](https://github.com/delongwangshu49-hub/ifc-clashtrace/commit/15cf379603b9d25f52f1904469a899d1526be72d) → [产品与治理根文件 `a0d9f080c11d40f12c494c5e28f8bb19fc60b702`](https://github.com/delongwangshu49-hub/ifc-clashtrace/commit/a0d9f080c11d40f12c494c5e28f8bb19fc60b702)。七个提交的文件数精确为 `3 / 6 / 4 / 1 / 1 / 1 / 6`，父链逐项回查为 `e1ef61e → 07a8938 → 301bbe5 → 89c9592 → 71d4b20 → d1351d7 → 15cf379 → a0d9f08`。

**网页核验与异常：** Chrome 逐项核验七个提交的精确文件树、完整父 SHA、提交标题、`main` 分支和 `Public` 可见性；远端树包含全部 22 个候选，且根目录无本地总纲、`scripts/` 无任何 `audit-*.ps1`。第七组首次点击隐藏文件输入时文件选择器超时；只读检查确认暂存文件为 `0`、提交摘要为空且远端无新提交，因此立即停止。用户确认 Chrome 文件访问权限始终开启后，改用页面可见选择控件成功选择精确六个根文件，没有重复提交或越界文件。

**完成规则：** 本地实现、全部冻结测试、桌面 Chrome 产品验收、精确发布审计、用户行动时授权、七组网页上传、公开文件/父链/可见性核验和本地—公开映射均已通过。依据公开台账自引用闭合规则，只再发布本条已派生的公开总纲和最终 `PROGRESS_SYNC.md`；最终公开台账 SHA 只登记在随后一个不改文件的本地 `sync(G4)` 提交中，不递归重发。此后严格停止在 G4。

---

### L-0048 — G4-R1 有限审计修复

**时间：** 2026-08-28（Asia/Hong_Kong）

**Gate：** G4-R1 `PASS`（有限修复、公开检查点与网页核验闭合；严格停止）

**审计发现与范围：** 用户授权只修复本轮 G4 审计指出的问题。自选 IFC 更换后原共享坐标确认会沿用，且查看器以固定 `CUSTOM` 来源 ID 缓存模型，第二次自选运行可能让新记录对应旧 3D；旧结果、证据和 AI 预览也未在输入变化时立即失效。另有 G3A/G3B/G3C/G3 历史审计把四张 G4 受控 PNG 当文本读取，产生盘符式二进制字节序列的绝对路径误报；总纲、G4 说明和台账仍保留发布前状态措辞。

**实现与守卫：** 新增统一运行状态失效路径：任一文件变化或切换受控示例都会清空旧记录、来源、选中项、证据与 AI 预览，撤销共享坐标确认并将 3D 来源缓存归零；每次新运行也在计算前清除旧审阅状态，确保自选模型重新载入。G4 测试锁定上述状态转移并继续拒绝任何自动勾选。四个历史审计只跳过 G4 精确审计已经单独批准并计数的四张程序生成 PNG，文本候选的路径、邮箱和凭据扫描保持不变。技术提交为 `cb2c0aa3e692b08e8ed1574089b89f8078e9e112`（`fix(G4-R1): close run state and audit gaps`）。

**本地验收：** 当前本地浏览器连续运行两组不同自选 IFC：第一组得到 `CUSTOM · CLASH`；更换任一文件后共享坐标确认立即恢复未选，旧结果与证据不可见；重新确认并运行第二组得到 `CUSTOM · CLEAR`，3D 画布重新可用，控制台警告/错误为 `0`。G1、G2、G3A、G3B、G3C、G3、DG、G4 八组冻结测试和相应八组当前发布审计全部 `PASS`，绝对路径、邮箱、凭据与 Git 远程命中均为 `0`。

**公开检查点：** 用户对精确 6 文件集合给予一次行动时集体授权。首次网页批量上传将 `app/ui/app.mjs` 与 `scripts/test-g4.ps1` 错误扁平化到仓库根目录，形成 `72eda6c6e7c6c8f89afe8a4c11188f0d83b7fb83`；Chrome 核验立即发现路径错误，随后以 `3372a89` 与 `58bde53` 删除两个非预期根文件，再以正确目录提交 `914b0d92d0ae77b3cd4ed49885690acf2de37a8c`（运行状态修复）和 `ae8f16784afbaaecf2b69ab80d98f75d6340e4b6`（回归合同）。README 与 G4 证据页分别发布为 `406b2ab66736457f6109c4a607aef0a355218ce8` 和 `0324a88f33a0b68b5896b25b98dc0b5453ae0acb`。失败提交与纠正链均保留为可审计历史；当前树不存在两个错误根文件。

**边界与停止门：** 不修改 G3 确定性规则、IFC 数据、依赖、三页视觉范围或 DG 设计契约；不加入 G4AI 提供商/API、部署、公开访问变更或视频。基础 G4 的原 22 文件公开链保持历史事实；本轮仅发布已授权的必要勘误、运行状态修复与映射尾部。本地总纲和全部审计脚本未发布。G4-R1 闭合后严格停止，等待用户决定下一阶段。

---

### L-0049 — G4AI 启动、提供商决策、本地验收与受控 live 验证

**时间：** 2026-08-28（Asia/Hong_Kong）

**Gate：** G4AI `PASS`（本地实现、模拟验收、受控 live 验证与限定 Chrome 公开检查点均已闭合；严格停止）

**用户授权与前置复核：** 用户明确授权在唯一工作区直接开始 G4AI，并要求完成后严格停止。开始改动前已完整分段读取当前总纲与同步台账；独立确认 PowerShell `7.6.4`、唯一 Git 根、`main`、HEAD `6c42367f663b3488b1ec4f2e14ceef3bd71b1ab2`、洁净工作树、远程数 `0`，`git fsck --full` 仅含既有 dangling tree。根目录无失败上传遗留的 `app.mjs`/`test-g4.ps1`，正确路径文件存在。G1/G2/G3A/G3B/G3C/G3/DG/G4 当前测试与现行发布审计全部通过；历史 G0A 审计仍按启动期全工作区大文件假设返回预期 `FAIL`，但其三文件公开候选敏感扫描为零，未把历史规则改写成当前 Gate 证据。

**官方研究与决定：** 仅使用实施当日官方资料比较 GroqCloud、Google Gemini Developer API 与 Hugging Face Inference Providers。Groq 免费计划列出 `openai/gpt-oss-20b` 的 `30 RPM / 1,000 RPD / 8K TPM / 200K TPD`，支持严格结构化输出；默认推理不留存，但可靠性/滥用调查可能临时记录输入输出最多 30 天，所有客户可在数据控制启用 ZDR，潜在保留数据位于美国 GCP。Google 免费层明确将内容用于改进产品；Hugging Face 免费用户每月仅有 `$0.10` 且路由还受底层提供商政策约束。用户在审阅候选后明确选择 Groq，并批准自动测试零 live、开发日最多 5 次、审查/演示前 24 小时停止非必要调用、至少预留 40K 日 token 的策略。实现的 completion 上限为每次 900 token；仅按 200K/900 折算约 222 次，但输入、schema 与推理 token 同样计数，故该数不是保证。免费、地区、账号、模型、额度和条款均仅为 2026-08-28 时点结论。

**实现：** 新增提供商中立契约与编排层、服务端 Groq 适配器、同源本地 Node API、空值 `.env.example`、G4AI 页面交互与专项文档/测试。浏览器只构造语言、冻结规则边界、状态汇总、记录本地别名、状态、双方 allowlist IFC 类型和既有有限测量；GUID、名称、IFC、网格、文件名、路径、哈希、诊断、位置、浏览器/账号元数据均被丢弃。用户开启 AI 不发送数据，必须先查看逐条字段预览并勾选新的发送同意；支持发送、取消、重试、复制和关闭。密钥只由 Node 进程从 `GROQ_API_KEY` 读取，浏览器源码无 Groq 端点、认证头或密钥访问。最终提供商响应是以精确 actionable record alias 为全部必填键的闭合对象，仅允许 attention/rationale/next-step enum；所有用户可见文案均由本地可信中英文模板生成。界面为中文时只显示中文、为英文时只显示英文；切换语言会取消在途请求并作废旧预览/结果。AI 结构不含状态、规则、距离或证据字段，未知/重复/遗漏别名、状态词、数值单位、URL、畸形/超长内容均拒绝。

**本地验证：** `scripts/test-g4ai.ps1` 与 Node 单元/同源集成测试通过：最小字段、提示注入剥离、确定性输入不可变、模拟成功、严格请求/响应、中文/英文逐语言匹配、切换语言撤销旧结果、超时、取消、限流、配额耗尽、缺少提供商、断网、畸形响应、同源/content-type/额外字段守卫和确定性模板降级全部 `PASS`；G4/G3 回归和测试前后已跟踪工作树不变。自动测试不读取真实密钥、不访问外部 API、不消耗配额。`scripts/audit-g4ai.ps1` 对精确 19 个唯一公开候选及缺失/多余/重复负向集合均通过，绝对路径、个人邮箱、凭据、大文件、禁用范围、浏览器密钥/提供商端点与 Git 远程命中为零；本地权威总纲和所有 `scripts/audit-*.ps1` 保持排除。

**本地技术提交：** `5474ae187dfe8080e9ffff336a2cfe2bd995dabf`（`fix(G4AI): constrain live bilingual interpretation`），父提交 `b8bafb207ee33d4988fe96738203a562e7a53a2b`。连同此前 `f393e4c5e90b6bfd171aa2948ee4ed795845a10c` 与 `b8bafb207ee33d4988fe96738203a562e7a53a2b`，形成本地实现、确定性规则 ID 修复和 live/bilingual 收敛链。提交后 `main` 洁净、Git 远程数 `0`；`git fsck --full` 成功并报告原有 `48b9fdfe...` 及本阶段索引/提交过程留下的两个不可达 tree `7230b12c...`、`895d406a...`，无 dangling commit/blob、无引用损坏且不删除历史。

**Chrome 与 live 验证：** 本地 review pack 始终保持确定性汇总 `CLASH/WARNING/CLEAR/NOT_EVALUATED = 1/1/1/1`；AI 初始关闭，开启不发送，预览只列 `R01`–`R04` 与最小事实，新同意未勾选时发送禁用；无提供商时正确显示 `provider_unconfigured` 模板。用户随后逐次确认四次使用同一生成 review-pack 最小派生字段的 live 调用：第一次自由文本虽通过提供商 schema 但被本地语义验证拒为 `malformed_response`；第二次动态数组约束被提供商拒为 `provider_rejected`；第三次静态数组返回的 actionable 引用集合不可靠，被拒为 `malformed_response`；第四次精确必填键/enum-only 闭合对象成功。最终页面显示独立“AI 生成解读”标签和本地中文模板，覆盖 `R01/R02/R04`，机器汇总与证据抽屉未变，Chrome warning/error 为 `0`。前三次均证明失败关闭，第四次后不再调用 live。此前同一已登录 Chrome 还只读复核 G4-R1 六提交链和失败上传—两笔清理链、正确嵌套路径及两个错误根路径 `File not found`。

**限定 Chrome 公开检查点：** 用户在收到精确 19 文件、按目录分组及其后仅公开总纲/进度台账映射尾的集中清单后明确集体批准。已登录 Chrome 行动时确认仓库仍为 `Public`、分支为 `main`、远端起点为 G4-R1 最终台账 `bb3b59ec95b2a379725215dbe44caedf3b7cdfe3`。精确文件按目录形成连续父链：根 6 文件 `21a84770460635e23730e7ea31068b8bc9cd1318` → `app/ai` 3 文件 `5cfe7222754e4252601aae324342d60fea52223a` → `app/ai/adapters/groq.mjs` `f55a16253fa6e6dec3ea9d2abd52ca139ad8f630` → `app/index.html` `6ae2adbdd43334b71ebe601bf1aa30f5da78fb66` → `app/ui` 2 文件 `2d92c628c4dbc2a13cbaaa52fe014be629f18eff` → `scripts` 4 文件 `bbd939294462dcf35a7c3d3f1101991bb809db5d` → `docs` 2 文件 `734d86856a35624899d3b01f87e517b376eafffd`。Chrome 逐笔确认 `6/3/1/1/2/4/2 = 19` 文件、父 SHA 和正确路径；无文件扁平化。本地总纲、全部 `audit-*.ps1`、`.env.local`、密钥、数据、依赖、输出、部署文件与视频均未上传。

**边界与停止门：** 未部署、未改变公开访问、未开始视频、未增加移动端计算、未扩展 IFC 支持或确定性规则。四次 live 均在行动时逐次获批，只使用程序生成的受控 review-pack 最小派生记录，未记录密钥或原始提供商 payload；当前 live 验证已闭合，不再发起调用。精确 19 文件技术公开检查点已通过；仅按已批准范围以公开总纲和进度台账完成证据映射尾，并以不改文件的本地空 `sync(G4AI)` 提交登记最终台账远端 SHA，不递归重发。随后严格停在 G4AI。

---

### L-0050 — G4AI-R1 首次人工试用可用性修复

**时间：** 2026-08-29（Asia/Hong_Kong）

**Gate：** G4AI-R1 `PASS`（本地验收、授权网页技术发布与核验完成；公开映射尾部按自引用规则闭合）

**用户反馈与授权：** 用户用第一个受控试样实际审阅后指出，当前 AI 文案只是状态复述与零碎动作标签，未提供约定的分析内容；生成入口藏在完整证据抽屉内且按钮只描述发送合规步骤，缺少清晰任务指引。用户明确要求修改提示词并适度放宽，但没有授权新 live、GitHub 上传、部署、G5 或其他阶段。

**修复决定：** 保留最小出站字段、默认关闭、可见预览、新同意、服务端密钥、同源请求、exact-key actionable record、双语匹配、确定性权威和失败关闭；把 provider 输出从 enum-only 升级为受长度限制的跨记录综合、逐记录成段证据解读和单句协调重点。提示词要求比较复核顺序、解释记录关系并区分直接冲突、净距余量和证据缺口，禁止只复述状态或输出碎片；同时禁止臆测原因、位置、责任专业、设计意图、法规/安全结论、新尺寸或首选工程方案。错误语言、状态词重述、数值/单位、URL、未知/遗漏引用和超长内容继续由本地拒绝。

**入口与降级：** AI 入口从证据抽屉提升到确定性汇总正下方，显示同步开关与“开启 → 预览字段 → 确认并分析”路径；按钮改为面向结果的“生成 AI 协调分析”。本地失败降级也改为同样的综合—证据解读—协调重点结构，避免提供商不可用时退回无信息量碎片。

**离线验证：** `scripts/test-g4ai.ps1` 通过并新增 `G4AI_SUBSTANTIVE_COORDINATION_ANALYSIS=PASS`；mock 覆盖 exact-key 成段分析、中英文匹配、错误语言、状态/数值/URL、未知或遗漏引用拒绝，以及既有超时、取消、限流、配额、断网、同源、密钥和确定性输入不变守卫。G4/G3 聚合回归通过，G1/G2/G3A/G3B/G3C/G3/DG/G4/G4AI 九组当前发布审计全部 `PASS`，远程数保持 `0`。

**本地实现提交：** `fe08ecc66a4914f0f87ed786e0aed413361cce14`（`fix(G4AI-R1): improve analysis and entry`）。该提交仅存在于权威本地 Git；未配置远程、未上传，且不构成 G4AI-R1 外部检查点闭合。

**浏览器验收：** 在无 `GROQ_API_KEY` 的独立本地端口还原 review pack，确定性汇总仍为 `1/1/1/1`。运行后无需打开证据抽屉即可看到新入口；开启、预览、同意和本地失败关闭路径均可操作。中文结果显示综合分析、R01/R02/R04 的成段证据解读与协调重点；切换英语会立即作废中文面板，重新生成后全部 AI 标题、综合与记录分析均为英语。该流程没有外部提供商请求或额度消耗。

**第二轮信息层级修复：** 用户进一步指出，运行前完整 AI 卡片与结果后的完整 AI 卡片重复，两个同步开关、三步路径及长按钮仍然冗杂。依据 D-040，运行前卡片和开关被删除，改为一行说明产品支持可选 AI，并前移 GroqCloud、模型、字段预览/二次确认以及额度、地区、数据条款的时点边界；结果后只保留唯一紧凑入口，标题为“可选 AI 解读”，开关标签仅供辅助技术读取，按钮缩短为“AI 解读 / Interpret”。预览字段、新同意和服务端边界未被删减。

**第二轮本地实现提交：** `1815527fb947cd71531c2bbfa46b0d96020457ce`（`fix(G4AI-R1): simplify interpretation entry`）。该提交通过本地双语浏览器验收、G4AI 全套回归和 G1/G2/G3A/G3B/G3C/G3/DG/G4/G4AI 九项公开候选审计；未发起 live 调用、未配置远程、未上传或发布。

**第三轮公众文案修复：** 用户指出无密钥路径直接显示 `provider_unconfigured`，且供应商说明中的“当前账户”、ZDR、免费额度和核验日期更像面向开发者的私人说明。依据 D-041，运行前说明改为可选服务、发送前确认和供应商当前公开条款；发送前预览改为说明最小字段以及明确不发送 IFC 文件、构件标识和本地文件信息。API 配置状态和内部错误码不再进入公众界面；缺少提供商、繁忙/配额、连接失败和其他异常分别映射为简洁的双语服务状态，并统一说明已切换本地解读且检测结果不受影响。技术审计中的账号、配额和保留边界不删除。

**第三轮本地实现提交：** `7510277`（`fix(G4AI-R1): make AI service copy public-facing`）。无密钥浏览器复现仍得到确定性 `1/1/1/1` 汇总，预览显示公众化供应商/隐私说明，中文和英文降级结果均不含 `provider_unconfigured` 或账户级术语；G4AI/G4 回归与 `git diff --check` 通过，未使用密钥、未消耗 live 额度、未配置远程或外部写入。

**授权公开技术链：** 用户审阅精确 13 文件、9 组、533,878 字节统计后给予一次集体授权。GitHub `main` 的已核验起点为基础 G4AI 最终公开台账 `3192be64330b002b75499edc8ca460a471036fa5`。前 7 组共 11 个技术/证据文件按正确目录发布为：协调分析合同 `5d4d89289f341d08ae4c019c889d8cb7ed1f10c3` → Groq 适配器 `d5af488740f26f0a4b1a5e7f93a3c0fb34a9d78e` → 工作台入口 `a9f224d4c478cc0893bc08fcf979619d6e677fc5` → 网页空提交事件 `c22480e71796200f004e38133b30648c7b7d6d79` → 工作台 UI `42c38c9761d621620bab4dc18032f7f2d2dbd208` → 运行时与测试 `92c324aede2e3dd4a16d825b8df1bd39d0301e88` → 架构证据 `748808268ace1c329686bfc0264836af6017acb2` → 产品与提示证据 `7fc70cb3490406a574cad1528353753201881861`。有效文件数依次为 `1/1/1/0/2/3/1/2 = 11`，父链连续，嵌套路径正确。

**网页异常与核验：** GitHub 在工作台入口提交后异步刷新较慢；同一已批准 `app/index.html` 的第二次提交动作形成 `c22480e…`，网页明确显示 `0 个文件更改` 与“没有更改”。它没有修改树、没有增加或扁平化路径，也没有越出授权集合；该零文件提交保留为公开审计事件，随后所有提交均以它为连续父链继续。Chrome 已逐笔核验标题、完整 SHA、父提交和精确路径；技术顶点 `7fc70cb…` 仍位于 `main`，仓库保持 `Public`。

**闭合与停止：** 只再发布与本地总纲等价的公开总纲，以及登记公开总纲 SHA 的最终 `PROGRESS_SYNC.md`；最终台账 SHA 仅写入随后一笔不改文件的本地 `sync(G4AI-R1)` 提交，不递归重发。本地总纲和全部 `scripts/audit-*.ps1` 始终排除。没有第五次 live 调用、密钥使用、部署、公开访问变更、移动端计算、视频或 G5 工作。

**当前边界：** 只实施 G4AI 有限可用性修复并使用 mock/本地无密钥路径验证；不发起第五次 live 调用，不使用或输出密钥，不改变 G3/G4 状态、规则、测量或证据，不配置远程，不进行任何外部写入，不进入 G5、部署、移动端计算或视频。

---

### L-0051 — G4AI-R2 同意、并发与容量有限修复

**时间：** 2026-08-29（Asia/Hong_Kong）

**Gate：** G4AI-R2 `LOCAL_PASS / LIVE_UNVERIFIED`（本地修复与失败关闭验收通过；当前成段合同 live 未成功；严格停止）

**授权与基线：** 用户批准只修复审计发现的重试同意复用、异步陈旧结果竞争和 40 记录/900 completion token 容量失配，并单独授权恰好一次当前 R1 成段分析合同 live smoke。改动起点为 `main` HEAD `14de4e90aab5f38f314f0be396fabc09067ffbfa`、洁净工作树、远程数 `0`；PowerShell 为 `7.6.4`。完整性检查成功，仅报告四个已登记 dangling tree，无损坏引用、提交或 blob。开始前重跑现行 G4AI 测试和九组发布审计，均通过。

**实现：** `AI_MAX_RECORDS=6` 同时约束浏览器构造、服务端验证和界面无发送提示；七条及以上记录在本地失败，不发起提供商请求。重试按钮不再直接复用旧请求，而是返回相同最小字段预览并生成未勾选的新同意。预览、发送、取消、关闭、停用 AI、语言切换、输入变化与新运行都通过单调操作身份和 AbortController 所有权隔离；旧状态查询或请求完成不能渲染到或清除更新的预览、语言和结果。技术提交为 `0f2d9af407f44e91447b1ff13e680718122dbeb5`（`fix(G4AI-R2): close consent concurrency and capacity gaps`）。

**本地验证：** Node 测试覆盖六条接受、七条拒绝、900 token 上限、exact-key 成段合同及既有中英文/错误语言/状态/数值/URL/引用完整性守卫；PowerShell UI 契约锁定 retry-to-preview、操作身份和控制器所有权。G4AI/G4/G3 回归、G1/G2/G3A/G3B/G3C/G3/DG/G4/G4AI 九组当前发布审计和 `git diff --check` 均通过。桌面 Chrome 在故意断开本地服务后得到可重试连接失败；点击重试只回到新预览，复选框未选且发送禁用，证明旧同意不能复用。

**密钥事件与处置：** 首个替换密钥曾被误粘贴到普通可见终端；在任何 live 请求前即停止，用户随后确认已删除 Groq 中全部旧密钥并新建密钥。新密钥只经本地遮罩输入框注入子服务进程环境，未写入仓库、辅助脚本、日志或证据；临时辅助脚本不含密钥且已删除，服务进程退出后环境随之销毁。审计未检出凭据。该事件不改变“密钥不得进入终端、文件、日志、截图或公开候选”的长期规则。

**唯一 live 结果：** 官方 Groq 模型严格结构化输出、免费额度和数据处理边界在 2026-08-29 重新只读核验；这些仍是时点事实。配置状态显示服务可用后，Chrome 以生成 review pack 的四条最小派生记录 `R01`–`R04`、新同意和中文界面恰好发送一次。请求最终进入公众连接类降级提示并渲染本地协调分析；隐私设计没有持久化原始提供商响应，因此不把连接类进一步猜测为 timeout 或 network。确定性汇总仍为 `1/1/1/1`，记录、规则、测量与证据未变，控制台问题为 `0`。没有自动或人工重试。

**结论与边界：** R2 三项逻辑修复及失败关闭均可接受，本地状态为 `LOCAL_PASS`；但唯一 live 没有得到当前 bounded-prose schema 的成功响应，不能证明提供商实时接受该合同，因此总体只能记为 `LOCAL_PASS / LIVE_UNVERIFIED`，不能写成严格 `PASS`。基础 G4AI enum-only live 和已发布 G4AI-R1 历史证据保持不变。本轮没有 GitHub 上传、远程配置、部署、公开访问变更、移动端计算、视频或 G5；严格停止在 G4AI-R2，等待用户决定是否未来另行授权实时复验或下一阶段。

---

### L-0052 — G4AI-R2 单独授权 live 重试

**时间：** 2026-08-29（Asia/Hong_Kong）

**Gate：** G4AI-R2 `LOCAL_PASS / LIVE_UNVERIFIED`（重试再次失败关闭；不再调用；严格停止）

**授权：** 用户在 L-0051 的唯一 live 连接类降级后明确回复批准重试。该决定只授权一次同一当前 R1 bounded-prose 合同、同一生成 review pack 最小派生字段的 live 调用；不授权第三次调用、自动重试、上传、部署、公开访问变更、移动端计算、视频或 G5。

**安全启动：** 第一次遮罩启动器运行因无法解析项目本地 Node 可执行文件而在服务启动前终止，未产生 HTTP 提供商请求，密钥环境随进程清除。修正为显式项目内 Node 路径后，用户再次经遮罩框输入当前密钥；本地服务明确报告 provider configured。密钥未写入仓库、脚本或日志。请求结束后服务被停止，进程环境销毁，无密钥临时启动器删除。

**发送与结果：** 英文界面重新运行生成 review pack，确定性汇总为 `CLASH/WARNING/NOT_EVALUATED/CLEAR = 1/1/1/1`。AI 开关由用户流程开启后仅显示 `R01`–`R04` 的 locale、状态汇总、本地别名、allowlist IFC 类型和既有 measurement kind；发送按钮在新的同意未勾选时禁用。勾选后恰好发送一次。提供商路径没有生成应用可接受的 AI 解读，页面显示英文本地协调分析；公众界面和隐私设计不暴露或持久化内部失败 subtype、原始提供商 payload 或密钥，因此不猜测具体 provider 错误。

**核验与判断：** 请求后机器汇总仍为 `1/1/1/1`，记录、规则、测量和证据未变，浏览器 warning/error 日志为 `0`。没有进一步调用。重试再次证明新同意状态机、语言一致性和失败关闭有效，但第二次当前合同 live 仍未成功，因此 R2 保持 `LOCAL_PASS / LIVE_UNVERIFIED`，且 Groq 当前 bounded-prose 路径不应被宣传为演示可靠。基础 enum-only live 与已发布 G4AI-R1 历史事实不被改写。

---

### L-0053 — G4AI-R3 提供商合同错误修复

**时间：** 2026-08-29（Asia/Hong_Kong）

**Gate：** G4AI-R3 `LOCAL_PASS / LIVE_UNVERIFIED`（本地合同修复与无密钥回归通过；未授权新 live；严格停止）

**授权与基线：** 用户在两次当前成段合同 live 均失败关闭后要求“修复错误”，授权本地诊断和修复，但没有授权再次使用密钥或发起提供商请求。改动前 PowerShell 为 `7.6.4`，`main` HEAD 为 `e56e067db2c4ec3c2b44010dc87e70f32afa7b46`，工作树洁净，Git 远程数 `0`；`git fsck --full` 成功并仅报告五个已登记 dangling tree，无损坏引用、提交或 blob。开始前现行 G4AI 测试通过。

**只读研究与判断：** 2026-08-29 重新核验 Groq 官方 Structured Outputs、API Reference、Rate Limits 与 Data Controls。所选 `openai/gpt-oss-20b` 仍列为支持 `strict: true`；官方要求对象属性全部 required 且每个对象 `additionalProperties: false`，并明确只支持 JSON Schema 子集。当前文档没有把 `minLength`/`maxLength` 列入该严格子集；同时 `max_completion_tokens` 是生成上限。由于 R2 两次请求按隐私设计都没有持久化原始响应或内部 subtype，不能把任一风险追溯断言为唯一真实根因，只能修复可复现的合同风险。

**实现：** 提供商 strict schema 删除未在当前官方结构子集中列明的字符串长度关键字，但独立本地校验器继续强制 synthesis、analysis 与 next-step 的长度、语言、状态词、数字、URL 和引用完整性边界；completion cap 从 `900` 提高为 `1,600`，temperature 固定为零，仍保持单次最多六条记录和 low reasoning effort。适配器把 `finish_reason=length` 归类为可重试 `output_limit`，把显式 refusal 归类为不可重试 `provider_refused`；提供商 HTTP 拒绝、畸形响应和本地语义拒绝继续分开，且不保存原始提供商正文。技术提交为 `e27dd4f47bb97de0050ff635555567c81bce90c2`（`fix(G4AI-R3): align Groq prose contract`）。

**容量边界：** 官方 Free Plan 公布值仍为 `8K TPM / 200K TPD`，实际组织 Limits 页和响应头优先。以 `1,600` 输出上限机械相除约为每天 `125` 个 output-cap equivalents，但输入、schema 和 reasoning token 同样计入，因此不是保证调用次数。既有每日最多五次开发调用、演示前 24 小时停止非必要调用、保留 40K token 与 20K floor 政策不变。

**验证：** G4AI Node 套件新增严格 schema 关键字守卫、`1,600` 上限、输出截断、显式拒绝和本地语义拒绝分类测试；完整 `scripts/test-g4ai.ps1`、G4/G3 聚合回归、`git diff --check` 以及 G1/G2/G3A/G3B/G3C/G3/DG/G4/G4AI 九组现行发布审计全部通过。无密钥本地浏览器复验保持确定性 `1/1/1/1`；中文结果切换为英文后旧结果隐藏，英文预览只含 `R01`–`R04` 最小字段，发送按钮在新同意前禁用，本地降级生成英文 synthesis 与 R01/R02/R04 分析，页面没有汉字、内部 `provider_unconfigured` 或控制台 warning/error。

**结论与边界：** 可复现的 schema、输出容量和错误可观测性风险已在本地闭合，G4AI-R3 为 `LOCAL_PASS`；没有新 live 证据，因此当前 bounded-prose 提供商路径仍为 `LIVE_UNVERIFIED`，不能宣传为演示可靠或严格 `PASS`。没有读取、写入或输出新密钥，没有 Groq 调用、GitHub 上传、Git 远程、部署、公开访问变更、移动端计算、视频或 G5。严格停止在 G4AI-R3，等待用户另行决定是否授权一次新的 live smoke。

---

### L-0054 — G4AI-R3 中英文成段分析 live 闭合

**时间：** 2026-08-29（Asia/Hong_Kong）

**Gate：** G4AI-R3 `PASS`（当前 bounded-prose 合同、语义守卫、中英文 live 与失败关闭闭合；严格停止）

**授权与安全边界：** 用户在 L-0053 的 `LOCAL_PASS / LIVE_UNVERIFIED` 状态后明确批准冒烟测试“直到功能成功实现”。所有请求继续只使用生成 C01/C03/C05/C08 review pack 的 locale、固定规则边界、状态汇总、本地别名、allowlist IFC 类型和既有 measurement 字段；每次发送前均重新打开预览并勾选新的同意。IFC、GUID、名称、路径、哈希、诊断、浏览器元数据、密钥和原始提供商响应均未写入证据。该授权未扩展到 GitHub、部署、公开访问、移动端计算、视频或 G5。

**五次 live 序列：** 第一次返回公开可重试降级，隐私设计未保留私有 subtype。为避免盲目消耗额度，第二次经 localhost-only 临时诊断转发层发送同一最小字段；该层只输出 `semantic_rejected / retryable=true`，不打印请求或响应正文。该结果定位到本地状态词正则使用大小写不敏感模式，错误拒绝了正常英文 `clear/warning/clash` 语汇。修复后第三次进入 provider 模式，但人工内容审计发现输出提及输入未支持的安全、可施工、设计意图、责任专业、false-negative 和物理改造建议，因此不予验收。随后 strict schema 字段说明与 system prompt 明确只允许证据复核动作，本地校验新增未支持工程/归属/安全/方案概念拒绝；自然小写协调词仍允许，大写机器 token、数字单位和 URL 继续拒绝。

**最终成功：** 第四次英文请求返回 `AI coordination analysis` provider 模式，包含 synthesis 与 R01/R02/R04 成段 evidence reading / coordination focus；未命中未支持概念，未出现汉字，发送前新同意有效。随后切换简体中文会隐藏英文结果；第五次重新预览并新同意后返回 `AI 协调分析` provider 模式，综合分析和 R01/R02/R04 全部为中文，未出现英文句子或未支持概念。两次最终成功前后确定性汇总均为 `CLASH/WARNING/NOT_EVALUATED/CLEAR = 1/1/1/1`，机器记录、规则、测量和证据未变，浏览器 warning/error 为 `0`。安全诊断层只报告 `mode=provider / error=none`。

**实现与验证：** 技术提交 `57ee0492cafd35546594b4c3448d1a42695aaef3`（`fix(G4AI-R3): ground live coordination prose`）建立大小写精确机器 token 守卫、未支持概念拒绝、schema 描述和 evidence-only next-step 约束；前一技术提交 `e27dd4f47bb97de0050ff635555567c81bce90c2` 保持 strict schema 子集、`1,600` cap、temperature 零和细分失败类别。`scripts/test-g4ai.ps1` 新增 `G4AI_UNSUPPORTED_CLAIM_GUARD=PASS`，并连同 G4/G3 回归通过。最终九组现行发布审计、`git diff --check`、Git 完整性与零远程在闭合提交前复验。

**额度与清理：** 本序列恰好使用五次授权请求；这次“直到成功”的明确授权优先于既有普通开发日五次上限，成功后立即停止，未继续测试。密钥只进入遮罩启动的子进程环境；每次代码重载前停止旧进程并清除环境。最终 key-bearing 服务和 localhost 诊断代理均已停止，两个位于 ignored `temp/` 的无密钥 helper 已删除，4177/4178 无监听。

**结论与停止门：** 当前 G4AI-R3 bounded-prose 路径已获得英文与简体中文 live 成功证据，且同时通过隐私、确定性不变、fresh consent、未支持概念拒绝、失败关闭和控制台验收，因此由 `LOCAL_PASS / LIVE_UNVERIFIED` 提升为严格 `PASS`。这不是 Groq 永久可用、免费额度永久存在、任意 IFC 兼容或工程正确性的承诺。严格停止在 G4AI-R3；不自动上传 GitHub，不进入部署、G5、移动端计算或视频。

---

### L-0055 — G4AI-R3 授权公开检查点与映射尾

**时间：** 2026-08-29（Asia/Hong_Kong）

**Gate：** G4AI-R3 `PASS`（11 个技术/证据文件的授权 Chrome 公开链已核验；按自引用规则闭合公开总纲与最终台账；严格停止）

**授权与精确范围：** 用户在审阅自 G4AI-R1 最终公开检查点以来的差异统计后，明确批准通过已登录 Chrome 将 13 个文件按 8 组上传到 `delongwangshu49-hub/ifc-clashtrace` 的 `main`，并允许在同一授权内只更新公开总纲与进度台账的远端 SHA 映射尾。统计总量为 `520,083` 字节；固定技术/证据内容为 11 个文件，映射尾为 `BIMCLASH_AGENT_MASTER_PLAN.public.md` 与 `PROGRESS_SYNC.md`。本地权威总纲、全部 `scripts/audit-*.ps1`、密钥、`.env.local`、原始 IFC、运行输出、缓存、依赖和部署文件继续排除。

**发布前核验：** 本地 `main` HEAD 为 `691c6f8a5359ee71cdd2519db7b72b9269a1f394`，工作树洁净，Git 远程数为 `0`；`scripts/audit-g4ai.ps1` 的 19 文件精确候选、负向集合、隐私、凭据、浏览器密钥/端点、大小、禁传范围、依赖与公开总纲等价守卫全部 `PASS`。Chrome 只读确认仓库为 `Public`、分支为 `main`，远端起点为 G4AI-R1 最终公开台账 `3cd4d61a92ff555147cefec8b7ab5b478f7a72cd`。

**授权公开技术链：** Chrome 依照目录边界形成连续提交：AI 合同三文件 `dfde9a8a483950faec84136d2d77d5519bbd7d97` → Groq 适配器 `018d68c741fba6d88108deb3e6906715f78e825d` → 工作台 UI `0b6a65eb519d9c7f25533fee334cd4c044af8de5` → G4AI-R3 测试 `20499dc9178b3943e1d80dd679596205ae81eb89` → 架构证据 `056f94520c0b80d953f0d564e6cf605c2f636eb8` → 产品证据 `0f4987ce0a347ba474c76858fcad0984b2312bfc`。有效文件数精确为 `3/1/1/2/2/2 = 11`；每笔提交的标题、本地短 SHA `691c6f8`、完整父 SHA 和正确嵌套路径均已逐笔回查，没有空提交、文件扁平化、额外文件或越权路径。

**映射与停止规则：** 本条写入本地权威总纲及其等价公开副本；公开总纲提交标题映射本地闭合提交，其远端 SHA 只登记在随后最终 `PROGRESS_SYNC.md` 中。最终公开台账 SHA 只写入一笔不改文件的本地 `sync(G4AI-R3)` 提交，不递归重发。本轮不新增 live 调用，不读取或使用密钥，不配置 Git 远程，不部署、不改变公开访问、不扩展移动端计算或 IFC 兼容承诺，不进入视频或 G5；完成映射后严格停止在 G4AI-R3。

---

### L-0056 — G5 本地技术评价与公开候选冻结

**时间：** 2026-08-29（Asia/Hong_Kong）

**Gate：** G5 `TECH_PASS_SYNC_BLOCKED`（本地技术、官方样本、Chrome 可用性、隐私与许可证审计通过；精确公开检查点尚未获行动时授权）

**强制前置与基线：** 开始前完整阅读当前 3,061 行本地权威总纲和完整进度台账，并使用 PowerShell 7.6.4 独立复核唯一工作区、项目/Git 根、`main`、HEAD `a72ca5a16726e97dab81fb62850f8a4a2f4c8869`、洁净工作树、单一工作树、零远程与 `git fsck --full`。完整性检查只报告七个已登记 dangling tree，无损坏引用、提交或 blob。G4AI、G4 与 G3 回归及 G1/G2/G3A/G3B/G3C/G3/DG/G4/G4AI 九组现行审计全部重跑通过。已登录 Chrome 只读回查公开仓库 `Public/main`、从 G4AI-R1 最终台账到 G4AI-R3 最终台账的 8 笔父链、精确 `3/1/1/2/2/2/1/1` 文件数和嵌套路径，最终公开台账仍为 `e8b3ba9d80ca8629d9397e040c7bcddf9a4b9997`。

**指标与三方一致性：** `scripts/g5-evaluate-controlled.mjs` 以冻结构造台账为权威，比较 IfcOpenShell 参考与产品 web-ifc 核心，并逐案重复五次。8 个受控案例的状态、产品预期 GUID 对和三方状态一致性均为 `8/8`；硬碰撞二元指标为 `TP=3 / FP=0 / FN=0 / TN=4`，另有 1 个预期失败关闭，precision/recall 均为 `1.00`。G3C 的精确解析与独立三角网格路线分别匹配 `9/9`，分布为 4 WARNING、2 CLEAR、2 NOT_EVALUATED 与 1 个硬碰撞抑制。40 次本地核心观测范围约 `0.22–33.04 ms`，但仅适用于微型生成模型。

**官方样本与 O-005：** 通过 buildingSMART 公开 GitHub 页面只读核验 `Certification-datasets` 的 PCERT IFC4 `Building-Hvac.ifc` 与 `Building-Structural.ifc`、源提交 `e6f1c1d80ac216e1c1d6f88d4650f13d8c8277b7` 和 CC BY 4.0。两文件只保存于 ignored `data/external/`，哈希分别为 `11a8552...2b3` 与 `68be7223...ef67`，不进入本地提交或公开候选。web-ifc 与 IfcOpenShell 均能打开两份 IFC4；IfcOpenShell 识别 `0.001` 单位换算、`5/16` 个几何项且无迭代失败，产品 GUID 为 `10/10` 与 `22/22` 唯一。样例使用毫米且 HVAC 没有 `IfcPipeSegment`；产品和 Chrome UI 均正确返回 `NOT_EVALUATED`，证据抽屉明确显示只支持无前缀米制。官方目录没有审计碰撞对真值，因此不产生准确率。D-046 据此关闭 O-005：正式支持仍限冻结 IFC4 边界，IFC4X3 继续探索。

**AI、性能与可用性：** 本地 mock 的 6 个事实保持检查全部通过，其中 5 个无配置/畸形/状态注入/数值注入/未知引用路径都返回精确本地降级模板且序列化确定性请求不变。五次同源非流式端点样本的中位首字节/完成约为 `40.7/41.1 ms`，只代表本地 mock plumbing；没有读取密钥、调用提供商或保留新 provider 正文。人工可读性复用 L-0054 已授权并通过的英/中内容审计，不伪称新盲测。Chrome 最小路径中，四案例 review pack 在 `9,685 ms` 完成并显示 `1/1/1/1`、筛选、3D 与证据；官方样例在 `3,201 ms` 失败关闭并暴露明确诊断，页面未崩溃，也未触发 AI 发送。

**实现、提交与审计：** 新增三类四个评价脚本、公开 `scripts/test-g5.ps1`、本地 `scripts/audit-g5.ps1` 与 `docs/evaluation.md`，并更新 README、提示词、许可证说明、package script、总纲和 O-005。技术提交为 `d9fb56e014c79eb360bda96828b9524a6e1dcec3`（`feat(G5): evaluate controlled and official evidence`）。带 `-RequireOfficialSamples` 的 G5 测试通过；精确 12 文件公开候选审计通过缺失/额外负向自测，绝对路径、邮箱、凭据、超 1 MiB、禁传文件和外部 IFC 命中均为 0，依赖仍精确为三项，公开总纲与本地总纲等价，Git 远程为 0。

**停止门：** 本地技术范围已完成，但总纲要求的公开检查点仍是 PASS 条件；任何 GitHub 文件提交属于外部写入，必须在行动时取得用户对精确 12 文件范围的集体授权。在授权、Chrome 上传、父链/路径/文件数回查及公开计划/最终台账 SHA 映射完成前，G5 保持 `TECH_PASS_SYNC_BLOCKED`。不配置 Git 远程，不使用 GitHub CLI/API，不上传本地权威总纲、任何审计脚本、buildingSMART IFC、密钥、本地输出或缓存；不进入 G6、部署、公开访问变更、移动计算、视频或其他阶段。

---

### L-0057 — G5 授权公开检查点与映射尾

**时间：** 2026-08-29（Asia/Hong_Kong）

**Gate：** G5 `PASS`（10 个技术/证据文件的授权 Chrome 公开链已核验；按自引用规则闭合公开总纲与最终台账；严格停止）

**授权与精确范围：** 用户在收到精确 12 文件清单、目录分组、排除项与映射尾机制后，明确要求停止本地服务并批准集体上传。授权范围只包括 10 个技术/证据文件，以及 `BIMCLASH_AGENT_MASTER_PLAN.public.md` 与 `PROGRESS_SYNC.md` 两个映射尾文件。本地权威总纲、全部 `scripts/audit-*.ps1`、buildingSMART IFC、密钥、`.env.local`、运行输出、缓存、依赖、部署文件和其他路径继续排除。

**授权公开技术链：** 已登录 Chrome 在 `Public/main`、G4AI-R3 最终公开台账 `e8b3ba9d80ca8629d9397e040c7bcddf9a4b9997` 上按目录形成连续提交：`scripts` 五文件 `47232f08e4b786a8865312083cb047f2067d15f1` → `docs` 两文件 `af10488c4a05b5def2dec255390bd68693228be4` → 根目录产品证据三文件 `027baee300bdcd2a043769922b49931778321cf0`。有效文件数精确为 `5/2/3 = 10`；每笔提交的标题、完整父 SHA、文件数与正确嵌套路径均已逐笔回查，没有空提交、文件扁平化、额外文件或越权路径。

**映射与停止规则：** 公开总纲提交 `1f9b7c300f8c44cd9bd4c62e344a52d9bd45ec71` 映射本地闭合提交 `8b5fa416e4f4b57dd17f9a409e3c5ea95909c436`；最终公开台账提交 `be4e186cbef2796dd83902fe7788c9c65d2107b5` 已由 Chrome 回查，并只登记在不改文件的本地 `sync(G5)` 提交 `27a9b78c89afe7f13ad1d01b0df1996baebc8477` 中，没有递归重发。G5 的本地技术证据、公开链与映射尾均已闭合为 `PASS`；严格停止在 G5，不进入 G6、部署、公开访问变更、移动端计算、视频或其他阶段。

---

### L-0058 — G5-R1 严格限定一致性修复（本地）

**时间：** 2026-08-29（Asia/Hong_Kong）

**Gate：** G5-R1 `TECH_PASS_SYNC_BLOCKED`（本地修复与验证准备完成；公共修复检查点尚待行动时确认）

**授权与范围：** 用户批准一次严格限定的 G5 一致性修复。范围只包括：把不可解析的 G5 技术完整 SHA 纠正为本地可解析提交 `d9fb56e014c79eb360bda96828b9524a6e1dcec3`；移除 `docs/evaluation.md` 对原 G5 公共检查点仍 pending 的过期陈述；让官方样本 `PASS` 同时要求零几何迭代失败和全部产品均有唯一 GlobalId；在公开测试中锁定这些条件；让本地 G5 审计验证提交可解析性、祖先关系、原最终空同步提交、证据状态与完整映射。不得改写 G3/G4 权威结果、修改检测算法、发起 AI/live、引入依赖或进入 G6。

**独立复核：** PowerShell `7.6.4`；唯一项目/Git 根和单一工作树；`main` HEAD `27a9b78c89afe7f13ad1d01b0df1996baebc8477`；工作树起始洁净；Git 远程数 `0`；4173/4177/4178 均无监听；`git fsck --full` 无损坏引用、提交或 blob，仅有七个既有 dangling tree。原错误完整 SHA 不可解析，真实 G5 技术提交、闭合提交与空同步提交均可解析并位于当前祖先链。Chrome 只读回查原 G5 五组提交时间为 2026-08-29 16:41:57–16:48:43 +08:00，父链、文件数、路径和 `Public/main` 保持通过。

**公开边界：** 后续拟公开修复只允许 `scripts/g5-evaluate-official.py`、`scripts/test-g5.ps1`、`docs/evaluation.md`、`BIMCLASH_AGENT_MASTER_PLAN.public.md` 与 `PROGRESS_SYNC.md`。本地权威总纲、`scripts/audit-g5.ps1`、全部其他审计脚本、外部 IFC、凭据、输出、缓存、依赖与后续阶段内容继续禁止上传。任何 GitHub 提交仍须在实际提交前取得一次精确、集中的行动时确认。

**当前结论：** 原 G5 指标和产品范围不变；G5-R1 在公共修复链与映射尾闭合前不得标记为 `PASS`。

---

### L-0059 — G5-R1 行动时授权与公开技术修复链

**时间：** 2026-08-29（Asia/Hong_Kong）

**Gate：** G5-R1 `PASS`（公开技术修复链已验证；公开总纲、最终台账与空本地同步映射尾按自引用停止规则随后闭合）

**行动时授权：** 用户在收到精确五文件、四组目录提交、禁止上传项和最终空本地同步机制后明确回复“确认”，一次性批准既定 G5-R1 公开修复范围；没有扩大到本地权威总纲、任何 `scripts/audit-*.ps1`、外部 IFC、密钥、输出、缓存、依赖、AI/live、部署、公开访问变更、移动计算、视频、G6 或其他阶段。

**已验证公开技术链：** 已登录 Chrome 在原 G5 最终公开台账 `be4e186cbef2796dd83902fe7788c9c65d2107b5` 之后连续提交 `scripts/g5-evaluate-official.py` 与 `scripts/test-g5.ps1` 两文件 `102abe284507a27730fb4ad925cad3dbff2f9d17`，再提交 `docs/evaluation.md` 一文件 `82418256f01b74d3c9d922724d4c046b82ba9707`。两笔提交的父链、`Public/main`、完整 SHA、精确文件数 `2/1`、嵌套路径与变更内容均已逐笔回查；上传时间为 2026-08-29 19:10:39–19:12:48 +08:00。

**映射与停止规则：** 当前公开总纲仅登记已经闭合的技术修复链；其自身公开 SHA 与随后最终 `PROGRESS_SYNC.md` SHA 只能在各自提交生成后向后映射，不递归重发。最终公开台账 SHA 必须由一个不改文件的本地 `sync(G5-R1)` 提交登记。全部映射完成后仍严格停止在 G5。

---

### L-0060 — G6 本地部署候选、私有预览准备与发布审计

**时间：** 2026-08-29（Asia/Hong_Kong）

**Gate：** G6 `TECH_PASS_EXTERNAL_AUTH_BLOCKED`

**独立前置复核：** 完整读取当前权威总纲与公开同步台账；PowerShell `7.6.4`、唯一项目/Git 根、单一 `main` 工作树、HEAD `0675801ff9c6bc2b0f65b0826525060e7a76d06c`、Git 远程数 `0`、`git fsck --full` 无损坏对象。G5-R1 本地测试/审计重新通过，Chrome 只读核验公开链 `102abe284507a27730fb4ad925cad3dbff2f9d17` → `82418256f01b74d3c9d922724d4c046b82ba9707` → `383e740fbe456b53fcf5ab20f4374942373c8342` → `1d4380806f741563d4179395ff76e91c83485940` 的标题、父链、文件数和路径均通过；未发生外部写入。

**实现与本地验证：** 增加 Vite 三页构建、Sites 元数据插件、Worker-compatible 同源 AI 边界、忽略目录下的精确部署输入暂存、44 项锁定依赖许可证清单、第三方通知、G6 架构/隐私/提供商审计和公开 G6 测试。构建输出含 `/`、`/app/`、`/development/`、16 个项目生成 IFC、`web-ifc.wasm` 与 `dist/server/index.js`，不含 source map。无密钥状态/解释、异源拒绝和静态回退测试通过；本地预览五条路由通过；`npm audit` 为 `0`；G4AI/G4/G3 与 G5 回归全部通过。四张既有 `.png` 预览实际为 JPEG 字节，已在不改变画面语义的情况下重编码为真实 PNG；无 EXIF/text 元数据且均低于 1 MiB。

**隐私、许可证与 AI 审计：** 浏览器 bundle 无 Groq 端点、认证头、密钥读取或 source map；服务端只从可选部署 secret 读取 `GROQ_API_KEY`，当前未配置托管密钥，项目代码无请求/响应日志。AI 继续默认关闭、逐次字段预览与新同意、最多六条精确最小字段，任何错误失败关闭且不改机器记录。Groq 官方模型、免费层、数据保留/ZDR 与云服务条款在 2026-08-29 重新核验并作为时点边界记录。仅 16 个 CC0 项目生成 IFC 和 MPL-2.0 WASM 进入候选；buildingSMART 样本、私人 IFC、外部数据、密钥、`.env.local`、缓存、输出、本地总纲与全部 `scripts/audit-*.ps1` 均排除。

**外部停止门：** 未创建 Sites 项目、临时写凭据、源版本、私有部署 URL，未配置托管 AI 密钥，未改变任何访问策略，也未执行 GitHub 写入。下一步必须先向用户集中列明 Sites 所有者私有部署与精确 26 文件 GitHub 候选范围并取得一次行动时集体授权；随后还需用户通过私有 URL 人工验收。授权、私有部署回查、人工验收、G6 本地提交/公开检查点及映射尾全部完成前不得把 G6 标记为 `PASS`，不得进入 VG/G7 或公开 Sites。

---

### L-0061 — G6 所有者私有 Sites 部署与回查

**时间：** 2026-08-29（Asia/Hong_Kong）

**Gate：** G6 `PRIVATE_PREVIEW_USER_ACCEPTANCE_PENDING`

**行动时授权：** 用户明确批准完成除 GitHub 上传外的操作。授权只覆盖唯一 Sites 项目、项目 ID 写入、完整重测/审计、本地技术提交、短期凭据脱敏源推送、版本保存、所有者私有部署与回查；不包括 GitHub、托管 AI 密钥、Groq live、公开访问、额外用户/组、VG、G7、移动计算或视频。

**本地与脱敏源：** 完整 G6 测试、G4AI/G5 回归、`-RequireSitesProject` 发布审计和 diff 检查通过。本地权威技术提交为 `1fc427e96b681481c8a2502305e07aaf721505f9`（`feat(G6): package private deployment candidate`）。为严格禁止发布本地总纲和全部审计脚本，另从该提交构造无父历史的脱敏源提交 `011dffe72dfcc2c8081299638c1e342cb2e4653d`，逐树确认 13 个禁传路径全部为 0，再以每命令短期授权头推送；本地 Git 仍为 0 个远程。

**保存与部署：** 使用官方 Sites 打包助手生成仅含 `dist/` 的归档，46 个目录/文件条目中禁传命中为 0；版本 1 绑定精确脱敏源 SHA 并保存成功。部署前和部署后均回查为当前用户 `owner`、`custom` 访问、恰好 1 个允许账号、0 个工作区组、0 个租户组、0 个外部访客；因此使用私有部署路径。部署状态为 `succeeded`，托管环境变量键数为 0，未设置 `GROQ_API_KEY` 或其他密钥。

**停止门：** 私有 URL 只交给用户人工验收，不在公开候选中登记。用户接受前不把 G6 标记为 `PASS`；GitHub 上传仍未获授权，Sites 仍不得公开，不进入 VG/G7、移动计算或视频。

---

### L-0062 — G6 私有预览根路径 404 诊断与本地修复

**时间：** 2026-08-29（Asia/Hong_Kong）

**Gate：** G6 `PRIVATE_PREVIEW_USER_ACCEPTANCE_PENDING`

**事故证据：** 用户提交的私有预览截图显示 `ERR_HTTP_RESPONSE_CODE_FAILURE`。Sites 项目回查仍为 `active`、当前用户 `owner`、`custom`、恰好 1 个允许账号、0 个组和 0 个外部访客；生产 Worker 日志中的已认证 `GET /` 请求稳定返回 `404`，执行 outcome 为 `ok`。因此排除账号未授权和 Worker 崩溃，定位为部署产物布局错误。

**根因与修复：** 版本 1 同时包含静态文件和 `dist/server/index.js`，但静态文件位于 `dist/` 根而不是 Sites/Cloudflare 资产绑定约定的 `dist/client`，导致 Worker 环境没有 `ASSETS`，落入显式 404。D-050 将静态产物移至 `dist/client`，保留 Worker 在 `dist/server/index.js`；本地 preview 也固定从 `dist/client` 提供内容，公开测试锁定该目录契约。

**本地验证：** G6 构建、根页/工作台/开发页/WASM/生成 IFC 五路由、44 项许可证、零已知 npm 漏洞、媒体元数据、无密钥 AI 失败关闭、G4AI/G4/G3 与 G5 完整回归全部通过。AI 数据最小化、默认关闭、逐次同意、确定性结果权威和失败关闭均未改变。本地技术修复提交为 `02cf9709ed29b3d02e9c2e3a3377f6d73bf5c3c4` 与构建清理守卫提交 `2cf9b06f50f49d68dc8085666a7687a16d42cda2`。

**归档与部署：** 首个修复归档回读发现版本 1 遗留的 `dist/` 根静态副本与新 `dist/client` 并存，文件和体积异常翻倍；保存的版本 2 因此在部署前拒绝。随后构建准备显式清除可再生 `dist`，测试锁定根目录只允许 `.openai`、`client` 与 `server`。最终归档有 47 个目录/文件条目、0 个禁传项、0 个意外根文件；无父脱敏源提交 `a10f0c1d627be020fecf6ef1a684ec6e41c45e81` 含 126 个文件且本地总纲与全部 12 个审计脚本命中为 0。版本 3 私有部署状态为 `succeeded`。

**最终回查与停止门：** Sites 为 `active`、当前 live 版本 3、当前用户 `owner`、`custom`、仅 1 个允许账号、0 个工作区/租户组、0 个外部访客、0 个托管环境变量。自动浏览器受 URL 安全策略阻止刷新私有域名，未绕过该策略；因此保留用户人工刷新与验收门。用户接受前 G6 不得标记 `PASS`。GitHub、公开 Sites、托管密钥、Groq live、VG/G7、移动计算和视频继续禁止。

---

### L-0063 — G6 私有预览验证入口人工反馈与本地修复

**时间：** 2026-08-29（Asia/Hong_Kong）

**Gate：** G6 `PRIVATE_PREVIEW_USER_ACCEPTANCE_PENDING`

**人工证据：** 用户在版本 3 开发页实际选择“验证方法”，确认其 GitHub 目标没有可用内容，并要求考虑替换。该反馈也证明版本 3 页面已可加载；不把页面可访问等同于 G6 最终接受。

**采用修复：** D-051 将入口改为“受控验证 / Controlled validation”，以新标签打开同一私有部署的 `/app/#controlled-review`。工作台为受控示例卡增加稳定锚点，直接提供 C01/C03/C05/C08 冻结案例和真实浏览器几何路径，不依赖 GitHub 是否已同步。公开测试同时要求新目标存在并禁止旧空文档路径回归。

**本地验证与候选边界：** `scripts/test-g4.ps1` 与完整 `scripts/test-g6.ps1` 均通过，五路由、Worker、无密钥 AI 失败关闭、G4AI/G4/G3/G5 回归保持通过。G6 基线后的精确公开候选随本次 3 文件修复由 26 项更新为 29 项；本地总纲与全部 `scripts/audit-*.ps1` 仍严格排除，GitHub 未写入。本地权威修复提交为 `b6db4ec13946ddab1f842c88516e20c0a6f183df`。

**脱敏源、归档与部署：** 从本地修复提交构造无父脱敏源 `20f06a22cb427f8a995b9f0077c731bb0ae0dfba`，含 126 个文件且本地总纲/全部审计脚本命中为 0；官方助手归档有 47 个条目、0 个禁传项和 0 个意外根文件。版本 4 绑定该精确源和归档，所有者私有部署状态为 `succeeded`。

**最终回查与停止门：** Sites 为 `active`、当前 live 版本 4、当前用户 `owner`、`custom`、仅 1 个允许账号、0 个工作区/租户组、0 个外部访客、0 个托管环境变量。等待用户刷新开发页并复验“受控验证”入口；用户接受前 G6 不得标记 `PASS`。GitHub、公开 Sites、托管密钥、Groq live、VG/G7、移动计算和视频继续禁止。

---

### L-0064 — G6 私有预览人工验收与 GitHub 候选整理

**时间：** 2026-08-29（Asia/Hong_Kong）

**Gate：** G6 `PRIVATE_PREVIEW_ACCEPTED_GITHUB_AUTH_PENDING`

**人工验收：** 用户在版本 4 私有站点完成基本使用后明确反馈“基本使用体验我觉得没问题”，因此 D-052 关闭私有预览人工验收门。该反馈不等于批准外部提交；GitHub 仍未写入，Sites 仍保持所有者私有。

**精确公开候选：** G6 基线后的 29 个唯一路径保持不变。实际技术链按 GitHub 网页正确目录形成 9 笔 27 文件：`.openai` 1、`app` 根入口 1、`app/ai` 1、`app/ui/previews` 4、`development` 1、`docs` 5、`scripts` 6、`worker` 1、根目录技术/证据 7；技术链回查后再分别提交 `BIMCLASH_AGENT_MASTER_PLAN.public.md` 1 文件和 `PROGRESS_SYNC.md` 1 文件闭合映射尾。每一项均已在本地公开候选审计中通过路径、PII、凭据、尺寸、许可证和计划等价性检查。

**禁止上传：** 本地权威 `BIMCLASH_AGENT_MASTER_PLAN.md`、全部 `scripts/audit-*.ps1`、`dist`、`outputs`、`node_modules`、`data/external`、`.env*`、凭据、浏览器状态、缓存和第三方 IFC 继续排除。不得配置 Git 远程，不得使用 `gh`、GitHub API 或命令行远程。

**停止门：** 用户已对精确 29 文件给予一次行动时集体授权。27 文件技术链已通过；下一步仅允许按同一授权完成公开总纲和最终台账各 1 文件映射尾、逐笔回查与空本地 `sync(G6)` 闭合。完成前 G6 保持非 `PASS`，不得进入 PG/VG/G7 或公开 Sites。

---

### L-0065 — 0.4.0 展示就绪范围修订

**时间：** 2026-08-29（Asia/Hong_Kong）

**Gate：** G6 `PRIVATE_PREVIEW_ACCEPTED_GITHUB_AUTH_PENDING`（计划修订；PG 尚未开始）

**用户发现与目标：** 用户指出当前网站仍有内容停留在过去阶段，典型证据是研发历程页只展示到 DG；要求在视频制作前全量校准网站进度，允许为版面和视频叙事提前准备 G7 文案，考虑成片回嵌首页、GitHub 首页动效 Logo，以及可供人工网站试用的近真实工程文件样例。

**总纲修订：** 文档升为 `0.4.0`，在 G6 与 VG 之间新增强制 `PG` 展示就绪 Gate：PG-C 建立页面主张台账并覆盖三页、README、双语/双风格/亮暗/桌面宽度与链接；PG-B 复用 DG 已批准 Logo，完成 GitHub 安全动效格式试验、静态终态与可访问性/体积回查；PG-E 建立工程情境化但非真实客户项目的双 IFC、哨兵预期和用户端到端 UAT。视频阶段拆为 G7A 成片 QA 与 G7B 首页回嵌/最终部署复验。

**研究诚信边界：** 可以提前编写从当前 Gate 之后直至项目结束的全部后续阶段文案与组件，但只能隐藏或明确标为 `DRAFT/PLANNED`、“计划中/即将开展/尚未开始”。在真实证据与必要授权形成前，不得伪造完成状态、日期、指标、链接、验收、公开可用性、权限或证据；预写不启动阶段、不通过 Gate、不替代用户验收，不授权外部写入、部署或公开访问，也不得绕过停止门。页面、README、视频旁白和字幕遵守同一规则。

**访问与回退：** 最终网站只需保证香港教授团队可打开，但公开访问变化仍须用户明确授权。G7B 必须执行香港地区未登录/无痕访问复验；首页视频默认不自动播放、懒加载，并提供字幕/文字稿与直接链接回退。Logo 动效不稳定时降级为已批准静态终态；视频回嵌失败时回退到不含视频的上一稳定首页。

**验证与本轮边界：** 本轮只修订本地/公开总纲与 `PROMPTS.md`，未修改网站、README、样例、Logo 或视频，未执行 GitHub 写入、Sites 访问变化、部署、Groq live 或视频制作。文档修改后已重新运行 G6 发布审计：候选仍为精确 29 个唯一路径，路径/邮箱/电话/凭据/大文件命中均为 0，媒体、公开计划等价性、零 Git remote 与单工作树检查通过。候选路径数量不变，但文档内容与哈希须以本次修订后的版本为准，不沿用旧哈希。

**下一步：** 先完成 G6 更新候选重审并请求精确范围的 GitHub 行动时集体授权；G6 `PASS` 后按 `PG-C → PG-B → PG-E → VG → G7A → G7B → G7` 执行。

---

### L-0066 — G6 GitHub 技术链授权、目录安全拆分与逐笔回查

**时间：** 2026-08-29 23:27–23:36 +08:00

**Gate：** G6 `PASS`（公开总纲、最终台账与空本地同步按自引用停止规则闭合）

**行动时授权与前置：** 用户明确回复“确认按上述 29 文件方案上传”，一次性授权 0.4.0 重审后的 27 文件技术链、公开总纲与最终台账各 1 文件，以及逐笔网页回查；不授权额外路径、Git 远程、CLI/API GitHub、Sites 公开、托管密钥或后续阶段。Chrome 确认当前账号拥有仓库，仓库为 `Public/main`，起始远程 HEAD 精确为 G5-R1 台账 `1d4380806f741563d4179395ff76e91c83485940`。

**目录能力调整：** 首笔 `.openai/hosting.json` 发布并回查后，GitHub 上传控件只支持多文件而不支持目录选择（`multiple=true`、`webkitdirectory=false`）。为禁止 `app/ai` 与 `app/ui/previews` 被扁平化，D-057 将同一 `app` 六文件组拆为根入口、AI 客户端和四张预览图三笔；授权的 29 个唯一路径及内容不变。技术标题保留已展示的本地上传计划短 SHA `2469294`；实际 0.4.0 权威内容来自其后已审计的本地计划提交 `4910437cdcdfdae4b97c38ad7aa760bf4061dbb2`，两者关系在本日志中显式登记，不伪称标题短 SHA 是最终内容提交。

**已验证技术链：** `76ea248846b58b85a3eecd6317c10ed8a7702603` → `d3c181e45d806eedc798b31f4f5b43616ac46e75` → `0e102d3a15a45a074c2a15b1801b7895baa8aa49` → `b554932610e5a3875aa14f9d0c8674ce8da22c05` → `bab13eb1eb8ea7f19dc92adcd10af9e6bec37c0f` → `42c6ef322a49dfff506c9a0222a61e47380ca151` → `be21b3dd10c25f763847739ec70fc06272f5a997` → `294616135dc00bf40fd0360b97e7f59008649fac` → `e7217d65cfe43fc965c8c0a192656113a3a11aaf`。九笔有效文件数精确为 `1/1/1/4/1/5/6/1/7 = 27`；完整父 SHA、正确嵌套路径、文件数与 `Public/main` 均逐笔回查通过。

**映射、排除与停止门：** 本条作为 G6 `PASS` 公开总纲候选先发布；其远端 SHA 只登记在随后最终 `PROGRESS_SYNC.md` 中，最终公开台账 SHA 只写入一笔不改文件的本地 `sync(G6)` 提交，不递归重发。本地权威总纲、全部审计脚本、构建/输出/依赖目录、外部 IFC、密钥、浏览器状态和缓存均未上传；本地 Git 仍为零远程。只有公开总纲、最终台账逐笔回查及空本地闭合全部完成后，运行中的 Gate 状态才实际闭合为本候选所声明的 `PASS`；随后严格停止在 G6，不进入 PG/VG/G7 或公开 Sites。

---

### L-0067 — G6-R1 有限公开证据一致性修复与映射闭合

**时间：** 2026-08-30（Asia/Hong_Kong）

**Gate：** G6-R1 `PASS`；基础 G6 保持 `PASS`

**审计结论与用户授权：** 只读复核确认 G6 构建、Worker、许可证、媒体、隐私扫描、AI 失败关闭、G4AI/G4/G3/G5 回归、29 文件候选、Git 完整性和 Sites 所有者私有版本 4 均通过；同时发现 README 与隐私报告仍把已完成的私有预览写成待审核，最终台账保留提交前将来时，并含一个字面量 `+##` 标题。用户随后批准一次有限修复工作。

**严格范围：** 只更新 `README.md`、`docs/g6-privacy-license-audit.md`、`PROGRESS_SYNC.md`、本地/公开总纲、`PROMPTS.md` 与本地专用 `scripts/audit-g6.ps1`。README 和隐私报告明确区分已完成的所有者私有版本 4 验收与仍待独立决定的 O-006 公开访问；台账把基础 G6 最终远端 `afa603f4b0d110df8d6d33f18c743ec2aee2bb42` 和空本地同步 `efcd39526cd92210f6b2797ec0d1adb96fb9b5ad` 改写为已完成事实并修复标题。审计新增过期措辞、正确验收/公开边界、Markdown 标题、最终 SHA、祖先关系、空提交树和提交消息守卫。

**不变边界：** 不修改应用代码、确定性状态/规则/证据、AI 字段或提供商契约、依赖、IFC、媒体、构建产物、部署、Sites 访问、托管变量或任何后续 Gate；基础 G6 技术与产品结论不重算、不扩展。`scripts/audit-g6.ps1` 和本地权威总纲继续禁止公开。

**公开修复链与映射：** 本地权威修复提交为 `751ead7628426dbffb763ad12440cb43253ece2f`。用户对精确 5 路径、4 笔 Chrome 方案给予行动时授权后，Chrome 已验证 `ebe1a1062dfc3b2d8df3f676f298d66bfd97428a`（父 `afa603f4b0d110df8d6d33f18c743ec2aee2bb42`，仅隐私审计）→ `0a93db9d5e24104bd791f16165b415c0f2383490`（父 `ebe1a1062dfc3b2d8df3f676f298d66bfd97428a`，仅 README 与 PROMPTS）两笔公开修复，文件数精确为 `1/2`，路径与 `Public/main` 均正确。本条作为 G6-R1 `PASS` 公开总纲候选发布；其远端 SHA 只登记在随后最终 `PROGRESS_SYNC.md`，最终台账 SHA 只写入一笔不改文件的本地 `sync(G6-R1)` 提交，不递归重发。

**停止门：** 只有公开总纲、最终台账逐笔回查及空本地同步全部完成后，运行中的 G6-R1 才实际闭合为本候选声明的 `PASS`。本地权威总纲与 `scripts/audit-g6.ps1` 未上传；本地 Git 继续保持零远程。闭合后严格停在 G6，不进入 PG/VG/G7，不公开 Sites、不改访问权限、不配置托管密钥。

---

### L-0068 — PG-C 全站内容与进度口径技术闭合

**时间：** 2026-08-30 12:41–12:50 +08:00

**Gate：** PG-C `TECH_PASS_EXTERNAL_SYNC_PENDING`；PG-B、PG-E、VG 与 G7 系列保持 `NOT_STARTED`

**治理扩展：** 按用户要求把 D-054 从仅允许预写 G7 扩展为允许预写当前 Gate 之后直至项目结束的全部后续阶段文案与组件。预写必须隐藏或明确标为 `DRAFT/PLANNED`、计划中或尚未开始；不得伪造完成状态、日期、指标、链接、验收、公开可用性、权限或证据。预写不启动阶段、不通过 Gate、不替代用户验收，不授权外部写入、部署或公开访问，也不得绕过停止门。

**内容闭合：** 新增 `docs/content-claim-ledger.md`，覆盖首页、工作台、研发历程页、README、导航/页尾、元数据、错误/空状态和演示截图八类表面。研发历程从 DG 更新到最新闭合 G6-R1，并把 PG-C 显示为当前审计；PG-B、PG-E、VG、G7A、G7B 与 G7 均以双语 `PLANNED`/尚未开始展示。Sites 明确保持仅所有者私有，公开 GitHub 与私有 Sites 不混淆。历史研发页截图标为 G4 时点历史资料；CLASH precision/recall 从易误读的 `100 / 100` 校准为证据值 `1.00 / 1.00`。

**自动与浏览器验证：** `scripts/test-pg-c.ps1` 对 8/8 表面、12/12 已闭合 Gate、6/6 计划阶段、3/3 双语元数据、空目标、未来冒充完成、中英文不一致、历史截图标记和 source/build 事实一致性均为 `PASS`；完整 G6、G4AI、G4、G3 与 G5 回归通过。当前 Chrome 插件使用原生 viewport override 完成三页 × 1024/1280/1433 px × 简中/英语 × 大众体验/工程极简 × 亮/暗共 72/72 状态检查：横向溢出、可见文字裁切、空目标、Gate/元数据错配均为 0，控制台错误为 0。工作台唯一静态双语差异是 `#run-description` 被受信任实时运行状态文案按设计覆盖，现以 `data-runtime-copy="true"` 明确标记并单独验证。

**精确范围与停止门：** 当前公开候选为 15 个唯一路径；本地权威总纲与 `scripts/audit-pg-c.ps1` 继续只留本地。尚未执行 GitHub 写入、Sites 保存/部署、访问或权限变化、托管密钥、Groq live、PG-B、PG-E、VG、G7、移动端计算或视频。PG-C 只有在本地提交、精确 Chrome GitHub 检查点、所有者私有 Sites 同事实候选回查、映射尾与空本地同步全部完成后才可标为 `PASS`；所有外部写入必须在行动时取得用户对精确范围的集体授权。

**本地技术检查点：** `ae6d0133426460ab822b4df2e5ad77765f28e729`（`step(PG-C): align site claims and progress`）精确包含 17 个文件：15 个已审计公开候选，以及继续禁止公开的本地权威总纲与 `scripts/audit-pg-c.ps1`。提交后工作树洁净、远程数为 0；公开检查点与所有者私有 Sites 候选仍待行动时授权。

---

### L-0069 — PG-C 外部候选验证与 PASS 闭合

**时间：** 2026-08-30 12:53–13:06 +08:00

**Gate：** PG-C `PASS`；严格停止在 PG-C，PG-B、PG-E、VG 与 G7 系列保持 `NOT_STARTED`

**行动时授权：** 用户批准 D-062 的精确外部动作，并要求全部完成后再展示需查看内容。GitHub 写入全程仅使用登录态 Chrome 插件；未使用 Windows 控制、Git 远程、GitHub CLI 或 API。

**公开候选：** Chrome 把 15 个已审计路径形成从既有 `a36214031161e8dcca3fdd6d5e568c849d301ffb` 连续延伸的 8 笔 `Public/main` 提交，文件数为 `3/1/4/1/1/3/1/1`，技术与映射尾依次落在 `c6f557d4bfb2dc50e5d0b31fece3168e4d00e1ac`、`d65da0f705906f86e408c77ef2786ea31fe1ec4c`、`0d3312ac946d467a082509cf8fc08cf5b02fbb04`、`ad0874ff9483776d109aee529c58fb1e366a67da`、`5f88d7cb1be29f19c18f579fcb9cce3d31e4c7bb`、`6caf72479dd8296c4c46cd7b0d0a6bffd8299432`、`522963ca123d33864f88fe6cb2fe488d30a072a9` 与 `728d1717b5c80bd0dd4ba54e5bd4ce20747770bc`。本地权威总纲与全部审计脚本未上传。

**所有者私有候选：** 从排除本地总纲与全部 `scripts/audit-*.ps1` 的 128 文件脱敏源提交 `a97218591a1989f6f3eb9629fabd7f78614d9968` 构建并保存 Sites 版本 5；部署 `appgdep_6a93b93fda4481918121513080cf1452` 成功。连接器复核为 `custom`、当前用户 `owner`、恰好 1 位获准所有者、0 组、0 外部访客、0 环境变量。Chrome 首次选到未获准账户时按设计被拒绝，随后切换至获准所有者账户成功加载主页与研发进程页；未改变任何访问策略。

**闭合规则：** 自动回归、72/72 Chrome 状态、本次连续公开候选和所有者私有 Sites 同事实候选均已通过，PG-C 因而提升为 `PASS`。仅再发布 D-062 授权内 6 个状态闭合路径，并保存/部署同内容的最终所有者私有版本；这些动作产生的最终远端 SHA、Sites 版本与部署 ID 只登记在随后一个不改文件的本地 `sync(PG-C)` 提交中，不递归重发。全程不进入任何后续阶段。

**本地 PASS 检查点：** `50716c65a988eaab1247e52bfc8c0efacffdd0dd`（`close(PG-C): register verified external candidate`）精确包含 6 个状态闭合公共路径及继续只留本地的权威总纲与 `scripts/audit-pg-c.ps1`；公开尾提交标题映射该检查点。

---

### L-0070 — PG-C 新会话入口与亮色图注定向修复

**时间：** 2026-08-30 13:20–13:55 +08:00

**Gate：** PG-C 保持 `PASS`；本项为已闭合 Gate 内的定向修复，后续阶段仍为 `NOT_STARTED`

**实现：** 三页静态入口统一以 `en`、`mainstream`、`light` 为初始属性。偏好存储从跨标签页持久化改为当前标签页会话；新标签页首次命中 `/app/` 或 `/development/` 会返回主页，同一标签页内的站内链接改为原页导航，因此用户仍可在一次访问中保留主动选择。主页 A/B 图注使用 `#11161b` 深色底及高对比浅色文字。

**验证：** `scripts/test-g4.ps1` 新增新会话入口、同页导航、英语/大众/亮色默认值、禁止内部新标签页和 A/B 图注 WCAG 4.5:1 对比度守卫；完整 `scripts/test-pg-c.ps1` 回归通过。浏览器实测确认新标签页深链返回主页、初始控件显示 Popular experience / English / Light、同标签页进入工作台正常，且 A/B 实际计算样式与修复值一致。

**边界：** 本修复不改变确定性计算、AI 契约、依赖、许可、Sites 访问策略或任何后续 Gate。所有者私有 Sites 可更新为同内容版本；GitHub 未获本次授权，不执行 GitHub 写入。

---

### L-0071 — 0.4.1 取消首页视频回嵌并改用 YouTube 直链

**时间：** 2026-08-30（Asia/Hong_Kong）

**Gate：** PG-C 保持 `PASS`；本项只修订后续计划，PG-B、PG-E、VG 与 G7 系列仍为 `NOT_STARTED`

**用户决定：** 用户重新评估测试提交要求后认为把成片内嵌项目首页会徒增工作量且收益有限，明确要求修订总纲，删除成片回嵌首页步骤，改为 YouTube 上传相关操作。

**0.4.1 修订：** D-064 取代 D-056 的首页回嵌部分，但保留“G7A 成片 QA 通过后才能外部发布”的顺序。G7B 改为 YouTube `Unlisted` 上传与提交直链复验：上传前检查频道身份、标题/说明/封面/评论/语言/可见性；只上传已确认 MP4；上传并人工校对简体中文与英文字幕；等待处理与版权/政策检查；登记 SHA-256、视频 ID、watch URL 和时间；以香港 Chrome 无痕/未登录及窄屏视角复验；再把直接链接写入 README 和邮件材料。

**明确删除：** 最终视频不内嵌项目首页；G7B 不修改首页、不增加播放器、不做相关样式/性能回归，也不因视频执行 Sites 重部署。网站是否公开由 O-006 在 G7 独立决定，不与 YouTube 上传绑定。

**权限与隐私边界：** `Unlisted` 是持链接可见而非严格私密，链接可被转发；该边界必须在上传前由用户知情确认。YouTube 频道身份、视频上传与可见性设置均属于外部状态变化，必须在 G7B 行动时展示精确配置并取得授权。本轮未上传 YouTube、未改网站/README、未执行 GitHub 写入、Sites 部署/权限变化或视频制作。

**验证：** 本地/公开总纲脱敏等价性通过，`git diff --check` 通过；`scripts/test-pg-c.ps1 -SkipRegression` 连带 G6 构建/私有本地预览、Worker、媒体、AI 失败关闭与 PG-C 8/8 表面、13/13 已闭合 Gate、6/6 计划 Gate、双语元数据和未来完成状态守卫全部通过。计划修订未改变产品构建结果。

**下一步：** 项目仍严格停在 PG-C；后续顺序保持 `PG-B → PG-E → VG → G7A → G7B → G7`，其中 G7B 只负责 YouTube 与直链交付。

---

### L-0072 — PG-C-R2 共享主页重置、功能页顶部与审计修复

**时间：** 2026-08-30（Asia/Hong_Kong）

**Gate：** `PG-C-R2 PASS`；本地、所有者私有 Sites 与 GitHub `main` 同步均已通过，后续阶段仍为 `NOT_STARTED`

**用户澄清：** D-065 取代 D-063 的入口解释。审查团队收到的是主页根链接，因此无论同标签页再次进入或新标签页点击，每次加载 `/` 都恢复大众体验、英语、亮色。站内“Launch app”“Development log”及跨页工作台入口恢复新标签页；合法的功能/研发页新标签不再被重定向回主页。

**滚动根因与修复：** 工作台初始化调用 `chooseExample()`，其无条件聚焦 `run_checks`，浏览器因此把新页面滚到下方。初始化现改为 `chooseExample({ focusRun: false })`，只有用户主动选择示例时才移动焦点；非 hash 的工作台/研发页同时使用手动滚动恢复并在 `pageshow` 回到 `(0, 0)`，`#controlled-review` 等明确锚点不受影响。

**审计修复：** 偏好写入在 session storage 被禁用时不再中断控件应用。新增可执行入口测试，以真实内联 bootstrap 配合正常/拒绝存储环境验证主页重置、顶部进入和 hash 保留；对比度守卫从 CSS 解析实际声明。PG-C 审计不再把旧基线以来的所有后续合法提交误判为范围污染，改为当前内容审计加可选单提交范围检查。

**内容一致性：** 研发页的 G7B `PLANNED` 文案同步 D-064，改为 YouTube `Unlisted` 上传、双语字幕与香港未登录直链复验，并明确不回嵌首页。主页、README、主张台账与进度记录同步 D-065；A/B 高对比修复保持有效。

**外部闭合：** 用户对精确 14 个公开路径给予一次集体授权后，仅通过已登录 Chrome GitHub 网页完成上传。技术链顶点为 [`40c7270b6fc9b56f6976b938297d2b475eef7e39`](https://github.com/delongwangshu49-hub/ifc-clashtrace/commit/40c7270b6fc9b56f6976b938297d2b475eef7e39)；逐文件编辑差异复核为 `14/14` 一致，本地权威总纲与 `scripts/audit-pg-c.ps1` 均在公开仓库返回 `File not found`。Sites 保持版本 8、所有者私有、仅 1 个允许账号、0 个组、0 个外部访客和 0 个环境变量；未改变访问权限。

---

### L-0073 — PG-B 浅色品牌动效与 README 本地候选

**时间：** 2026-08-30 14:30–15:20 +08:00

**Gate：** `PG-B TECH_PASS_EXTERNAL_SYNC_PENDING`；最新闭合 Gate 仍为 PG-C，PG-E/VG/G7 系列保持 `NOT_STARTED`

**用户输入与边界：** 用户提供浅色产品 Logo PNG 和既有合作 GIF，要求读取动效逻辑、制作相似 GIF 并完成 PG-B 其余规划工作。PNG 只作为 IFC ClashTrace 品牌身份基准；GIF 只提取安静空场、图形入场、短暂停顿、橘色字标显现、黑色终态收束的节奏，不复制旧建筑图形或旧品牌名称。D-026/D-027/D-028/D-055 的墙—管—碰撞—提示气泡、橘红强调、安全间距与静态终态保持不变。

**参考分析与 D-068 修订实现：** 参考 GIF 为 1120 × 360、74 帧、4,800 ms。修订候选保持 1120 × 360，但放慢为 6,400 ms / 25 fps：纯白空场 → 高质感墙体入场 → 完整绿色管线沿自身约 9° 延伸轴平移 → 橙色碰撞环 → 提示气泡 → 橘红字标左到右显现 → 近黑字标 → 静态终态。墙、管、环、气泡全部重新映射到同一高保真 3D 源坐标，不再使用粗糙绘制图层或错位的独立中心。

**格式与布局：** README 直接加载 433,015 B GIF，静态 PNG 为 60,391 B；不发布 WebP 或 AI 故事板。GIF 编码为 107 个有效帧、总时长 6,400 ms，无 Netscape 循环扩展，播放一次后停在终态。背景为纯 `#FFFFFF`，左侧高质感图标约 315 px 宽，54 px 字标起点 x=465 px，最小安全间距 82 px；Logo 与字标同步放大，README 保留 reduced-motion 静态 PNG 与描述墙—管—碰撞—检查含义的 alt 文本。

**本地浏览器验收：** Codex 内置 Chromium 在 1280 × 720 与 360 × 640 选择 D-068 直接 GIF；自然尺寸 1120 × 360，分别渲染为 948 × 305 与 328 × 105，均无横向溢出。6,800 ms 后显示清晰高质感静态终态，控制台 warning/error 为 0。该证据仍不替代 GitHub 页面实际渲染。

**资产、审计与声明：** `docs/assets/brand/asset-manifest.json` 冻结尺寸、时序、体积、SHA-256、来源和格式决定；`docs/pg-b-brand-motion.md` 记录不变量、参考逻辑、格式/回退、可访问性、元数据和停止门；README、研发页与主张台账均把 PG-B 标为本地技术候选而非 PASS。所有公开媒体小于 1 MiB，未检测到 EXIF/XMP/文本块/ICC、本机绝对路径或账户标记。

**D-068 完整回归：** `scripts/audit-pg-b.ps1` 审计 19 个公开候选、2 个仅本地文件、7 个小于 1 MiB 的公开媒体、PII/凭据/本机路径、计划等价性以及旧 WebP/故事板排除守卫。`scripts/test-pg-b.ps1`、完整 `scripts/test-pg-c.ps1` 与 G6/G4AI/G4/G5/G3C 回归全部通过；受控三方状态保持 8/8，净距两路保持 9/9。

**本地技术提交：** `14cd4212185fdaf7b59d49179804c2cfffdeda0b`（`step(PG-B): build finite README brand motion`），精确包含 17 个公开候选与继续只留本地的权威总纲、`scripts/audit-pg-b.ps1`。

**停止门：** PG-B 只有在用户对精确 GitHub 文件集给予行动时授权、仅通过登录态 Chrome 网页上传，并完成 README 桌面/窄屏播放一次、终态、替代文本、source/fallback、布局稳定性与加载体积回查及本地—远端映射后才能标记 `PASS`。本地候选不授权 GitHub/Sites 写入、部署、访问/权限/密钥变化、PG-E、VG、G7、移动计算或视频。

---

### L-0074 — PG-B 取消 GIF 并采用静态 GitHub Logo

**时间：** 2026-08-30 16:40–16:59 +08:00

**Gate：** `PG-B PASS`；最新闭合 Gate 为 PG-B

**用户决定：** D-069 取消 D-066 至 D-068 的动画输出，指定新提供的透明 PNG 直接作为 GitHub Logo。旧动画决策仍作为审计历史，但不再控制 README 或发布候选。

**静态实现：** 原始 1672 × 941 RGBA PNG 逐字节复制为 `docs/assets/brand/ifc-clashtrace-github-logo.png`，468,496 B，SHA-256 为 `b56f3a2a3d3ef5d37599a8a84c54cf1a5f56527057db487601d7bced24793142`。README 居中显示宽度 520 px并提供描述墙—管—碰撞—检查含义的 alt 文本。

**清理与合同：** GIF、旧锁定 PNG、源输入、四个动画图层、构建脚本和旧动效文档均从当前树移除，可由 Git 历史恢复。新 manifest、静态 PG-B 文档、测试与发布审计禁止 `.gif`、`picture/source`、reduced-motion 分支和构建链回流。

**本地验收：** Chromium 在 1280 × 720 与 360 × 640 下分别渲染为 520 × 293 与 328 × 185，均无横向溢出。静态 PG-B 审计与完整 PG-C/G6/G4AI/G4/G5/G3C 回归通过，受控状态保持 8/8，两条净距路线保持 9/9。

**外部闭合：** 用户批准精确12路径后，Chrome 形成 `9a48c0cc350328c8138440f2bfe60c3b68f5adab`（根5）→ `729298eef672937c3407a7e1545c7998cccd2cf4`（品牌2）→ `460d803b27bbc239ddce9daa73ccb8afe65b1e62`（研发页1）→ `8c133019642d7d7210a3eaacb230bf9a2a598c40`（文档2）→ `b4ce6d56282111c585f757042fa7cfefc057da0e`（测试2）的连续公开 `main` 技术链。真实 README 桌面渲染 520 × 293、窄屏 279 × 157，均保持 1672 × 941 自然尺寸、无横向溢出、无控制台警告/错误；12/12 路径映射通过。后续浏览器审计确认状态尾父检查点 `6f58bf246fc53cdc5d06a0dd61175cd24d1e3993`；可变当前 HEAD 不在公开文件内自引用，而由本地空同步提交记录。PG-B 因此闭合为 `PASS`。不授权 Sites、权限、密钥、PG-E、VG、G7、移动计算或视频动作。

### L-0075 — PG-E 工程情境样例技术候选与用户 UAT 门

**时间：** 2026-08-30 18:00–19:10 +08:00

**Gate：** `PG-E IN_PROGRESS / TECH_PASS_USER_UAT_PENDING`；最新闭合 Gate 仍为 PG-B

**工程包：** 按 D-070 生成一对 IFC4/米制/共享坐标的 CC0-1.0 单层双 IFC，空间层级为场地—建筑—`Level 01 · Coordination Zone`。MEP 含 6 个 `IfcPipeSegment`，结构含 4 个 `IfcWall` 与 2 个 `IfcBeam`，形成 36 个候选对；没有外部、雇主、客户、学校、私人或真实项目数据。

**独立哨兵：** 人工构造台账冻结墙碰撞、49 mm Warning、50 mm 非预警边界、200 mm 安全间隔、梁碰撞与缺几何失败关闭六对。50 mm 场景披露使用高于阈值 `0.01 µm` 的表示偏移，防止 Float32 三角化把名义等值推入阈值下方；冻结 `< 0.05 m` 规则不变、无 epsilon 死区。

**技术证据：** 发布核心对 36 对重复运行三次，规范化输出稳定，六个哨兵 6/6 一致；全量结果为硬碰撞 `2 CLASH / 28 CLEAR / 6 NOT_EVALUATED`，净距为 `1 WARNING / 27 CLEAR / 6 NOT_EVALUATED`。两次隔离生成必须与提交的两份 IFC、manifest 和 sentinel baseline 逐字节一致；文件头、许可、相对路径、哈希、空间层级、实体数、隐私与既有 PG-B/PG-C/G6 回归均纳入 `scripts/test-pg-e.ps1`。21路径本地技术候选提交为 `59fefc47191e0ef22cf9ae7903546e96d0c21ba4`，其中18路径可公开候选与3路径本地专用治理/审计范围由 `scripts/audit-pg-e.ps1 -ScopeCommit 59fefc4` 验证通过。

**未完成与停止门：** `docs/pg-e-engineering-uat.md` 提供用户 Chrome 清单和空白记录模板。尚未形成用户试用、浏览器版本/内存/控制台/问题修复复验、GitHub 行动时授权或公开映射，因此 PG-E/PG 不得标 `PASS`；未启动 VG/G7、未写 GitHub/Sites、未改访问/权限/密钥、未调用 live provider、未进入移动计算或视频。

### L-0076 — PG-E 拟真单层社区诊所修复

**时间：** 2026-08-30 20:30–21:30 +08:00

**Gate：** `PG-E IN_PROGRESS / TECH_PASS_USER_UAT_PENDING`；最新闭合 Gate 仍为 PG-B

**用户否决与重建：** 用户指出 L-0075 的分散测试体量不符合现实世界正常房屋结构，展示与测试说服力不足。按 D-071 将场景重建为 12 m × 8 m、层高 3.2 m 的单层社区诊所：四面外围护，两个带 0.8 m 对齐门洞的内隔墙带，200 mm 地坪，6 根 300 mm 方柱，3 根梁，A–C / 1–4 共7条轴网，以及沿中央通行带与两侧房间布置的7段具象吊顶管线；第8个导入管段故意缺失几何，仅用于失败关闭夹具。

**冻结边界与技术证据：** 墙/梁仍是唯一规则结构类型，柱、板和轴网只用于建筑上下文与查看器显示。8个管段记录对8道墙、3根梁形成88条候选记录，其中77条完成评估、11条因缺表示管段失败关闭；发布核心三次规范化输出稳定，六哨兵 6/6 一致。全量结果为硬碰撞 `4 CLASH / 73 CLEAR / 11 NOT_EVALUATED`，净距 `1 WARNING / 72 CLEAR / 11 NOT_EVALUATED`。S04 对圆管离散网格作 1.01786 mm 轴位补偿，使实际测得净距 `0.2000000477 m` 与独立 200 mm 设计预期严格一致；未改变检测阈值。

**本地技术提交：** `d0598830b674c36df722ba74499f9277cb08983f`（`repair(PG-E): rebuild realistic one-storey clinic`）精确修改22个文件：20个公开候选路径，以及继续只留本地的权威总纲与 PG-E 审计。完整允许清单仍为21个公开候选与3个本地专用路径；其中未变化的 `package.json` 与 PG-C 审计不进入本次提交。

**停止门：** 新版仍是合成展示模型，不是任意项目精度或施工合规证明。用户尚未确认建筑拟真，也未完成端到端 Chrome UAT、修复复验、精确公开文件授权或 GitHub 映射；因此 PG-E 不得标 `PASS`。未写 GitHub/Sites，未改访问/权限/密钥，未启动 VG/G7、移动计算或视频。

### L-0077 — 拟真诊所实例与 Logo v2 集体在线更新候选

**时间：** 2026-08-30 22:00–23:00 +08:00

**Gate：** `LOCAL_PREPARATION_IN_PROGRESS / COLLECTIVE_AUTHORIZATION_PENDING`；最新闭合 Gate 仍为 PG-B

**用户决定：** 用户确认 L-0076 的拟真诊所效果满意，同意放入网页实例选项；同时要求把“产品化优化”对话中重构的 1024 × 1024 透明 Logo v2 替换 GitHub README 原 Logo。用户要求先统计完整上传内容，随后再给一次集体授权。

**实现边界：** 实例选项必须同时闭合 UI 下拉、运行时双 IFC 路径和 G6/Sites 静态构建，确保线上不是空选项；检测仍为88对和6/6哨兵，不扩张规则。Logo v2 只逐字节覆盖规范 `ifc-clashtrace-github-logo.png`，README 改为420 px和新替代文本，不上传源命名 v2 PNG/SVG，不恢复 GIF。

**停止门：** 先完成本地回归、浏览器复验、29公开候选/3本地专用允许清单审计和实际变更路径统计。集体授权前不写 GitHub/Sites、不部署、不改访问/权限/密钥；旧 v1 的公开渲染与映射只作历史证据，不能当作 v2 已验收。

### L-0078 — 拟真诊所实例与 Logo v2 集体在线同步

**时间：** 2026-08-30 19:04–19:17 +08:00

**Gate：** `PG-B-R1 PASS / PG-E EXTERNAL_SYNC_PASS_USER_HOSTED_UAT_PENDING`；最新闭合 Gate 为 PG-B/PG-B-R1

**授权与 GitHub：** 用户在收到29文件、909,289字节公开清单及5项排除后明确回复“批准”。登录态 Chrome 按目录形成从 `33190860ebd64d1a3f2f2a215a0ffe74318136e4` 到 `b3b03046982011567f1108967dd84672cfad69bd` 的连续10提交技术链；固定远端 HEAD 下载与本地候选29/29逐字节一致。源命名 Logo v2 PNG/SVG、本地权威总纲和全部审计未上传。

**Logo 与 Sites：** 公共 GitHub Logo v2 自然尺寸1024×1024，Chrome 桌面420×420，360 px 公共页面约279.33×279.33，无横向溢出和控制台警告/错误。脱敏 Sites 源提交 `b85fb0d99e84392ff3642abfa3e4649b8dad1ed9` 生成所有者私有版本9，部署 `appgdep_6a9410376c248191952546a2416970fc` 成功；访问仍为custom、仅1位所有者、0组、0外部访客，未增加环境变量或密钥。

**在线诊所预检与停止门：** Chrome 从在线实例选择器加载双 IFC，得到88条记录（4 CLASH、1 WARNING、11 NOT_EVALUATED、72 CLEAR），3D进入真实构件聚焦状态且控制台0警告/错误。该次先前发布的外部同步因此闭合，但用户尚未在最终托管候选上完成完整试用记录；后续 L-0079 本地修复重新打开公开同步门，PG-E/PG仍不得标`PASS`。不公开 Sites、不改权限/密钥、不启动VG/G7、移动计算或视频。

### L-0079 — PG-E 审计闭环与 PG-B-R2 Logo 比例修复候选

**时间：** 2026-08-30 20:31–20:55 +08:00

**Gate：** `PG-B-R1 PASS / PG-B-R2 LOCAL_REPAIR_CANDIDATE_EXTERNAL_SYNC_PENDING / PG-E LOCAL_REPAIR_PASS_EXTERNAL_SYNC_PENDING_USER_HOSTED_UAT_PENDING`

**逻辑修复：** 明确模型包含7条具象吊顶管线与1个故意缺失几何的导入 `IfcPipeSegment`；88条候选记录由77条完成评估和11条失败关闭组成，不再把全部记录称为“已评估”。新增 `data/pg-e-uat-record.json`，冻结下一托管候选、13项用户检查、浏览器/哈希/性能观察和最终决定的空值门；本地测试拒绝状态字符串单独转为PASS。

**证据守卫：** 先前 GitHub 最终证据头 `b1727db600ae169eb603c5dd6863b417faef1b9f`、Sites版本9功能预检和版本10证据尾均保留为历史证据。当前发布审计不再把文档中的29/29字符串冒充活体远端核验；未提供完整远端SHA时输出 `PENDING_PUBLICATION`，授权上传后才可对30个公开候选逐字节校验。

**Logo 本地候选：** 从用户原1024×1024 RGBA源图裁取 `(36,172)–(986,850)`，得到951×679画布，内容四边保留48 px透明呼吸区；所有可见像素与alpha不缩放、不重绘，README宽度收敛至240 px。Chromium在1280×720与360×640均渲染240×171.3542，自然尺寸951×679，无横向溢出和控制台警告/错误。

**停止门：** 当前候选包含30个公开路径与本地专用治理/审计路径。仅完成本地实现、回归、浏览器验收与本地提交；不写GitHub/Sites、不部署、不改访问/权限/密钥。完成后必须向用户报告精确字节变更清单并等待一次集体授权。

### L-0080 — PG-E / PG-B-R2 公开同步与最终托管 UAT 闭环

**时间：** 2026-08-30 20:55–21:20 +08:00

**Gate：** `PG-B-R2 PASS / PG-E PASS`；下一阶段 `VG PLANNED / NOT_STARTED`

**授权与公开同步：** 用户先批准全部计划工作，并在 GitHub 公共提交动作前再次回复“确认”。登录态 Chrome 在既有30路径允许清单内完成15个实际差异文件的连续提交；技术头为 `3038d431157c0e1eb1e1f2b4a9870ddb01609921`，固定SHA下载与本地候选30/30逐字节一致。一次合并文档批次收到HTTP 400且未形成提交，拆分后在原范围内成功。源命名 Logo v2 PNG/SVG、本地权威总纲和全部审计仍未公开。

**Logo 验收：** 不可变提交页报告自然尺寸951×679，README渲染240×171.354 px，控制台0警告/错误。`raw/main` CDN曾短暂返回旧1024方图；不把可变缓存作为权威证据，以不可变提交渲染和30/30字节映射闭合 PG-B-R2。

**Sites 与托管 UAT：** 脱敏源提交 `dfe46aca1b033a7f80c409af0f9b907f619d3de4` 形成仅所有者Sites版本11，部署 `appgdep_6a942076ec308191a41d58f4cf02cf3e` 成功；访问仍为custom、1位所有者、0组、0外部访客，未更改访问策略或环境变量。Chrome在1707×876 CSS px、DPR 1.5运行拟真诊所，5569 ms返回88条记录（4 CLASH、1 WARNING、11 NOT_EVALUATED、72 CLEAR）；49/50/200 mm边界、墙/梁碰撞、缺失几何失败关闭、证据抽屉与3D定位均通过。改用C01重跑只返回1条CLASH，证明陈旧状态失效；中英、两体验、亮暗组合可用，主页与工作台控制台均0警告/错误。

**结论与边界：** 用户授权代理执行最终托管UAT；结构化记录13/13检查均为true且决定为`PASS`。PG-E与PG-B-R2因此闭合。Sites不公开，未改权限/密钥，未调用托管提供商，未启动VG/G7、移动计算或视频。

### L-0081 — VG 预制作审核候选与 G7A 边界重申

**时间：** 2026-08-30 21:20–23:30 +08:00

**Gate：** `VG IN_PROGRESS / REVIEW_CANDIDATE_USER_CONFIRMATION_PENDING`；G7A 未启动

**用户决定：** 按 D-075 采用共同创作并允许有限细化；定位为产品优先、学术可信支撑，保持现有品牌调性并采用克制、精确、原创的高质感呈现。用户选定 M2 `Masking the Masters` 与 V2 `Warm Modern / Ava`，要求在制作时保持最佳旁白—音乐音量差；PG-E 不用于 AI，AI 仅使用小型受控案例。按 D-076 再次确认 VG 不生成最终成片，最终旁白、正式录屏、实时 AI、剪辑混音、字幕和渲染全部留待 VG 通过后的 G7A。

**审核候选：** `docs/vg-video-preproduction.md` 汇总172.2秒、13句/13镜、286词英语旁白与简体中文待人工复核草稿、M2 100 BPM 源片段与节拍网格、V2发音/语速/混音目标、三张统一章节风格帧、真实录屏与备用镜头、C01/C03/C05/C08 AI最小化流程、字幕策略、隐私/连续性/许可/事实台账。三组原创程序化SFX试听 P1/P2/P3 只用于 VG 选择，不是最终声音设计。

**验证与停止门：** `scripts/test-vg-preproduction.ps1` 输出边界 PASS、脚本13行、镜头13行、风格帧3张、SFX试听3组、最终视频类文件0个，并保持 `VG_STATUS=REVIEW_PENDING`。用户尚未批准完整脚本/镜头/风格/音频/SFX组合，因此 VG 不得标 `PASS`；不生成最终旁白、不正式录屏、不调用实时 AI、不制作或渲染成片，不写 GitHub/Sites/YouTube，不改访问/权限/密钥。

### L-0082 — VG 中文审核包、关键帧表与零费用女声修订

**时间：** 2026-08-30 23:30–2026-08-31 00:30 +08:00

**Gate：** `VG IN_PROGRESS / REVIEW_CANDIDATE_USER_CONFIRMATION_PENDING`；G7A 未启动

**用户决定与成本边界：** 按 D-077，VG 增加可复建关键帧预处理和中文主审阅稿，英语台本以英中逐句对照保留；SFX 选择 P1 `Dry Precision`。Azure 付费层即使项目非商业也会按资源使用产生费用，因此默认发行音源改为本机 `Kokoro-82M`，试听为 `af_heart` 与 `af_bella`，使用 Apache-2.0 许可且不产生语音 API 费用。Ava 只保留为 V2 表演方向参考。

**预处理资产：** `scripts/build-vg-keyframe-board.py` 从真实项目截图、既有三张风格帧和规范品牌资产构建13张1920×1080关键帧，以及一张3840×2160的 `VG-keyframe-table-4k.png`；不生成假产品 UI，不把关键帧冒充最终成片。`docs/vg-video-preproduction.zh-CN.md` 将阶段边界、创意、镜头、音频、隐私、许可、事实与 G7A 入口全部中文化，第4节保留286词英语原台本并并列简体中文待人工复核稿。

**G7A 建议与授权：** 按 D-078，主任务建议 `gpt-5.6-sol` + `xhigh`；意图冻结的机械循环可临时用 `high`，额度与工期允许时仅做一次 `max` 导出前审计。G7A 可自由使用 Remotion、video-shotcraft、其他已安装技能/插件及免费额度，以质量为首要目标；不自动授权付费购买、超额费用、公开发布、访问变化或新增隐私暴露。

**停止门：** D-079 后用户已对 H 与其余审核内容给出基本肯定，并新增 C 自然度挑战者；仍需在 H/C 中选择发行声线并给出最终 VG 放行。此前不生成最终旁白、不正式录屏、不调用实时 AI、不开始 Remotion 成片、不制作最终字幕/混音/渲染，不写 GitHub/Sites/YouTube。

---

### L-0083 — VG 录屏入口、关键帧占位门与 Chatterbox 试听

**时间：** 2026-08-31 00:30–01:15 +08:00

**Gate：** `VG IN_PROGRESS / VOICE_FINALIST_SELECTION_PENDING`；G7A 未启动

**正式录屏入口修订：** 按 D-079，PG-E 不再允许通过网页实例下拉载入。S05 必须连续录制 A1 `Choose MEP IFC` → 中性公开演示文件夹选 MEP → A2 `Choose structural IFC` → 选配对结构文件 → 共享坐标确认 → Run；文件选择器不得出现用户名、仓库路径或私人位置。S11 则反向固定为在 B · DEMO 点选 `Review pack · C01 / C03 / C05 / C08`、点击 `Load example` 并运行四次真实受控计算；不得为受控包打开原生文件选择器。

**关键帧占位治理：** 用户指出4K总表多次复用旧素材。审核包现明确这些内容只是 VG 赶时间的构图权宜之计，所有现有关键帧位图均不可直接作为产品证据进入正式时间线。新增 S01–S13 分组替换矩阵和 `FINAL_SOURCE_VERIFIED` 强制入库门：主页/工作台、PG-E、研发证据、受控 review pack 与支持边界必须分别换成当前、正确、对应的全新产品录屏/截图；4K总表底部也加入醒目中文占位声明。

**新增零费用声音候选：** H `Kokoro af_heart` 继续作为稳妥推荐；B 降为被否决/备用。新增 C：Resemble AI Chatterbox 内置默认女声，MIT许可。一次完整本地安装因超出 VG 时间预算被主动中止且与业务依赖隔离；随后仅向官方免费 Hugging Face Space 发送公开试听文案，显式传入空参考音频，未上传或克隆任何真人声音，固定 seed/克制参数生成12.8秒、24kHz、单声道PCM16试听。输出带模型内置不可感知水印；若 C 入选，G7A 应冻结官方权重后在本机完成最终合成，不依赖托管可用性。

**当前确认面：** 用户认为 H 不错，除本轮三项修订外其余审核内容基本无异议。VG 只剩 H/C 听感决选和最终放行；自动验收保持中英台本13/13、镜头13/13、关键帧13张/总表1张、P1、零费用声音文件3份、决选2份、最终视频类文件0。

---

### L-0084 — C 男声淘汰与 Parler 命名女声 D

**时间：** 2026-08-31 01:15–01:40 +08:00

**Gate：** `VG IN_PROGRESS / H_D_VOICE_SELECTION_PENDING`；除人声外的 VG 方案已获用户基本确认，G7A 未启动

**观察与纠正：** 用户指出 C 实际为男声。该判断正确：40ms窗自相关基频检查得到 C 中位117.6Hz（四分位102.6–164.4Hz），而H为193.5Hz（175.2–220.2Hz）。C因此不再称default female，文件重命名为`V2-zero-cost-chatterbox-default-male-rejected.wav`并只留作审计，禁止用于最终旁白。

**D 女声候选：** 选择无需参考人声的 Parler-TTS Mini v1.1，而不是用来源不清的女性录音做克隆。官方模型为Apache-2.0、支持用文字控制性别/音高/语速/录音质感，并提供34个命名speaker。D固定`Laura`，描述为女性、温暖、自然、克制、中速、稍低音高、清晰近讲、无播音腔、几乎无底噪/混响；只向官方免费Space发送公开产品试听文案，不发送参考音频。输出为16.974秒、44.1kHz单声道PCM16，中位基频180.0Hz（168.3–205.1Hz），客观落在女声区且比H略低。

**停止门：** 用户已说明其余事务没有问题，VG现在仅剩H `Kokoro af_heart`与D `Parler Laura`的听感选择。选定并明确`VG通过`前，不生成最终旁白、不正式录屏、不进入Remotion成片、实时AI、最终字幕/混音/渲染或外部发布。

---

### L-0085 — H 冻结与 VG 创意内容放行

**时间：** 2026-08-31（本轮 H 决选）

**Gate：** `VG CONTENT APPROVED / REPAIR_AND_PUBLIC_CHECKPOINT_PENDING`；`G7A NOT_STARTED`

**最终决定：** 用户在明确“其他事务没有问题、仅剩定下人声”后选择 H。正式旁白声源冻结为本地 Apache-2.0 `Kokoro-82M af_heart`，V2 Warm Modern 继续定义表演，目标114–118 wpm；G7A 合成前需冻结模型/voice-pack hash、设置、读音覆盖和最终WAV hash。D `Parler Laura`降为未采用备份，除非用户重新开启此决定，否则不得替换H；C继续只作男声淘汰审计。

**VG内容确认面：** 13句双语台本、172.2秒/13镜分镜、SF01→SF03视觉系统、13张关键帧与`FINAL_SOURCE_VERIFIED`逐镜替换门、PG-E双原生文件选择器、C01/C03/C05/C08受控点选、M2节拍与音量关系、P1 SFX、事实/隐私/许可/成本边界均已获用户批准。该批准冻结创意内容，但不替代总纲要求的 VG GitHub 阶段检查点。

**边界：** 正式 VG `PASS` 与 G7A 启动资格仍待有限修复和公开检查点；本回合不合成最终旁白、不正式录屏、不调用实时AI、不创建Remotion成片、不剪辑/混音/渲染，也不写GitHub/Sites/YouTube或改变访问、权限、密钥。

---

### L-0086 — VG 有限逻辑修复与精确公开候选冻结

**时间：** 2026-08-31

**Gate：** `VG LOCAL_REPAIR_PASS / PUBLIC_CHECKPOINT_PENDING`；`G7A NOT_STARTED`

**修复结果：** 英语台本由审核候选的 286 词收敛为 272 词，13 句均通过 `114 wpm + 420 ms` 的逐镜守卫；S11 改为一次点选并加载 `C01/C03/C05/C08` 四记录受控批次。M2 数据明确区分源文件相位 `0.106208955 s` 与裁切后首拍 `0.000 s`，严格网格以 `98.258% / 8.377 ms / 2.538 ms` 通过。9 个已选 video-shotcraft 卡片冻结精确 card 文档、TSX demo、参考媒体与 SHA-256。

**公开边界：** `docs/vg-publication-manifest.md` 冻结精确 25 路径候选；只包含审核文档、可复现脚本、分析 JSON、带占位警示的 4K 总表、三张风格帧与音频文字台账。试听 MP3/WAV、Mixkit 源音乐、13 张单独 KF 占位、私有总纲、中间渲染和任何成片均保持本地且由 `.gitignore` 保护。

**停止门：** 本地/公开双审计通过只代表候选就绪，不构成上传授权。等待用户另行集体授权后，才可执行 GitHub Chrome 检查点与远端映射复验；在该检查点闭合前 G7A 保持未启动。

---

## 21. 每日状态摘要模板

```text
日期：
当前 Gate：
总体状态：ON_TRACK / AT_RISK / BLOCKED

今日完成：
1.
2.

量化结果：
- 测试：
- 正确性：
- 性能：
- 数据案例：

失败/异常：
1.

范围变化：无 / 有（对应决策 ID）
隐私与许可证变化：无 / 有
本地 Git：提交主题 / SHA / 工作树状态
GitHub 检查点：URL / 远程 SHA / 上传时间 / 回查结论

明日唯一主任务：
需要用户决定：无 / 有
```

---

## 22. 总纲审批区

当前审批状态：`APPROVED`

用户可选择：

- 确认本次修订并正式开始；
- 批准但要求指定修改；
- 暂不批准，继续讨论项目范围；
- 否决当前方向并重新规划。

用户已明确批准原 `0.2.0-draft` 并启动项目，随后批准 0.3.0 功能/视频扩展、0.4.0 展示就绪扩展，并在 2026-08-30 形成 0.4.1 视频发布路线修订。G0A 至 G5-R1、本地/公开 G6、有限 G6-R1、PG-C、PG-B、PG-B-R2 与 PG-E/PG 均已闭合为 `PASS`，Sites 仍为所有者私有。VG 创意内容已获批准并完成有限逻辑修复，冻结 H `Kokoro-82M af_heart`、M2、P1、272词/13句/13镜、SF01–SF03、关键帧逐镜替换门、PG-E双原生文件选择器、C01/C03/C05/C08受控点选及9个镜头卡来源；当前为 `VG LOCAL_REPAIR_PASS / PUBLIC_CHECKPOINT_PENDING`。G7A=`NOT_STARTED`，推荐`gpt-5.6-sol` + `xhigh`；尚未生成最终旁白、正式录屏、实时AI、Remotion成片、剪辑混音或渲染。精确25路径GitHub检查点等待用户集体授权；G7B/G7未启动，不公开Sites、不改访问权限、不配置托管密钥。
